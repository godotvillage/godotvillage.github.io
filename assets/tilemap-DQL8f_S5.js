const n=`---\r
title: 教程\r
index: false\r
icon: laptop-code\r
sidebar: true\r
comment: false\r
lastUpdated: false\r
editLink: false\r
contributors: false\r
breadcrumb: true\r
sticky: true\r
star: true\r
category:\r
  - 教程\r
author: false\r
pageInfo: false\r
toc: false\r
cover: https://plus.unsplash.com/premium_photo-1664297854029-21ebd7e03b24?ixlib=rb-4.1.0&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&q=80&w=1742\r
date: 2025-10-11\r
---\r
\r
## 📚 欢迎来到 Godot 新手村教程中心！\r
\r
这里是新手村的知识宝库，汇集了丰富的 Godot 学习资源，帮助你从零基础到游戏开发高手！\r
\r
<!-- more -->\r
### 🎥 视频教程\r
\r
#### B站UP主\r
我们特别收录了B站上的 Godot 精品视频教程。落叶轻羽老师和其他几位老师以其深入浅出的讲解风格和丰富的实战经验，为广大 Godot 学习者提供了优质的学习内容。\r
\r
- 🎯 **系统性学习路径** - 从基础概念到高级技巧，循序渐进\r
- 💡 **实战项目案例** - 通过具体项目学习 Godot 开发\r
- 🛠️ **最佳实践分享** - 积累多年的开发经验总结\r
\r
- [落叶轻羽B站视频列表](/tutorial/luoye)\r
- [其他B站视频列表](/tutorial/otherbilibili)\r
\r
### 📖 官方教程\r
\r
#### 管理员精选教程\r
新手村管理员团队精心整理和创作的官方教程内容，涵盖以下教程：\r
#### 功能教程\r
- [ ] 3D变换\r
- [x] [有限状态机](/tutorial/fsm)\r
- [x] [组件开发](/tutorial/component)\r
- [ ] 资源系统开发\r
- [ ] 动画播放器和动画树\r
- [x] [文件读写与存档系统](/tutorial/file) <Badge text="编写中" type="tip" vertical="middle" />\r
- [ ] 输入事件系统详解\r
- [x] [信号系统](/tutorial/signal)\r
- [x] [Tilemap详解](/tutorial/tilemap)\r
- [ ] 粒子系统\r
- [ ] UI系统和主题开发\r
- [x] [自动加载和单例模式](/tutorial/single)\r
- [x] [网络系统详解](/tutorial/mutiplayer_tutorial/network-architecture-overview)\r
- [x] [多人游戏开发](/tutorial/mutiplayer_tutorial/network-architecture-overview)\r
- [ ] 窗口系统详解\r
- [x] [导航系统以及A星算法](/tutorial/navigation)\r
- [ ] 物理系统\r
\r
#### 着色器教程\r
- [x] [着色器简介](/tutorial/shader/intro)\r
- [x] [着色器常用数学](/tutorial/shader/shader-math)\r
- [x] [着色器实现思路](/tutorial/shader/shader-implementation)\r
- [x] [着色器常见案例](/tutorial/shader/shader-common-cases)\r
- [ ] 2D顶点着色器\r
- [ ] 2D片段着色器\r
- [ ] 2D灯光着色器\r
- [ ] 3D顶点着色器\r
- [ ] 3D片段着色器\r
- [ ] 3D灯光着色器\r
- [ ] 粒子着色器\r
- [ ] 雾着色器\r
- [ ] 天空着色器\r
\r
#### 案例教程\r
- [ ] 2D游戏案例\r
- [ ] 3D游戏案例\r
- [ ] [实现一个2D画图软件](/tutorial/example/painter)  <Badge text="编写中" type="tip" vertical="middle" />\r
- [x] [局域网聊天软件](/tutorial/example/chat)\r
- [ ] 手把手带你实现一个桌宠\r
- [ ] 让你的角色产生智能\r
- [ ] 更改游戏中的键位\r
- [ ] 手把手实现一个音乐播放器\r
\r
#### 多人游戏教程\r
本系列教程基于 2D Multiplayer Course 项目，深入讲解 Godot 4 多人游戏开发的核心概念和实现方式。\r
\r
🎥 **配套视频教程**: [2D Multiplayer Course - YouTube](https://www.youtube.com/watch?v=F3P5uWgJrj0)\r
\r
- [x] [多人游戏教程概览](/tutorial/mutiplayer_tutorial/multiplayer-overview) - 教程系列介绍与学习路径\r
- [x] [网络架构概述](/tutorial/mutiplayer_tutorial/network-architecture-overview) - 网络架构设计、权威模式、RPC通信\r
- [x] [玩家输入同步机制](/tutorial/mutiplayer_tutorial/player-input-synchronization) - PlayerInputSynchronizerComponent、输入采集与同步\r
- [x] [大厅与连接管理](/tutorial/mutiplayer_tutorial/lobby-and-connection-management) - ENetMultiplayerPeer、LobbyManager、连接与断线处理\r
- [x] [敌人AI与状态管理](/tutorial/mutiplayer_tutorial/enemy-ai-and-state-management) - 敌人状态机、服务器权威 AI\r
- [x] [攻击与碰撞系统](/tutorial/mutiplayer_tutorial/attack-and-collision-system) - 子弹、Hitbox/Hurtbox、伤害同步\r
- [x] [场景管理与玩家生命周期](/tutorial/mutiplayer_tutorial/scene-management-and-player-lifecycle) - MultiplayerSpawner、玩家生成与复活\r
- [x] [CallableStateMachine实现](/tutorial/mutiplayer_tutorial/callable-state-machine-implementation) - 自定义状态机实现详解\r
\r
### 📝 学习建议\r
\r
1. **新手推荐路径**：先观看基础视频教程，再结合官方文档进行实践\r
2. **进阶学习**：参与社区讨论，分享学习心得，互相学习\r
3. **实战练习**：尝试完成小项目，在实践中巩固知识\r
\r
### 🤝 互动交流\r
\r
- 💬 **[讨论区](/discussion/)** - 与其他学习者交流心得，解答疑问\r
- 🎮 **[游戏中心](/game/)** - 查看和分享学习成果\r
- 🌱 **[填坑农场](/farm/)** - 管理你的学习项目，监督学习进度\r
\r
---\r
\r
*在这里，每个人都可以找到适合自己的学习方式。让我们一起在 Godot 的世界中探索、学习、成长！*\r
`,e=`---\r
title: 在Godot中使用组件\r
sticky: false\r
star: false\r
category:\r
  - 教程\r
tag:\r
  - 设计模式\r
date: 2025-10-13\r
---\r
\r
\`\`\` ::close 教程使用环境\r
- Godot v4.5.stable \r
- Windows 10 (build 19045)\r
\`\`\`\r
\r
## 继承模式 vs 组件模式\r
\r
在游戏开发中，有两种主要的架构模式：**继承模式**和**组件模式**。\r
\r
<!-- more -->\r
\r
### Godot的继承模式\r
\r
\`\`\`gdscript\r
# 基础角色类\r
class Character extends Node2D:\r
    # 实现基础的逻辑，例如攻击受击、属性管理。\r
    pass\r
\r
# 玩家类，继承自角色\r
class Player extends Character:\r
    # 实现玩家逻辑，在角色的基础上额外可受到玩家的操控。\r
    pass\r
\r
# 敌人类，继承自角色\r
class Enemy extends Character:\r
    # 实现敌人逻辑，玩家不可操控。会对玩家做出攻击。\r
    pass\r
\r
# 地面敌人类，继承自敌人\r
class GroundEnemy extends Enemy:\r
    # 敌人的子类，会在地面接近玩家，做出攻击。\r
    pass\r
\r
# 飞行敌人类，继承自敌人\r
class FlyingEnemy extends Enemy:\r
    # 敌人的子类，会在空中接近玩家，做出攻击。\r
    pass\r
\r
\`\`\`\r
\r
Godot的继承模式特点：\r
- Godot采用节点继承的方式构建游戏对象\r
- 每个节点都有特定的功能，如Area2D专门处理物理碰撞检测\r
- 通过继承和组合节点来构建复杂的游戏对象\r
\r
!!! note 思考\r
如果此时希望设计一种敌人，既可以飞天，也可以落地，应该继承自哪个类？\r
\`\`\`gdscript\r
class FlyingAndGroundEnemy extends ?\r
\`\`\`\r
我们会发现，继承自哪个类都不合适，如果继承自\`Enemy\`，则地面和空中的逻辑需要再实现一次。而无论是继承自\`GroundEnemy\`还是\`FlyingEnemy\`，另一种移动方式也需要实现。为了减少需要实现的代码逻辑。我们可以尝试使用组件来解决这个问题。\r
!!!\r
\r
### 组件模式架构\r
\r
#### 各种组件类\r
\`\`\`gdscript\r
class InputComponent extends Node:\r
    # 操控组件，处理输入逻辑\r
    pass\r
\r
class AttackComponent extends Node:\r
    # 攻击组件，处理攻击逻辑\r
    pass\r
\r
class HitComponent extends Node:\r
    # 受击组件，处理受击逻辑\r
    pass\r
\r
class AttributeComponent extends Node:\r
    # 属性组件，管理角色属性\r
    pass\r
\r
class FlyComponent extends Node:\r
    # 飞行组件，处理飞行逻辑\r
    pass\r
\r
class GroundMoveComponent extends Node:\r
    # 地面运动组件，处理地面移动逻辑\r
    pass\r
\`\`\`\r
\r
\r
#### 具体实体的组件组合\r
\r
\`\`\`gdscript\r
# 玩家实体，包含玩家所需的组件\r
class Player extends Node2D:\r
    var input_component: InputComponent\r
    var attack_component: AttackComponent\r
    var hit_component: HitComponent\r
    var attribute_component: AttributeComponent\r
    \r
\r
# 地面敌人实体，包含地面敌人所需的组件\r
class GroundEnemy extends Node2D:\r
    var attack_component: AttackComponent\r
    var hit_component: HitComponent\r
    var attribute_component: AttributeComponent\r
    var ground_move_component: GroundMoveComponent\r
    \r
\r
# 飞行敌人实体，包含飞行敌人所需的组件\r
class FlyingEnemy extends Node2D:\r
    var attack_component: AttackComponent\r
    var hit_component: HitComponent\r
    var attribute_component: AttributeComponent\r
    var fly_component: FlyComponent\r
\r
    \r
\`\`\`\r
\r
其他引擎的组件模式（如Unity）：\r
- 游戏对象是一个通用的容器\r
- 功能通过添加不同的组件来实现\r
- 组件之间相互独立，易于复用和组合\r
\r
!!! note 思考\r
有了组件模式，我们就可以根据实体的功能任意组合需要的组件。\r
例如既可以飞行也可以落地的敌人，我们可以这样实现。\r
\`\`\`gdscript\r
class FlyingAndGroundEnemy extends Node2D:\r
    var attack_component: AttackComponent\r
    var hit_component: HitComponent\r
    var attribute_component: AttributeComponent\r
    var ground_move_component: GroundMoveComponent\r
    var fly_component: FlyComponent\r
\`\`\`\r
如果此时我还想有一个无法移动，也无法受伤，但却会对玩家造成伤害的敌人（可能看起来是个陷阱），我们很快就可以组装起一个需要的实体：\r
\`\`\`gdscript\r
class TrapEnemy extends Node2D:\r
    var attack_component: AttackComponent\r
    var attribute_component: AttributeComponent\r
\`\`\`\r
!!!\r
\r
## 案例学习\r
以上代码中已经讲解了组件模式的开发思路，也展示了部分代码。接下来则会使用一个案例来说明在项目中，如何使用组件模式。\r
\r
### 操控组件\r
首先按照常见的方式来创建一个游戏项目。\r
#### 非组件模式\r
创建了一个玩家(\`player_extends.tscn\`)场景，并为玩家添加了根据鼠标输入移动。则可以有以下代码：\r
\`\`\`gdscript\r
# player_extends.gd\r
extends CharacterBody2D\r
\r
const SPEED = 300.0\r
\r
func _physics_process(_delta: float) -> void:\r
	var direction := Input.get_vector("ui_left", "ui_right", "ui_up", "ui_down")\r
	velocity = direction * SPEED\r
	move_and_slide()\r
\`\`\`\r
玩家可以根据鼠标的输入（上下左右方向键）移动。\r
\r
如果将以上代码改为组件式应该如何编写代码呢？\r
\r
#### 组件模式\r
首先我们应先创建一个\`input_component\`作为接收输入和控制移动的脚本，并带有一个direction变量供玩家场景读取。组件可以是脚本，也可以是场景。为了方便演示，此处新建一个脚本。\r
\`\`\`gdscript\r
# input_component.gd\r
class_name InputComponent\r
extends Node\r
\r
var direction: Vector2 = Vector2.ZERO\r
\r
func _physics_process(_delta: float) -> void:\r
	direction = Input.get_vector("ui_left", "ui_right", "ui_up", "ui_down")\r
\r
\`\`\`\r
然后新建一个玩家场景(\`player_use_component.tscn\`)，在其中添加必要元素后，新增加一个\`InputComponent\`节点，然后编写玩家场景脚本(\`player_use_component.gd\`)。代码如下：\r
\`\`\`gdscript\r
extends CharacterBody2D\r
# 引用输入组件\r
@onready var input_component: InputComponent = $InputComponent\r
\r
const SPEED = 300.0\r
\r
func _physics_process(_delta: float) -> void:\r
	# 获取输入组件中的方向\r
	velocity = input_component.direction * SPEED\r
	move_and_slide()\r
\`\`\`\r
目前场景节点树如下：\r
![](/images/tutorial/component/input_component_node_tree.png)\r
\r
至此我们完成了对普通的处理输入到使用组件处理输入的转变。可能这看起来有些麻烦，因为我们并没有从案例中看到任何好处。那么我们可以继续学习一个案例，来体会组件式的结构带来的封装的便利。\r
\r
### 再来一个案例\r
暂时没想好做啥，先空着`,r=`---
title: Godot开发局域网聊天软件
author:
  - Moshangzhu
date: 2026-03-19
category:
  - 教程
tag:
  - 案例
---

## 教程简介

本教程将通过开发一个局域网聊天软件，演示 Godot 4 的多人网络连接、RPC 通信、大厅系统等核心功能。

完成该教程后，你将会对 \`ENetMultiplayerPeer\`、RPC 通信模式、自动加载单例等相关知识点有一定了解。

<!-- more -->

### 获取项目代码

!!! note 项目代码在文章末尾，请下载后导入 Godot 4.6 使用
!!!
### 环境

教程使用 \`Godot 4.6\` 版本。

## 核心概念

### ENetMultiplayerPeer

Godot 4 提供的 P2P 网络连接模块，支持创建服务器和客户端。

\`\`\`gdscript
# 创建服务器
var peer := ENetMultiplayerPeer.new()
peer.create_server(port, max_players)

# 创建客户端
var peer := ENetMultiplayerPeer.new()
peer.create_client(server_ip, port)
\`\`\`

### RPC 通信模式

| RPC 模式 | 说明 |
|---------|------|
| \`@rpc("any_peer", "call_local")\` | 发送方本地执行，消息转发至服务器 |
| \`@rpc("authority", "call_local")\` | 服务器权威广播给所有客户端 |
| \`@rpc("authority", "call_remote")\` | 仅远程客户端执行 |

### 自动加载（单例）

用于管理全局状态，如网络连接、聊天逻辑等。通过 \`project.godot\` 的 \`[autoload]\` 配置。

## 项目架构

\`\`\`
┌─────────────────────────────────────────────────────────┐
│                    Autoload (单例)                      │
├─────────────────────┬─────────────────────────────────┤
│   NetworkManager     │         ChatManager               │
│  - 连接/断开管理     │  - 大厅玩家列表                   │
│  - ENetMultiplayer  │  - RPC 消息广播                   │
└─────────────────────┴─────────────────────────────────┘
         │                        │
         ▼                        ▼
┌─────────────────────┬─────────────────────────────────┐
│    MainMenu.tscn    │         Lobby.tscn               │
│  - 创建/加入服务器  │  - 聊天消息显示                   │
│  - 用户名输入       │  - 玩家列表                       │
└─────────────────────┴─────────────────────────────────┘
\`\`\`

## 实现步骤

### 第一章 创建项目与自动加载

!!! note
请先创建 Godot 项目，并创建 \`scenes\` 和 \`scripts\` 目录
!!!
#### 配置自动加载

在 \`project.godot\` 中添加自动加载脚本：

\`\`\`ini
[application]
config/name="局域网聊天"
run/main_scene="res://scenes/MainMenu.tscn"

[autoload]
NetworkManager="*res://scripts/NetworkManager.gd"
ChatManager="*res://scripts/ChatManager.gd"
\`\`\`

#### NetworkManager - 网络管理

创建 \`scripts/NetworkManager.gd\`，负责网络连接管理：

\`\`\`gdscript
extends Node

# 网络配置
const DEFAULT_PORT := 3000
const MAX_PLAYERS := 32

# 连接状态
enum ConnectionState {
	DISCONNECTED,
	HOSTING,
	CONNECTED
}

var current_state: ConnectionState = ConnectionState.DISCONNECTED
var username: String = "Player"

# 信号定义
signal connection_succeeded()
signal connection_failed()
signal server_disconnected()
signal peer_connected(peer_id: int)
signal peer_disconnected(peer_id: int)

func _ready() -> void:
	multiplayer.connected_to_server.connect(_on_connected_to_server)
	multiplayer.connection_failed.connect(_on_connection_failed)
	multiplayer.server_disconnected.connect(_on_server_disconnected)
	multiplayer.peer_disconnected.connect(_on_peer_disconnected)
	multiplayer.peer_connected.connect(_on_peer_connected)

## 创建服务器（主机）
func create_server(port: int, player_name: String) -> bool:
	username = player_name

	var peer := ENetMultiplayerPeer.new()
	var error := peer.create_server(port, MAX_PLAYERS)

	if error != OK:
		push_error("创建服务器失败: %s" % error)
		return false

	multiplayer.multiplayer_peer = peer
	current_state = ConnectionState.HOSTING

	# 主机自己加入大厅
	ChatManager.add_player_to_lobby(1, username)
	ChatManager.send_system_message("%s 创建了聊天室" % username)

	connection_succeeded.emit()
	return true

## 加入服务器（客户端）
func join_server(ip: String, port: int, player_name: String) -> bool:
	username = player_name

	var peer := ENetMultiplayerPeer.new()
	var error := peer.create_client(ip, port)

	if error != OK:
		push_error("连接服务器失败: %s" % error)
		return false

	multiplayer.multiplayer_peer = peer
	return true

## 回调：连接成功
func _on_connected_to_server() -> void:
	current_state = ConnectionState.CONNECTED
	ChatManager.request_join_lobby.rpc_id(1, username)
	connection_succeeded.emit()

## 回调：玩家断开
func _on_peer_disconnected(peer_id: int) -> void:
	if not multiplayer.is_server():
		return
	var player_name := ChatManager.remove_player_from_lobby(peer_id)
	if player_name != "":
		ChatManager.send_system_message("%s 离开了聊天室" % player_name)
\`\`\`

#### ChatManager - 聊天管理

创建 \`scripts/ChatManager.gd\`，负责大厅和消息管理：

\`\`\`gdscript
extends Node

# 大厅玩家数据
var lobby_players: Dictionary = {}

# 信号
signal player_joined(peer_id: int, player_name: String)
signal player_left(peer_id: int, player_name: String)
signal chat_message_received(peer_id: int, player_name: String, message: String)
signal system_message_received(message: String)
signal lobby_updated(players: Dictionary)

## RPC: 请求加入大厅（客户端发送，服务器处理）
@rpc("any_peer", "call_local", "reliable")
func request_join_lobby(player_name: String) -> void:
	if not multiplayer.is_server():
		return

	var sender_id := multiplayer.get_remote_sender_id()

	if not lobby_players.has(sender_id):
		lobby_players[sender_id] = player_name
		player_joined.emit(sender_id, player_name)
		lobby_updated.emit(lobby_players)

		# 通知其他客户端有新玩家加入
		notify_player_joined.rpc_id(sender_id, player_name)
		# 将当前大厅玩家列表同步给新玩家
		sync_lobby_state.rpc_id(sender_id, lobby_players)

## RPC: 通知其他客户端有新玩家加入
@rpc("authority", "call_remote", "reliable")
func notify_player_joined(peer_id: int, player_name: String) -> void:
	if not lobby_players.has(peer_id):
		lobby_players[peer_id] = player_name
		player_joined.emit(peer_id, player_name)
		lobby_updated.emit(lobby_players)
		system_message_received.emit("%s 加入了聊天室" % player_name)

## RPC: 同步大厅状态给新玩家
@rpc("authority", "call_remote", "reliable")
func sync_lobby_state(players: Dictionary) -> void:
	lobby_players = players
	lobby_updated.emit(lobby_players)

## RPC: 客户端发送聊天消息到服务器
@rpc("any_peer", "call_local", "reliable")
func receive_message_from_client(message: String) -> void:
	if not multiplayer.is_server():
		return
	var sender_id := multiplayer.get_remote_sender_id()
	var player_name: String = lobby_players.get(sender_id, "未知")
	broadcast_chat_message.rpc(sender_id, player_name, message)

## RPC: 聊天消息（广播）
@rpc("authority", "call_local", "reliable")
func broadcast_chat_message(peer_id: int, player_name: String, message: String) -> void:
	chat_message_received.emit(peer_id, player_name, message)

## RPC: 系统消息（广播）
@rpc("authority", "call_local", "reliable")
func broadcast_system_message(message: String) -> void:
	system_message_received.emit(message)

## 添加玩家到大厅
func add_player_to_lobby(peer_id: int, player_name: String) -> void:
	lobby_players[peer_id] = player_name
	player_joined.emit(peer_id, player_name)
	lobby_updated.emit(lobby_players)

## 从大厅移除玩家
func remove_player_from_lobby(peer_id: int) -> String:
	var player_name: String = lobby_players.get(peer_id, "")
	if player_name != "":
		lobby_players.erase(peer_id)
		player_left.emit(peer_id, player_name)
		lobby_updated.emit(lobby_players)
	return player_name

## 获取大厅玩家列表
func get_lobby_players() -> Dictionary:
	return lobby_players.duplicate()

## 清空大厅
func clear_lobby() -> void:
	lobby_players.clear()
	lobby_updated.emit(lobby_players)

## 发送聊天消息
func broadcast_message(player_name: String, message: String) -> void:
	receive_message_from_client.rpc(message)

## 发送系统消息
func send_system_message(message: String) -> void:
	broadcast_system_message.rpc(message)
\`\`\`

### 第二章 主菜单场景

#### MainMenu.tscn 场景结构

创建 \`scenes/MainMenu.tscn\`：

\`\`\`
MainMenu (Control)
└── PanelContainer
    └── VBoxContainer
        ├── Title (Label) - "局域网聊天"
        ├── UsernameSection (VBoxContainer)
        │   ├── UsernameLabel (Label) - "用户名"
        │   └── UsernameLineEdit (LineEdit)
        ├── HostSection (VBoxContainer)
        │   ├── HostLabel (Label) - "创建聊天室"
        │   ├── PortLabel (Label) - "端口"
        │   ├── HostPortSpinBox (SpinBox)
        │   └── HostButton (Button) - "创建"
        ├── JoinSection (VBoxContainer)
        │   ├── JoinLabel (Label) - "加入聊天室"
        │   ├── IPLabel (Label) - "服务器IP"
        │   ├── IPLineEdit (LineEdit)
        │   ├── PortLabel2 (Label) - "端口"
        │   ├── JoinPortSpinBox (SpinBox)
        │   └── JoinButton (Button) - "加入"
        └── StatusLabel (Label)
\`\`\`

#### MainMenu.gd - 主菜单脚本

创建 \`scripts/MainMenu.gd\`：

\`\`\`gdscript
extends Control

# 引用节点
@onready var username_line_edit: LineEdit = $PanelContainer/VBoxContainer/UsernameSection/UsernameLineEdit
@onready var host_port_spinbox: SpinBox = $PanelContainer/VBoxContainer/HostSection/HostPortSpinBox
@onready var host_button: Button = $PanelContainer/VBoxContainer/HostSection/HostButton
@onready var ip_line_edit: LineEdit = $PanelContainer/VBoxContainer/JoinSection/IPLineEdit
@onready var join_port_spinbox: SpinBox = $PanelContainer/VBoxContainer/JoinSection/JoinPortSpinBox
@onready var join_button: Button = $PanelContainer/VBoxContainer/JoinSection/JoinButton
@onready var status_label: Label = $PanelContainer/VBoxContainer/StatusLabel

func _ready() -> void:
	host_button.pressed.connect(_on_host_pressed)
	join_button.pressed.connect(_on_join_pressed)
	NetworkManager.connection_succeeded.connect(_on_connection_succeeded)
	NetworkManager.connection_failed.connect(_on_connection_failed)
	username_line_edit.text = "玩家" + str(randi() % 1000)

## 创建服务器
func _on_host_pressed() -> void:
	var username: String = username_line_edit.text.strip_edges()
	if username.is_empty():
		status_label.text = "请输入用户名"
		return

	var port: int = int(host_port_spinbox.value)
	status_label.text = "正在创建服务器..."
	host_button.disabled = true
	join_button.disabled = true

	if NetworkManager.create_server(port, username):
		_load_lobby()
	else:
		status_label.text = "创建服务器失败"
		host_button.disabled = false
		join_button.disabled = false

## 加入服务器
func _on_join_pressed() -> void:
	var username: String = username_line_edit.text.strip_edges()
	if username.is_empty():
		status_label.text = "请输入用户名"
		return

	var ip: String = ip_line_edit.text.strip_edges()
	if ip.is_empty():
		ip = "127.0.0.1"

	var port: int = int(join_port_spinbox.value)
	status_label.text = "正在连接服务器..."
	host_button.disabled = true
	join_button.disabled = true

	NetworkManager.join_server(ip, port, username)

## 连接成功
func _on_connection_succeeded() -> void:
	_load_lobby()

## 连接失败
func _on_connection_failed() -> void:
	status_label.text = "连接服务器失败"
	host_button.disabled = false
	join_button.disabled = false

## 加载大厅场景
func _load_lobby() -> void:
	var tree := get_tree()
	if tree:
		tree.change_scene_to_file("res://scenes/Lobby.tscn")
\`\`\`

### 第三章 聊天大厅场景

#### Lobby.tscn 场景结构

创建 \`scenes/Lobby.tscn\`：

\`\`\`
Lobby (Control)
└── HSplitContainer
    ├── PlayerListPanel (PanelContainer)
    │   └── VBoxContainer
    │       ├── PlayerListTitle (Label) - "在线玩家"
    │       └── PlayerList (ItemList)
    └── ChatPanel (PanelContainer)
        └── VBoxContainer
            ├── ChatHistory (RichTextLabel)
            └── InputContainer (HBoxContainer)
                ├── MessageLineEdit (LineEdit)
                ├── SendButton (Button) - "发送"
                └── DisconnectButton (Button) - "断开"
\`\`\`

#### Lobby.gd - 大厅脚本

创建 \`scripts/Lobby.gd\`：

\`\`\`gdscript
extends Control

# 引用节点
@onready var player_list: ItemList = $HSplitContainer/PlayerListPanel/VBoxContainer/PlayerList
@onready var chat_history: RichTextLabel = $HSplitContainer/ChatPanel/VBoxContainer/ChatHistory
@onready var message_line_edit: LineEdit = $HSplitContainer/ChatPanel/VBoxContainer/InputContainer/MessageLineEdit
@onready var send_button: Button = $HSplitContainer/ChatPanel/VBoxContainer/InputContainer/SendButton
@onready var disconnect_button: Button = $HSplitContainer/ChatPanel/VBoxContainer/InputContainer/DisconnectButton

func _ready() -> void:
	send_button.pressed.connect(_on_send_pressed)
	disconnect_button.pressed.connect(_on_disconnect_pressed)
	message_line_edit.text_submitted.connect(_on_message_submitted)

	ChatManager.player_joined.connect(_on_player_joined)
	ChatManager.player_left.connect(_on_player_left)
	ChatManager.chat_message_received.connect(_on_chat_message_received)
	ChatManager.system_message_received.connect(_on_system_message_received)
	ChatManager.lobby_updated.connect(_on_lobby_updated)
	NetworkManager.server_disconnected.connect(_on_server_disconnected)

	_update_player_list()
	message_line_edit.grab_focus()
	_add_system_message("欢迎来到聊天室！")

## 发送消息
func _on_send_pressed() -> void:
	_send_message()

func _on_message_submitted(message: String) -> void:
	_send_message()

func _send_message() -> void:
	var message: String = message_line_edit.text.strip_edges()
	if message.is_empty():
		return
	NetworkManager.send_chat_message(message)
	message_line_edit.clear()

## 断开连接
func _on_disconnect_pressed() -> void:
	NetworkManager.disconnect_from_server()
	get_tree().change_scene_to_file("res://scenes/MainMenu.tscn")

## 玩家加入回调
func _on_player_joined(peer_id: int, player_name: String) -> void:
	_update_player_list()

## 玩家离开回调
func _on_player_left(peer_id: int, player_name: String) -> void:
	_update_player_list()

## 聊天消息回调
func _on_chat_message_received(peer_id: int, player_name: String, message: String) -> void:
	var display_name := player_name if peer_id != NetworkManager.get_local_peer_id() else "你"
	chat_history.append_text("[%s] %s: %s\\n" % [_get_time_string(), display_name, message])
	_scroll_to_bottom()

## 系统消息回调
func _on_system_message_received(message: String) -> void:
	_add_system_message(message)

## 大厅更新回调
func _on_lobby_updated(players: Dictionary) -> void:
	_update_player_list()

## 服务器断开回调
func _on_server_disconnected() -> void:
	_add_system_message("已与服务器断开连接")
	await get_tree().create_timer(1.5).timeout
	get_tree().change_scene_to_file("res://scenes/MainMenu.tscn")

## 更新玩家列表
func _update_player_list() -> void:
	player_list.clear()
	var players: Dictionary = NetworkManager.get_players()

	for peer_id in players.keys():
		var player_name: String = players[peer_id]
		var prefix := "● " if peer_id == NetworkManager.get_local_peer_id() else "  "
		var host_tag := " [主机]" if peer_id == 1 and NetworkManager.is_host() else ""
		player_list.add_item(prefix + player_name + host_tag)

## 添加系统消息
func _add_system_message(message: String) -> void:
	chat_history.append_text("[%s] [系统] %s\\n" % [_get_time_string(), message])
	_scroll_to_bottom()

## 滚动到底部
func _scroll_to_bottom() -> void:
	chat_history.scroll_to_line(chat_history.get_line_count() - 1)

## 获取时间字符串
func _get_time_string() -> String:
	var time := Time.get_time_dict_from_system()
	return "%02d:%02d:%02d" % [time.hour, time.minute, time.second]
\`\`\`

## 遇到的问题

### 问题1：变量类型推断错误

使用 \`:=\` 自动类型推断时，如果返回值是 \`Variant\` 类型，变量也会被推断为 \`Variant\`。

**解决方案**：显式声明类型。

\`\`\`gdscript
# 错误
var players := NetworkManager.get_players()

# 正确
var players: Dictionary = NetworkManager.get_players()
\`\`\`

### 问题2：get_tree() 返回 null

在某些时机下 \`get_tree()\` 可能返回 \`null\`。

**解决方案**：使用局部变量缓存并检查。

\`\`\`gdscript
func _load_lobby() -> void:
	var tree := get_tree()
	if tree:
		tree.change_scene_to_file("res://scenes/Lobby.tscn")
\`\`\`

### 问题3：客户端发送消息服务器收不到

最初使用 \`@rpc("authority")\` 广播消息，但客户端无法触发权威 RPC。

**解决方案**：采用「客户端请求 → 服务器广播」模式：

\`\`\`gdscript
# 1. 客户端请求
@rpc("any_peer", "call_local", "reliable")
func receive_message_from_client(message: String) -> void:
	if not multiplayer.is_server():
		return
	# 服务器广播
	broadcast_chat_message.rpc(sender_id, player_name, message)

# 2. 服务器广播
@rpc("authority", "call_local", "reliable")
func broadcast_chat_message(...) -> void:
	chat_message_received.emit(...)
\`\`\`

## 使用方法

1. **创建聊天室**：输入用户名 → 设置端口 → 点击"创建"
2. **加入聊天室**：输入用户名 → 输入服务器IP（留空默认127.0.0.1）和端口 → 点击"加入"
3. 聊天消息会自动同步给所有在线玩家

## 下载链接

[示例代码下载](/zips/localchat.zip)
`,t=`---\r
title: 实现一个2D画图软件\r
author: \r
  - Moshangzhu\r
  - 上杉炎\r
date: 2025-10-20\r
category:\r
  - 教程\r
tag:\r
  - 案例\r
---\r
\r
## 教程简介\r
本教程将通过实现一个画图软件，演示画图、选色、图层等核心功能。\r
\r
完成该教程后，你将会对鼠标事件、Image、ImageTexture、自动加载等相关知识点有一定了解。\r
\r
<!-- more -->\r
\r
### 获取项目代码\r
完整代码托管在[GitHub仓库](https://github.com/godotvillage/painter-example)，每个分支对应不同章节（如\`chapter-1\`表示第一章）。\r
\r
### 环境\r
教程使用\`Godot 4.5\`版本。\r
\r
## 教程章节\r
### 第一章 添加主面板布局\r
!!! note Git项目分支：\`chapter-1\`，可以直接切换到该分支查看当前章节代码\r
!!!\r
在这一章节中，我们将一起实现项目创建和主场景布局的搭建。\r
\r
#### 新建项目\r
- 打开\`Godot\`软件，点击左上角\`创建\`按钮\r
- 选择一个存放游戏目录的路径，并为你的游戏起一个名字\r
- 点击\`创建\`按钮\r
- 此时会看到一个仅包含\`icon.svg\`文件的空项目\r
\r
#### 新建主场景并添加布局\r
- 创建一个名为\`scenes\`的目录，在其中新建一个\`Control\`类型的场景，命名为\`main\`\r
- 接下来在\`main\`场景中添加以下节点。节点较多，可以慢慢来，不用着急\r
  \`\`\`\r
  Main (Control)\r
  ├── MarginContainer (MarginContainer)\r
  │   └── MainLayout (VBoxContainer)\r
  │       ├── Navbar (HBoxContainer)\r
  │       │   ├── Toolbar (HBoxContainer)\r
  │       │   │   ├── Pen (Button) - "画笔"\r
  │       │   │   └── Eraser (Button) - "橡皮"\r
  │       │   └── Colorbar (HBoxContainer)\r
  │       │       ├── FrontColorContainer (VBoxContainer)\r
  │       │       │   ├── FrontColor (ColorRect)\r
  │       │       │   └── Label (Label) - "前景色"\r
  │       │       └── BackColorContainer (VBoxContainer)\r
  │       │           ├── BackColor (ColorRect)\r
  │       │           └── Label (Label) - "背景色"\r
  │       ├── PaintBoard (Control)\r
  │       │   └── VBoxContainer (VBoxContainer)\r
  │       │       ├── HBoxContainer (HBoxContainer)\r
  │       │       │   ├── PaintLayers (Control)\r
  │       │       │   │   └── PaintLayer (TextureRect)\r
  │       │       │   └── VScrollBar (VScrollBar)\r
  │       │       └── HScrollBar (HScrollBar)\r
  │       ├── BottomPanel (HBoxContainer)\r
  │       └── Footbar (HBoxContainer)\r
  \`\`\`\r
- 完成节点添加后，让我们了解一下这些节点的作用\r
  - \`MainLayout\`：主场景的布局容器，软件的所有内容都在这个节点内\r
  - \`Navbar\`：画布上方的导航栏（后续可能会添加菜单栏）\r
    - \`Toolbar\`：工具栏，目前包含两个工具：画笔和橡皮\r
    - \`Colorbar\`：颜色栏，用于设置前景色和背景色\r
  - \`PaintBoard\`：画板区域，为了支持缩放功能，这里添加了两个滚动条\r
    - \`PaintLayers\`：图层容器，包含所有图层，缩放和拖动等操作都基于这个节点\r
      - \`PaintLayer\`：画板图层，使用\`TextureRect\`实现，是核心节点之一，后续很多功能都会围绕它展开\r
  - \`BottomPanel\`：底部面板，未来可以放置图层管理等功能\r
  - \`Footbar\`：底部状态栏，用于显示当前状态信息\r
- 接下来还需要调整一些节点属性\r
  - \`MarginContainer/MainLayout/PaintBoard/VBoxContainer/HBoxContainer\` 节点需要添加**垂直扩展**\r
  - \`MarginContainer/MainLayout/PaintBoard/VBoxContainer/HBoxContainer/PaintLayers\` 节点需要添加**垂直扩展**\r
  - \`MarginContainer/MainLayout/PaintBoard/VBoxContainer/HBoxContainer/PaintLayers/PaintLayer\` 需要添加尺寸（非最小尺寸）\r
\r
完成以上步骤后，运行游戏并将\`main\`场景设置为主场景，就可以看到以下界面：\r
![](/images/tutorial/example/painter/main_layout.png)\r
\r
\r
### 第二章 添加绘图代码\r
!!! note\r
Git项目分支：\`chapter-2\`，可以直接切换到该分支查看当前章节代码\r
!!!\r
接下来为\`PaintLayer\`节点添加脚本。创建一个名为\`paint_layer.gd\`的脚本，并填入以下代码：\r
\`\`\` gdscript\r
class_name PaintLayer\r
extends TextureRect\r
\r
# 图层的图像数据\r
var image : Image = null\r
\r
# 当前图层是否激活（可绘制）\r
var is_active: bool = true\r
\r
# 画笔半径\r
var brush_r = 5\r
# 画笔颜色\r
var brush_color : Color = Color(0,0,0, 1)\r
\r
# 是否正在绘制中\r
var drawing: bool = false\r
\r
func _ready() -> void:\r
	# 创建一个空白图像，大小与TextureRect相同\r
	image = Image.create_empty(int(size.x), int(size.y), false, Image.FORMAT_RGBA8)\r
	# 用白色填充图像\r
	image.fill(Color(1,1,1,1))\r
	# 将图像转换为纹理并赋值给TextureRect\r
	texture = ImageTexture.create_from_image(image)\r
\r
func _gui_input(event: InputEvent) -> void:\r
	# 如果图层未激活，不处理输入\r
	if not is_active:\r
		return\r
	# 处理鼠标按钮事件\r
	if event is InputEventMouseButton:\r
		if event.button_index == MOUSE_BUTTON_LEFT:\r
			# 左键按下时开始绘制，松开时停止绘制\r
			drawing = event.pressed\r
			if drawing:\r
				# 按下时在鼠标位置绘制一个圆\r
				paint_circle(get_local_mouse_position(), brush_r, brush_color)\r
	# 处理鼠标移动事件\r
	if event is InputEventMouseMotion and drawing:\r
		# 如果正在绘制，随着鼠标移动持续绘制圆形\r
		paint_circle(get_local_mouse_position(), brush_r, brush_color)\r
\r
func paint_circle(layer_pos: Vector2i, r: float, color: Color):\r
	# 半径小于等于0时不绘制\r
	if r <= 0:\r
		return\r
\r
	# 遍历圆形范围内的所有像素点\r
	for x in range(max(0, int(layer_pos.x - r)), min(image.get_width() - 1, int(layer_pos.x + r)) + 1):\r
		for y in range(max(0, int(layer_pos.y - r)), min(image.get_height() - 1, int(layer_pos.y + r)) + 1):\r
			# 判断当前像素是否在圆形范围内（使用距离平方判断）\r
			if Vector2(x, y).distance_squared_to(layer_pos) < pow(r, 2):\r
				# 将该像素设置为指定颜色\r
				image.set_pixelv(Vector2i(x, y), color)\r
\r
	# 更新纹理以显示新绘制的内容\r
	texture.update(image)\r
\r
\`\`\`\r
这段代码实现了基础的绘图功能：\r
- 在\`_ready\`函数中创建空白画布（\`Image\`对象）并填充为白色\r
- 通过\`_gui_input\`函数监听鼠标事件：\r
  - 左键按下时开始绘制\r
  - 按住左键拖动时持续绘制\r
  - 松开左键时停止绘制\r
- \`paint_circle\`函数负责绘制圆形画笔：遍历圆形范围内的像素点，根据距离判断是否在圆内\r
- 每次绘制后更新纹理，使修改立即显示在画布上\r
\r
现在运行游戏，就可以在画布上自由绘制了！试着按住鼠标左键并拖动，会看到黑色的画笔痕迹。\r
\r
![](/images/tutorial/example/painter/drawing.png)\r
\r
### 第三章 优化绘图代码\r
!!! note\r
Git项目分支：\`chapter-3\`，可以直接切换到该分支查看当前章节代码\r
!!!\r
在上一章中实现了基础绘制功能，不过可能会注意到一个小问题：当快速移动鼠标时，画笔轨迹会出现断续的情况。这是因为鼠标事件的触发频率有限，快速移动时事件之间的间隔较大。\r
\r
解决方法很简单：记录每次绘制时的鼠标位置，在下一次绘制时，从上一个位置到当前位置之间画一条连续的线。具体实现就是在两点之间补充若干个圆形，让它们连接起来。\r
\r
让我们更新一下代码：\r
\`\`\` gdscript\r
class_name PaintLayer\r
extends TextureRect\r
\r
# 图层的图像数据\r
var image : Image = null\r
\r
# 当前图层是否激活（可绘制）\r
var is_active: bool = true\r
\r
# 画笔半径\r
var brush_r = 5\r
# 画笔颜色\r
var brush_color : Color = Color(0,0,0, 0.1)\r
\r
# 是否正在绘制中\r
var drawing: bool = false\r
\r
# 存储上一个鼠标位置，用于绘制连续线条\r
var last_mouse_pos: Vector2i = Vector2i.ZERO\r
\r
func _ready() -> void:\r
	# 创建一个空白图像，大小与TextureRect相同\r
	image = Image.create_empty(int(size.x), int(size.y), false, Image.FORMAT_RGBA8)\r
	# 用白色填充图像\r
	image.fill(Color(1,1,1,1))\r
	# 将图像转换为纹理并赋值给TextureRect\r
	texture = ImageTexture.create_from_image(image)\r
\r
func _gui_input(event: InputEvent) -> void:\r
	# 如果图层未激活，不处理输入\r
	if not is_active:\r
		return\r
	# 处理鼠标按钮事件\r
	if event is InputEventMouseButton:\r
		if event.button_index == MOUSE_BUTTON_LEFT:\r
			# 左键按下时开始绘制，松开时停止绘制\r
			drawing = event.pressed\r
			if drawing:\r
				# 按下时在鼠标位置绘制一个圆，并记录位置\r
				var current_pos = get_local_mouse_position()\r
				last_mouse_pos = Vector2i(current_pos.x, current_pos.y)\r
				paint_circle(last_mouse_pos, brush_r, brush_color)\r
			else:\r
				# 松开鼠标时重置上一个位置\r
				last_mouse_pos = Vector2i.ZERO\r
	# 处理鼠标移动事件\r
	if event is InputEventMouseMotion and drawing:\r
		var current_pos = get_local_mouse_position()\r
		var current_pos_i = Vector2i(current_pos.x, current_pos.y)\r
\r
		# 如果这是第一次移动，直接绘制\r
		if last_mouse_pos == Vector2i.ZERO:\r
			paint_circle(current_pos_i, brush_r, brush_color)\r
		else:\r
			# 在两个位置之间绘制连续的线条，避免出现断点\r
			draw_line_between(last_mouse_pos, current_pos_i, brush_r, brush_color)\r
\r
		# 更新上一个鼠标位置\r
		last_mouse_pos = current_pos_i\r
\r
func paint_circle(layer_pos: Vector2i, r: float, color: Color):\r
	# 半径小于等于0时不绘制\r
	if r <= 0:\r
		return\r
\r
	# 遍历圆形范围内的所有像素点\r
	for x in range(max(0, int(layer_pos.x - r)), min(image.get_width() - 1, int(layer_pos.x + r)) + 1):\r
		for y in range(max(0, int(layer_pos.y - r)), min(image.get_height() - 1, int(layer_pos.y + r)) + 1):\r
			# 判断当前像素是否在圆形范围内（使用距离平方判断）\r
			if Vector2(x, y).distance_squared_to(layer_pos) < pow(r, 2):\r
				# 将该像素设置为指定颜色\r
				image.set_pixelv(Vector2i(x, y), color)\r
\r
	# 更新纹理以显示新绘制的内容\r
	texture.update(image)\r
\r
# 在两个点之间绘制连续的线条，防止快速移动鼠标时出现断续的笔画\r
func draw_line_between(from: Vector2, to: Vector2, r: float, color: Color):\r
	var distance = from.distance_to(to)\r
\r
	# 如果两点距离很小，直接绘制终点即可\r
	if distance <= 1:\r
		paint_circle(to, r, color)\r
		return\r
\r
	# 计算需要插值的步数（基于距离和画笔半径，确保圆形之间有重叠）\r
	var steps = max(2, int(distance / (r * 0.5)))\r
\r
	# 在起点和终点之间进行线性插值，绘制一系列圆形形成连续线条\r
	for i in range(steps + 1):\r
		var t = float(i) / steps\r
		var interpolated_pos = from.lerp(to, t)\r
		paint_circle(Vector2i(interpolated_pos.x, interpolated_pos.y), r, color)\r
\r
\`\`\`\r
\r
这次更新主要包含以下改进：\r
- 新增 \`last_mouse_pos\` 变量，用于记录上一次的鼠标位置\r
- 鼠标按下时记录位置，松开时重置，便于下次绘制\r
- 鼠标移动时，会从上一个位置到当前位置绘制连续线条，而不只是单点\r
- 新增 \`draw_line_between\` 函数：\r
  - 计算两点间距离\r
  - 根据距离和画笔半径智能确定插值步数\r
  - 使用线性插值（\`lerp\`）在两点间补充若干圆形\r
  - 通过适当的步数（基于 \`r * 0.5\`）确保圆形重叠，形成流畅线条\r
- 同时将画笔颜色透明度调整为 \`0.1\`，呈现更自然的半透明效果\r
\r
经过这些优化，即使快速移动鼠标，画笔轨迹也能保持流畅连贯，再也不会出现断点啦！\r
\r
![](/images/tutorial/example/painter/drawing-better.png)\r
\r
### 第四章 画笔颜色和全局配置\r
未完待续`,a=`---\r
title: 文件读写与存档系统\r
author: Moshangzhu\r
date: 2025-10-19\r
category:\r
  - 教程\r
tag:\r
  - 文件\r
  - 未完成\r
---\r
\r
## 简介\r
\r
想象一下这样的场景：你的玩家在游戏中辛辛苦苦打了三个小时，终于攒够了装备、升到了满级、解锁了所有成就。但当他们关闭游戏再次打开时，却发现一切从头开始——所有的进度都消失了，就像从未玩过一样。\r
\r
或者你想制作一款剧情丰富的RPG游戏，玩家的每个选择都会影响后续的故事走向。但如果没有存档系统，玩家就必须一口气通关，否则所有的选择和进度都会丢失。\r
\r
再比如，你想让玩家自定义游戏设置——音量大小、画质选项、按键绑定。如果这些设置无法保存，每次启动游戏都要重新调整，这会让玩家非常抓狂。\r
\r
那么，如何让这些珍贵的游戏数据在关闭游戏后依然保留呢？\r
\r
答案是**文件读写与存档系统**。\r
\r
在现代游戏开发中，存档系统是几乎所有类型游戏的必备功能。无论是RPG的进度保存、动作游戏的检查点、还是简单的设置保存，都离不开文件读写操作。\r
\r
在Godot中，我们有多种方式来实现文件的读写和数据持久化——**ConfigFile**、**JSON**、**二进制文件**、**资源文件**、**数据库**等。每种方式都有其适用场景。\r
\r
<!-- more -->\r
\r
!!! note 数据持久化
数据持久化是指将程序运行时产生的数据保存到硬盘等存储介质上，使得数据在程序关闭后仍然存在，下次启动时可以重新加载。\r
\r
在游戏开发中，常见的需要持久化的数据包括：\r
- **游戏进度**：玩家的关卡进度、任务完成情况、收集品状态\r
- **角色数据**：等级、经验值、装备、技能、属性\r
- **游戏设置**：音量、画质、按键绑定、语言选择\r
- **统计数据**：游戏时长、击败敌人数、死亡次数、成就解锁\r
\r
选择合适的存档方式不仅能提升玩家体验，还能防止作弊、保护数据安全。
!!!\r
\r
## Godot中的五种数据持久化方式\r
\r
### 使用ConfigFile保存配置\r
待补充\r
\r
### 使用JSON保存游戏数据\r
\r
JSON（JavaScript Object Notation）是一种轻量级的数据交换格式，具有良好的可读性和跨平台兼容性。在Godot中，使用JSON保存游戏数据非常方便——通过 \`JSON.stringify()\` 将数据序列化为字符串写入文件，读取时再用 \`JSON.parse_string()\` 反序列化即可。\r
\r
但有一个关键限制：**JSON只能处理Dictionary和Array等基础类型**，无法直接序列化自定义类对象。因此，我们需要为每个数据类手动编写转换方法：\r
\r
- **\`to_dict()\`**：将对象转换为Dictionary（序列化），用于保存\r
- **\`from_dict()\`**：从Dictionary还原对象（反序列化），用于读取\r
\r
下面的示例代码展示了一个完整的JSON存档方案，包括：\r
1. **数据类定义**：如何为类编写 \`to_dict()\` 和 \`from_dict()\` 方法\r
2. **类型处理**：基本类型直接存储，\`Vector2\` 等Godot内置类型需要用 \`JSON.from_native()\` / \`JSON.to_native()\` 转换\r
3. **嵌套对象**：通过递归调用实现嵌套类的序列化\r
4. **文件读写**：使用 \`FileAccess\` 进行文件的读取和保存\r
\r
示例代码：\r
\`\`\`gdscript ::close
# ============================================================\r
# 数据类定义\r
# 将需要保存的数据封装成类，并提供序列化/反序列化方法\r
# ============================================================\r
\r
class TestClass:\r
	# 定义需要保存的属性\r
	var a: int = 0                              # 整数类型，默认值为0\r
	var b: String = ""                          # 字符串类型，默认值为空字符串\r
	var c: Vector2 = Vector2.ZERO               # Vector2类型，Godot内置类型，需要特殊处理\r
	var d: TestSubClass = TestSubClass.new()    # 嵌套的子类对象，需要递归序列化\r
\r
	## 将当前对象转换为字典（序列化）\r
	## 用于保存时将对象转换为JSON可处理的格式\r
	func to_dict() -> Dictionary:\r
		return {\r
			"a": self.a,                        # 基本类型可以直接存储\r
			"b": self.b,\r
			"c": JSON.from_native(self.c),      # Vector2等Godot内置类型需要用JSON.from_native()转换\r
			"d": self.d.to_dict(),              # 嵌套对象需要递归调用to_dict()\r
		}\r
\r
	## 从字典创建对象（反序列化）\r
	## 用于读取时将JSON数据还原为对象\r
	## [param dict] 从JSON解析得到的字典\r
	## [return] 返回还原后的TestClass实例\r
	static func from_dict(dict: Dictionary) -> TestClass:\r
		var res = TestClass.new()               # 创建新实例\r
		res.a = dict.get("a", 0)                # 使用get()方法获取值，第二个参数是默认值（防止键不存在时报错）\r
		res.b = dict.get("b", "")               # 注意：原代码这里默认值写成了0，应该是""\r
		res.c = JSON.to_native(dict.get("c", {}))   # 使用JSON.to_native()将数据还原为Vector2\r
		res.d = TestSubClass.from_dict(dict.get("d", {}))  # 嵌套对象递归调用from_dict()\r
		return res\r
\r
\r
# 子类示例：展示如何处理嵌套对象\r
class TestSubClass:\r
	var e: int = 0\r
\r
	## 子类的序列化方法\r
	func to_dict() -> Dictionary:\r
		return {\r
			"e": self.e\r
		}\r
\r
	## 子类的反序列化方法\r
	static func from_dict(dict: Dictionary) -> TestSubClass:\r
		var res = TestSubClass.new()\r
		res.e = dict.get("e", 0)\r
		return res\r
\r
\r
# ============================================================\r
# 文件读写函数\r
# ============================================================\r
\r
# 存档文件路径（使用user://确保导出后可写入）\r
var save_file_path = "user://save.json"\r
\r
# 存档对象实例\r
var save_object: TestClass\r
\r
## 从文件加载存档数据\r
func load_from_file():\r
	# 以只读模式打开文件\r
	var file = FileAccess.open(save_file_path, FileAccess.READ)\r
\r
	# 检查文件是否成功打开\r
	if FileAccess.get_open_error() == OK:\r
		# 读取文件的全部内容为字符串\r
		var file_content: String = file.get_as_text()\r
\r
		# 判断文件内容是否为空\r
		if not file_content == "":\r
			# 将JSON字符串解析为字典\r
			var dict = JSON.parse_string(file_content)\r
			# 通过反序列化方法将字典转换为对象\r
			save_object = TestClass.from_dict(dict)\r
		else:\r
			# 文件为空时，创建一个新的默认对象\r
			save_object = TestClass.new()\r
\r
		# 关闭文件（重要！不关闭可能导致资源泄露）\r
		file.close()\r
	else:\r
		# 文件打开失败时输出错误信息\r
		printerr("打开文件错误")\r
\r
\r
## 将存档数据保存到文件\r
func save_to_file():\r
	# 以写入模式打开文件（如果文件不存在会自动创建，存在则覆盖）\r
	var file = FileAccess.open(save_file_path, FileAccess.WRITE)\r
\r
	# 将对象转换为字典，再将字典序列化为JSON字符串\r
	var file_content = JSON.stringify(save_object.to_dict())\r
\r
	# 将JSON字符串写入文件\r
	file.store_string(file_content)\r
\r
	# 关闭文件\r
	file.close()\r

\`\`\`\r
\r
### 使用二进制文件保存数据\r
待补充\r
\r
### 使用Resource资源文件保存数据\r
\r
Resource是Godot引擎的原生资源系统，使用它来保存数据是最"Godot风格"的方式。相比JSON方案，Resource有以下优势：\r
\r
- **原生类型支持**：\`Vector2\`、\`Color\`、\`PackedScene\` 等Godot内置类型可以直接保存，无需手动转换\r
- **代码极简**：只需调用 \`ResourceSaver.save()\` 和 \`load()\`，无需编写序列化/反序列化方法\r
- **编辑器集成**：\`.tres\` 文件可以在Godot编辑器中直接查看和编辑\r
\r
!!! warning 注意事项
- 存档路径建议使用 \`user://\` 前缀（如 \`user://save.tres\`），因为 \`res://\` 目录在导出后是只读的\r
- Resource文件是Godot特有格式，不适合需要跨引擎或被外部程序读取的场景\r
- 如果需要防止玩家修改存档，应考虑使用二进制格式（\`.res\`）而非文本格式（\`.tres\`）
!!!\r
\r
示例代码：\r
\`\`\`gdscript ::close
# ============================================================\r
# 存档路径和资源实例\r
# ============================================================\r
\r
# 存档文件路径（使用user://确保导出后可写入）\r
var save_path = "user://save.tres"\r
\r
# 存档资源实例\r
var save_resource = TestResource.new()\r
\r
\r
# ============================================================\r
# 自定义Resource类\r
# 继承Resource即可，属性会自动被序列化\r
# ============================================================\r
\r
class TestResource extends Resource:\r
	# 基本类型 - 直接支持\r
	var a: int = 0\r
	var b: String = ""\r
\r
	# Godot内置类型 - 直接支持，无需手动转换！\r
	var c: Vector2 = Vector2.ZERO\r
\r
	# 甚至可以保存其他资源类型，如场景、纹理等\r
	var d: PackedScene = null\r
\r
\r
# ============================================================\r
# 文件读写函数\r
# ============================================================\r
\r
## 将资源保存到文件\r
func save_resource_to_file():\r
	# 使用ResourceSaver保存资源\r
	# 第一个参数：资源对象\r
	# 第二个参数：保存路径\r
	var error = ResourceSaver.save(save_resource, save_path)\r
	if error != OK:\r
		printerr("保存失败，错误码：", error)\r
\r
\r
## 从文件加载资源\r
func load_resource_from_file():\r
	# 检查文件是否存在\r
	if ResourceLoader.exists(save_path):\r
		# 使用load()函数加载资源，自动还原为Resource对象\r
		save_resource = load(save_path)\r
	else:\r
		# 文件不存在时，创建新的默认资源\r
		save_resource = TestResource.new()
\`\`\`\r
\r
\r
### 使用SQLite数据库保存数据\r
待补充\r
\r
## DLC 和 MOD\r
\r
DLC（Downloadable Content，可下载内容）和MOD（Modification，游戏模组）是扩展游戏内容的重要方式。在Godot中，我们可以通过**PCK资源包**来实现这一功能。\r
\r
### PCK资源包简介\r
\r
PCK（Pack）是Godot的资源打包格式。当你导出游戏时，所有资源都会被打包成一个 \`.pck\` 文件。利用这一特性，我们可以：\r
\r
- **制作DLC**：将新关卡、角色、道具等打包成独立的 \`.pck\` 文件，玩家购买后下载即可\r
- **支持MOD**：允许玩家创建自己的 \`.pck\` 资源包，为游戏添加自定义内容\r
\r
### 实现原理\r
\r
1. **创建DLC/MOD项目**：新建一个Godot项目，使用与主游戏**相同的路径结构**\r
2. **导出为PCK**：导出时选择"导出PCK/ZIP"而非完整游戏\r
3. **运行时加载**：主游戏启动时扫描指定目录，使用 \`ProjectSettings.load_resource_pack()\` 加载PCK文件\r
4. **资源覆盖**：PCK中的资源会覆盖主游戏中同路径的资源，或添加新资源\r
\r
!!! tip 路径一致性
DLC项目中的资源路径必须与主游戏保持一致。例如，如果主游戏的关卡在 \`res://levels/\` 目录下，DLC中的新关卡也应放在 \`res://levels/\` 目录。
!!!\r
\r
示例代码：\r
\`\`\`gdscript ::close
# ============================================================\r
# DLC加载器\r
# 在游戏启动时自动扫描并加载dlc目录下的所有PCK文件\r
# ============================================================\r
\r
extends Control\r
\r
# 加载DLC后要跳转的场景（在编辑器中选择.tscn文件）\r
@export_file('*.tscn') var start_scene\r
\r
\r
func _ready() -> void:\r
	# 游戏启动时自动加载DLC\r
	load_dlc()\r
\r
\r
## 扫描并加载所有DLC资源包\r
func load_dlc() -> void:\r
	# 打开dlc目录（相对于游戏可执行文件的位置）\r
	# 注意：这里使用 './' 而非 'res://'，因为DLC文件在游戏外部\r
	var dlc_dir = DirAccess.open('./dlc')\r
\r
	if dlc_dir != null:\r
		# 打印当前目录路径（调试用）\r
		print("DLC目录：", dlc_dir.get_current_dir())\r
\r
		# 开始遍历目录\r
		dlc_dir.list_dir_begin()\r
		var file_name = dlc_dir.get_next()\r
\r
		# 循环遍历目录中的所有文件和子目录\r
		while file_name != "":\r
			if dlc_dir.current_is_dir():\r
				# 跳过子目录（如果需要递归加载，可以在这里添加逻辑）\r
				print("跳过目录：", file_name)\r
			else:\r
				print("发现文件：", file_name)\r
\r
				# 只加载.pck格式的资源包\r
				if file_name.ends_with(".pck"):\r
					# 使用ProjectSettings.load_resource_pack()加载PCK文件\r
					# 加载成功后，PCK中的资源会合并到游戏的虚拟文件系统中\r
					var success = ProjectSettings.load_resource_pack('./dlc/' + file_name)\r
\r
					if success:\r
						print("✓ 加载DLC成功：", file_name)\r
					else:\r
						printerr("✗ 加载DLC失败：", file_name)\r
\r
			# 获取下一个文件\r
			file_name = dlc_dir.get_next()\r
\r
		# 结束目录遍历（释放资源）\r
		dlc_dir.list_dir_end()\r
	else:\r
		# dlc目录不存在，这是正常情况（玩家可能没有安装任何DLC）\r
		print("未找到dlc目录，跳过DLC加载")\r
\r
	# 等待一小段时间，确保资源加载完成\r
	await get_tree().create_timer(0.1).timeout\r
\r
	# 跳转到起始场景\r
	# 此时DLC中的资源已经可用，如果DLC覆盖了某些资源，将使用DLC版本\r
	get_tree().change_scene_to_file(start_scene)
\`\`\``,i=`---\r
title: Godot有限状态机\r
description: 三种Godot中实现的状态机\r
sticky: false\r
star: false\r
category:\r
  - 教程\r
tag:\r
  - 设计模式\r
date: 2025-10-11\r
---\r
\r
## 简介\r
-  有限状态机（Finite State Machine，简称FSM）是一种计算模型，它由一组状态以及在这些状态之间的转移组成。这个模型可以用于描述系统的行为，其中系统在任何给定时间点都处于某个特定的状态，并且可以根据输入或触发条件从一个状态转移到另一个状态。\r
## 使用**脚本**构建有限状态机\r
- 来源: [timothyqiu](https://space.bilibili.com/7092): https://www.bilibili.com/video/BV1yM4y1j79P\r
### 代码\r
\`\`\`gdscript\r
class_name StateMachine\r
extends Node\r
\r
var currenrt_state: int = -1:\r
	set(v):\r
		owner.transition_state(currenrt_state, v)\r
		currenrt_state = v\r
		state_time = 0\r
\r
var state_time: float\r
\r
func _ready() -> void:\r
	await owner.ready\r
	currenrt_state = 0\r
\r
func _physics_process(delta: float) -> void:\r
	while true:\r
		var next := owner.get_next_state(currenrt_state) as int\r
		if currenrt_state == next:\r
			break\r
		currenrt_state = next\r
		\r
	owner.tick_physics(currenrt_state, delta)\r
	state_time += delta\r
	\r
\`\`\`\r
### 说明\r
- 父节点中需要添加状态机节点。\r
- 父级节点需要实现三个函数和一个枚举类型\r
	- \`func transition_state(from: State, to: State) -> void:\`\r
		- 切换状态时执行的逻辑\r
		- 根据\`to\`参数播放动画\r
	- \`func get_next_state(state: State) -> State:\`\r
		- 主动切换状态\r
		- 根据当前状态以及某些条件，返回下一个将要变化的状态，默认应返回当前状态\r
	- \`func tick_physics(state:State, delta: float) -> void:\`\r
		- 每一帧根据状态执行代码\r
		- 一般用于控制移动\r
	- \`State\`枚举\r
		- 用于定义每一种状态\r
### 示例\r
- 场景树\r
![脚本构建状态机场景树示例](/images/tutorial/fsm/fsm_script_tree.png)\r
    - 只需要有一个脚本节点，无需其他节点。\r
- Player节点脚本\r
\`\`\`gdscript\r
extends CharacterBody2D\r
@onready var animation_player = $AnimationPlayer\r
\r
enum State {\r
	Idle,\r
	Walk,\r
}\r
	\r
var speed = 100\r
var direction = Vector2.ZERO\r
\r
func _ready() -> void:\r
	pass\r
	\r
func get_next_state(state: State) -> State:\r
	match state:\r
		State.Walk:\r
			if direction.length() == 0:\r
				return State.Idle\r
		State.Idle:\r
			if direction.length() > 0:\r
				return State.Walk\r
	return state\r
	\r
func tick_physics(state:State, _delta: float) -> void:\r
	direction = Input.get_vector("move_left", "move_right", "move_up", "move_down")\r
	\r
	match state:\r
		State.Walk:\r
			move()\r
	\r
func transition_state(from: State, to: State) -> void:\r
	#print("state from {0}, to {1}".format([from, to]))\r
	match to:\r
		State.Walk:\r
			animation_player.play("walk")\r
		State.Idle:\r
			animation_player.play("idle")\r
	\r
func move():\r
	velocity = direction * speed\r
	move_and_slide()\r
\`\`\`\r
### 优缺点\r
- 状态机代码都在父节点脚本中，代码逻辑耦合严重。\r
- 父节点的三个函数需要对每一种状态分别处理，代码会比较复杂。\r
## 使用**节点**构建有限状态机\r
- 来源: [ImRains](https://space.bilibili.com/66079515) : https://www.bilibili.com/video/BV1de411i7Lh/\r
### 代码\r
- StateMachine.gd\r
\`\`\`gdscript\r
extends Node\r
\r
class_name StateMachine\r
\r
@export var current_state: StateBase\r
\r
func _ready() -> void:\r
	for child in get_children():\r
		if child is StateBase:\r
			child.state_machine = self\r
	await get_parent().ready\r
	current_state.enter()\r
\r
func _process(delta: float) -> void:\r
	current_state.process_update(delta)\r
\r
func _physics_process(delta: float) -> void:\r
	current_state.physical_process_update(delta)\r
\r
## 修改状态\r
func change_state(target_state_name: String) -> void:\r
	var target_state = get_node(target_state_name)\r
	if target_state == null:\r
		printerr("状态传入错误")\r
		return\r
	current_state.exit()\r
	current_state = target_state\r
	current_state.enter()\r
\r
\`\`\`\r
- StateBase.gd\r
\`\`\`gdscript\r
extends Node\r
\r
## 基础状态\r
class_name StateBase\r
\r
var state_machine: StateMachine\r
\r
## 进入状态\r
func enter() -> void:\r
	pass\r
\r
## 退出状态\r
func exit() -> void:\r
	pass\r
\r
## 渲染帧触发\r
func process_update(delta: float) -> void:\r
	pass\r
\r
## 物理帧触发\r
func physical_process_update(delta: float) -> void:\r
	pass\r
	\r
\`\`\`\r
### 说明\r
- 为需要添加状态机的节点添加StateMachine状态机节点，并在状态机节点下创建若干StateBase节点。\r
- 为每一个StateBase节点修改名称并**扩展脚本**。\r
- 在扩展脚本中覆写\`enter\`、\`exit\`、\`process_update\`、\`physical_process_update\`等函数。\r
	- \`func enter() -> void:\`\r
		- 进入这个状态时需要执行的代码，可以用于播放动画，设置属性等。\r
	- \`func exit() -> void:\`\r
		- 退出这个状态时调用的代码，可以用于释放数据等操作。\r
	- \`func process_update(delta: float) -> void:\`\r
		- 当前状态的每一个渲染帧执行的逻辑。\r
	- \`func physical_process_update(delta: float) -> void:\`\r
		- 当前状态的每一个物理帧执行的逻辑。\r
		- 可以用于移动。\r
	- 在覆写的时候可以先调用父类super的同名方法。（虽然父类StateBase里面什么都没有）\r
- 在需要切换状态的地方使用以下代码切换状态\r
	\`\`\`gdscript\r
	state_machine.change_state("需要切换的状态名称")\r
	\`\`\`\r
### 示例\r
- 场景树\r
![节点构建状态机场景树示例](/images/tutorial/fsm/fsm_node_tree.png)\r
    - 需要有一个状态机节点和若干子状态节点。\r
- Player节点脚本\r
\`\`\`gdscript\r
extends CharacterBody2D\r
@onready var animation_player = $AnimationPlayer\r
\r
var direction = Vector2.ZERO\r
\r
func _ready() -> void:\r
	pass\r
\r
func _physics_process(delta: float) -> void:\r
	direction = Input.get_vector("move_left", "move_right", "move_up", "move_down")\r
\`\`\`\r
- Idle节点扩展脚本\r
\`\`\`gdscript\r
extends StateBase\r
\r
@onready var animation_player: AnimationPlayer = $"../../AnimationPlayer"\r
@export var player: CharacterBody2D = null\r
\r
func enter():\r
	super.enter()\r
	animation_player.play("idle_down")\r
\r
func physical_process_update(delta: float):\r
	super.physical_process_update(delta)\r
	if player.direction.length() != 0:\r
		state_machine.change_state("Walk")\r
\`\`\`\r
- Walk节点扩展脚本\r
\`\`\`gdscript\r
extends StateBase\r
@onready var animation_player: AnimationPlayer = $"../../AnimationPlayer"\r
@export var player: CharacterBody2D = null\r
\r
var speed = 100\r
\r
func enter():\r
	super.enter()\r
	animation_player.play("walk")\r
	\r
func physical_process_update(delta: float):\r
	super.physical_process_update(delta)\r
	if player.direction.length() == 0:\r
		state_machine.change_state("Idle")\r
	move()\r
	\r
func move():\r
	player.velocity = player.direction * speed\r
	player.move_and_slide()\r
\`\`\`\r
### 优缺点\r
- 将代码拆分到若干个节点的代码片段中。代码量增加，但逻辑更清晰。\r
- 由于状态节点经常需要使用父节点的属性，而这些属性只在父节点中声明使用，会导致代码的可读性降低。\r
\r
## 使用**函数**构建有限状态机\r
### 代码\r
\`\`\` gdscript\r
class_name CallableStateMachine\r
\r
# 保存状态函数的字典\r
var state_dictionary = {}\r
# 当前状态的名称\r
var current_state: String\r
\r
# 添加状态，需要传入每帧运行、进入状态、离开状态\r
func add_states(\r
	normal_state_callable: Callable,\r
	enter_state_callable: Callable,\r
	leave_state_callable: Callable\r
):\r
	state_dictionary[normal_state_callable.get_method()] = {\r
		"normal": normal_state_callable,\r
		"enter": enter_state_callable,\r
		"leave": leave_state_callable\r
	}\r
\r
# 设置初始状态\r
func set_initial_state(state_callable: Callable):\r
	var state_name = state_callable.get_method()\r
	if state_dictionary.has(state_name):\r
		_set_state(state_name)\r
	else:\r
		push_warning("No state with name " + state_name)\r
\r
# 状态机每帧调用当前状态的normal函数\r
func update(delta: float):\r
	if current_state != null:\r
		(state_dictionary[current_state].normal as Callable).call(delta)\r
\r
# 切换状态，需要传入要切换的状态的normal函数\r
func change_state(state_callable: Callable):\r
	var state_name = state_callable.get_method()\r
	if state_dictionary.has(state_name):\r
		_set_state.call_deferred(state_name)\r
	else:\r
		push_warning("No state with name " + state_name)\r
\r
# 内置设置状态函数\r
func _set_state(state_name: String):\r
	if current_state:\r
		var leave_callable = state_dictionary[current_state].leave as Callable\r
		if !leave_callable.is_null():\r
			leave_callable.call()\r
\r
	current_state = state_name\r
	var enter_callable = state_dictionary[current_state].enter as Callable\r
	if !enter_callable.is_null():\r
		enter_callable.call()\r
\`\`\`\r
### 说明\r
- 这种状态机需要在玩家或敌人的脚本中声明一个函数式状态机，并手动调用\`add_states\`添加若干个状态。以及需要调用\`set_initial_state\`手动指定初始状态。\r
- 每个状态需要传递三个函数。分别是每帧运行的函数（normal）、进入状态运行的函数（enter）以及离开状态运行的函数（leave）。\r
- 切换状态时，需要调用状态机的\`change_state\`函数并传递要切换的状态的normal函数。\r
  \r
\r
### 示例\r
- 场景树中无需任何新节点，只需要在玩家脚本中编写响应的代码。\r
- 玩家场景脚本\r
\`\`\`gdscript\r
var state_machine: CallableStateMachine = CallableStateMachine.new()\r
\r
# 当前状态\r
var current_state: String:\r
	get:\r
		return state_machine.current_state\r
	set(value):\r
		state_machine.change_state(Callable.create(self, value))\r
\r
\r
func _notification(what: int) -> void:\r
	if what == NOTIFICATION_SCENE_INSTANTIATED:\r
		state_machine.add_states(state_idle, enter_state_idle, Callable())\r
		state_machine.add_states(state_walk, enter_state_walk, leave_state_walk)\r
\r
func _ready():\r
	state_machine.set_initial_state(state_idle)\r
\r
func _process(delta: float) -> void:\r
	state_machine.update(delta)\r
\r
# 状态相关方法\r
func state_idle(_delta: float):\r
	# 每帧执行的idle状态代码\r
	# 切换状态\r
	var need_move = true\r
	if need_move\r
		state_machine.change_state(state_walk)\r
	pass\r
\r
func enter_state_idle():\r
	# 进入idle状态执行的代码\r
	pass\r
\r
func state_walk(_delta: float):\r
	# 每帧执行的walk状态代码\r
	# 切换状态\r
	var need_idle = true\r
	if need_idle\r
		state_machine.change_state(state_idle)\r
	pass\r
\r
func enter_state_walk():\r
	# 进入walk状态执行的代码\r
	pass\r
\r
func leave_state_walk():\r
	# 离开walk状态执行的代码\r
	pass\r
\r
\`\`\`\r
### 优缺点\r
- 将代码放在同一个文件中，可能会导致逻辑混乱，但由于声明状态时状态是若干个函数，而非各种枚举值，少去了很多match判断。\r
- 优点是由于都在需要控制的节点的脚本中，所以可以直接调用和操作节点中的变量，类似方向和速度等。免去了节点式构建的状态机需要通过传参才能获取属性的不方便。\r
- 由于每个状态都拆分成了三个函数，也让部分可以抽象成函数的逻辑继续复用，提高了代码的可读性。\r
`,o=`---\r
title: 落叶轻羽B站视频列表\r
description: 展示落叶轻羽的Godot教程视频\r
sticky: true\r
star: true\r
category:\r
  - 教程\r
date: 2025-10-11\r
---\r
## 关于落叶轻羽\r
B站UP主 [落叶轻羽](https://space.bilibili.com/375301589) 是国内知名的Godot引擎学习资源贡献者，专注于2D游戏开发教程领域。他通过精心筛选YouTube上的优质Godot教学视频，进行专业的翻译、配音和本地化处理，为中文社区提供了大量高质量的学习资源。目前频道已积累超过350个视频，内容涵盖从零基础入门到进阶开发的完整学习路径。\r
<!-- more -->\r
落叶轻羽的教程特色在于其系统性和实用性，不仅搬运了Brackeys等国际知名游戏开发者的经典教程，还包含了《杀戮尖塔》仿制、横版过关游戏开发等实战项目。他的视频采用中文语音解说，降低了国内开发者的学习门槛，让更多人能够轻松掌握Godot引擎的使用技巧。\r
\r
特别值得推荐的是，落叶轻羽整理的教程系列结构清晰、循序渐进，非常适合新手系统学习。从Godot基础概念讲解到完整的游戏项目开发，每个环节都有详细的视频指导，帮助学习者从零开始构建自己的游戏作品。这些资源已成为众多Godot初学者入门的首选学习材料，在游戏开发社区中享有良好口碑。\r
\r
## Brackeys系列视频\r
\r
### 说明\r
Brackeys是游戏开发领域极具影响力的YouTube主播，最初以Unity教程闻名，后来转向Godot引擎开发。他推出的四门Godot课程覆盖了完整的开发学习路径：\r
\r
- 入门课程帮助新手快速上手Godot引擎基础操作；\r
- GDScript语法课程深入讲解Godot专用编程语言的核心概念；\r
- 3D开发课程教授如何在Godot中创建三维游戏场景和对象；\r
- 灯光课程专门讲解光照系统的设置与优化。\r
\r
这些课程延续了Brackeys一贯的高质量教学风格，内容精炼实用，面向从零基础到进阶的广泛开发者群体，为想要从Unity转向Godot或直接学习Godot的开发者提供了系统化的学习资源。\r
\r
<BiliBlackboard width="750px" link="BV1fs421N7TD"/>\r
<BiliBlackboard width="750px" link="BV1Wz42127wh"/>\r
<BiliBlackboard width="750px" link="BV1sjKjeZEfo"/>\r
<BiliBlackboard width="750px" link="BV17Q8QzeEL1"/>\r
\r
## Godot4终极入门教程\r
\r
### 说明\r
Clear Code的《Godot4终极入门教程》是一门全面系统的Godot 2D游戏开发教程，涵盖了从基础概念到高级功能的完整知识体系。该课程从Godot引擎介绍开始，逐步深入场景节点、代码编程、物理系统、动画制作、灯光效果、UI界面等核心内容，包含43个精心设计的章节。教程特别注重实践应用，通过Tilemap、粒子系统、骨骼动画、路径寻路等实际案例帮助学习者掌握游戏开发技能。虽然基于Godot 4.2版本制作，当前已更新至4.5版本，部分节点如Tilemap有所变化，但整体教学内容依然具有很高的学习价值。课程设计新手友好，内容深度足够，是学习Godot 2D游戏开发的优质选择。\r
\r
<BiliBlackboard width="750px" link="BV1pu4y1b7p6"/>\r
\r
### 课程章节\r
\`\`\` ::close 章节列表
- 第 1 节：什么是Godot\r
- 第 2 节：场景和节点\r
- 第 3 节：代码\r
- 第 4 节：更多代码\r
- 第 5 节：增量时间 delta\r
- 第 6 节：获取输入\r
- 第 7 节：更多节点\r
- 第 8 节：物理\r
- 第 9 节：区域与信号\r
- 第 10 节：创建自定义信号\r
- 第 11 节：创建自定义场景\r
- 第 12 节：RigidBody2D\r
- 第 13 节：相机\r
- 第 14 节：TileMaps\r
- 第 15 节：瓦片地图与物理\r
- 第 16 节：发射粒子\r
- 第 17 节：灯光\r
- 第 18 节：动画\r
- 第 19 节：Tweens\r
- 第 20 节：将激光连接到关卡\r
- 第 21 节：继承\r
- 第 22 节：创建内部关卡\r
- 第 23 节：添加关卡过度\r
- 第 24 节：用户界面\r
- 第 25 节：将血条添加到UI\r
- 第 26 节：使UI具有交互性\r
- 第 27 节：为UI添加颜色\r
- 第 28 节：更漂亮的过度\r
- 第 29 节：创建物品\r
- 第 30 节：SetGet\r
- 第 31 节：创建宝箱\r
- 第 32 节：扩展内部关卡\r
- 第 33 节：创建侦察兵\r
- 第 34 节：创建手榴弹\r
- 第 35 节：侦察机与生命值\r
- 第 36 节：玩家与生命值\r
- 第 37 节：着色器\r
- 第 38 节：虫子敌人\r
- 第 39 节：无人机敌人\r
- 第 40 节：沿路径移动的汽车\r
- 第 41 节：路径寻路\r
- 第 42 节：骨骼动画\r
- 第 43 节：声音\r
- 第 44 节：最后的润色（完）
\`\`\`\r
\r
## 杀戮尖塔仿制教程\r
### 说明\r
这是一个由YouTube频道"GodotGameLab"制作的《杀戮尖塔》仿制教程，使用Godot 4引擎开发。该教程面向初学者到中级开发者，全面涵盖卡牌游戏开发的核心技术，包括资源使用、卡牌系统设计、复杂的鼠标交互机制。教程分为两季，第一季22集完整构建了卡牌战斗系统，包含卡片拖动、状态机、法力管理、敌人AI、音乐音效等完整功能；第二季继续深入开发，增加了主菜单、角色选择、地图生成、金币系统、状态系统、修饰器系统等高级功能。教程采用模块化架构设计，强调代码的简洁性和可扩展性，目前仍在持续更新中，是学习Godot 4开发卡牌类游戏的优质资源。\r
\r
<BiliBlackboard width="750px" link="BV1PC411L78f"/>\r
\r
### 课程章节\r
\`\`\` ::close 章节列表
- S1-01：介绍、架构和设置\r
- S1-02：卡片拖动机制\r
- S1-03：状态机\r
- S1-04：修复 BUG\r
- S1-05：卡牌资源\r
- S1-06：卡牌瞄准\r
- S1-07：资源、统计数据、卡牌堆\r
- S1-08：资源、统计数据、卡牌堆\r
- S1-09：卡牌视觉与逻辑\r
- S1-10：法力管理\r
- S1-11：卡牌禁用\r
- S1-12：卡牌提示信息\r
- S1-13：抽牌功能\r
- S1-14：弃牌与修复\r
- S1-15：加权随机选择、敌方行动基础类、AI核心\r
- S1-16：螃蟹敌人行动编码\r
- S1-17：敌人管理器，输赢状态\r
- S1-18：敌人意图\r
- S1-19：完善\r
- S1-20：音乐与音效\r
- S1-21：结束屏幕与脚本模板\r
- S1-22：蝙蝠敌人【第一季 - 完】\r
- 第二季：E01、导览与架构设计\r
- S2 E02-1、主菜单\r
- S2 E02-2、角色选择器\r
- S2 E02-3、运行场景\r
- S2 E03-1、卡牌菜单界面\r
- S2 E03-2、创建卡牌堆视图\r
- S2 E03-3、卡牌堆视图交互\r
- S2 E03-4、运行场景中接入卡牌堆视图\r
- S2 E04-1、卡牌稀有度\r
- S2 E04-2、金币\r
- S2 E4-3、金币奖励\r
- S2 E04-4、卡牌奖励\r
- S2 E05-1、地图生成介绍与算法\r
- S2 E05-2、第一步：初始网格生成\r
- S2 E05-3、第二步：起始点选取\r
- S2 E05-4、第三步：路径生成\r
- S2 E05-5、第四步：房间类型分配\r
- S2 E05-6、创建地图房间、线路场景\r
- S2-E05-7、更新主地图场景、脚本\r
- S2-E06-1、战斗数据资源\r
- S2-E06-2、灵活的遭遇池系统\r
- S2-E07-1、修复BUG\r
- S2-E07-2、篝火场景\r
- S2-E07-3、生命值UI重构\r
- S2-E08-1、状态系统架构与资源创建\r
- S2-E08-2、状态UI\r
- S2-E08-3、状态处理器\r
- S2-E08-4、为敌人场景添加状态处理器\r
- S2-E08-5、重做重击卡牌\r
- S2-E08-6、为玩家场景添加状态处理器\r
- S2-E09、修饰器系统-1\r
- S2-E09、修饰器系统-2\r
- S2-E09、修饰器系统-3\r
- S2-E09、修饰器系统-4\r
- S2-E10、动态用户界面-1\r
- S2-E10、动态用户界面-2
\`\`\``,l=`---
title: 攻击与碰撞系统
description: 子弹、Hitbox/Hurtbox设计与网络同步
sticky: false
star: false
category:
  - 教程
tag:
  - 多人游戏
  - 战斗系统
date: 2026-03-16
---

## 组件架构

项目使用组合模式实现攻击系统：

\`\`\`
HitboxComponent (攻击方)
        ↓
   碰撞检测
        ↓
HurtboxComponent (受击方)
        ↓
   伤害处理
        ↓
  HealthComponent
\`\`\`

## HitboxComponent (攻击组件)

### 定义

\`\`\`gdscript
class_name HitboxComponent
extends Area2D

signal hit_hurtbox(hurtbox_component: HurtboxComponent)

var damage: int = 1
var source_peer_id: int  # 攻击来源的 peer ID
var is_hit_handled: bool  # 防止重复命中
\`\`\`

> **设计说明**：\`source_peer_id\` 记录子弹是谁发射的，用于区分敌我伤害。\`is_hit_handled\` 防止单次碰撞触发多次伤害。

## HurtboxComponent (受击组件)

### 定义

\`\`\`gdscript
class_name HurtboxComponent
extends Area2D

signal hit_by_hitbox(hitbox_component: HitboxComponent)

var disable_collisions: bool = false

func _on_area_entered(area: Area2D):
    if area is HitboxComponent:
        if disable_collisions:
            return
        hit_by_hitbox.emit(area)
\`\`\`

> **设计说明**：受击框检测到攻击框时发出信号。\`disable_collisions\` 用于受伤后短暂无敌（如受伤闪烁期间）。

## 子弹系统

### 子弹创建

\`\`\`gdscript
# player.gd
func try_fire():
    if !fire_rate_timer.is_stopped():
        return

    var bullet = bullet_scene.instantiate() as Bullet
    bullet.damage = get_bullet_damage()
    bullet.global_position = barrel_position.global_position
    bullet.source_peer_id = player_input_synchronizer_component.get_multiplayer_authority()
    bullet.start(player_input_synchronizer_component.aim_vector)
    get_parent().add_child(bullet, true)
\`\`\`

> **设计说明**：
> - 第二个参数 \`true\` 表示在服务器端创建
> - \`source_peer_id\` 存储发射者的 peer ID，用于伤害归属
> - \`aim_vector\` 决定子弹飞行方向

### 子弹飞行

\`\`\`gdscript
# bullet.gd
class_name Bullet
extends Node2D

const SPEED: int = 600

var direction: Vector2
var source_peer_id: int
var damage: int = 1

func _process(delta: float):
    global_position += direction * SPEED * delta

func start(dir: Vector2):
    direction = dir
    rotation = direction.angle()
\`\`\`

> **设计说明**：子弹继承 Node2D（而非 RigidBody），手动计算位置。服务器和客户端各自运行子弹逻辑，保证一致性。

### 子弹碰撞

\`\`\`gdscript
func _ready() -> void:
    hitbox_component.damage = damage
    hitbox_component.source_peer_id = source_peer_id
    hitbox_component.hit_hurtbox.connect(_on_hit_hurtbox)
    life_timer.timeout.connect(_on_life_timer_timeout)

func register_collision():
    hitbox_component.is_hit_handled = true
    queue_free()

func _on_hit_hurtbox(_hurtbox_component: HurtboxComponent):
    register_collision()
\`\`\`

> **设计说明**：碰撞后立即标记 \`is_hit_handled\` 并销毁子弹，防止穿透再次触发。

## 伤害处理流程

### 1. 玩家被击中

\`\`\`gdscript
# player.gd
func _on_hit_by_hitbox():
    play_hit_effects.rpc()

# hurtbox 接收信号
func hit_by_hitbox(hitbox_component: HitboxComponent):
    # 造成伤害（仅服务器）
    if is_multiplayer_authority():
        health_component.damage(hitbox_component.damage)
\`\`\`

> **设计说明**：视觉效果（RPC）和伤害计算（服务器）分离。\`play_hit_effects.rpc()\` 所有客户端都会执行，伤害仅服务器计算。

### 2. 伤害计算

\`\`\`gdscript
# health_component.gd
class_name HealthComponent
extends Node

signal died
signal damaged
signal health_changed(current_health: int, max_health: int)

@export var max_health: int = 1
var _current_health: int

func damage(amount: int):
    current_health = clamp(current_health - amount, 0, max_health)
    damaged.emit()
    if current_health == 0:
        died.emit()
\`\`\`

> **设计说明**：生命值归一化到 [0, max_health] 范围。死亡时发出 \`died\` 信号，由父节点处理后续逻辑。

### 3. 死亡处理

\`\`\`gdscript
# player.gd
func _on_died():
    kill()

func kill():
    if !is_multiplayer_authority():
        push_error("Cannot call kill on non-server client")
        return

    _kill.rpc()
    await get_tree().create_timer(.5).timeout
    died.emit()
    queue_free()

@rpc("authority", "call_local", "reliable")
func _kill():
    is_dying = true
    player_input_synchronizer_component.public_visibility = false
\`\`\`

> **设计说明**：
> - \`_kill.rpc()\` 使用 \`call_local\`，服务器执行后本地也隐藏玩家
> - \`reliable\` 确保死亡状态同步
> - 延迟 0.5 秒后再销毁，让死亡动画有时间播放

## 攻击效果同步

### 开火效果

\`\`\`gdscript
@rpc("authority", "call_local", "unreliable")
func play_fire_effects():
    if weapon_animation_player.is_playing():
        weapon_animation_player.stop()
    weapon_animation_player.play("fire")

    # 枪口闪光
    var muzzle_flash: Node2D = muzzle_flash_scene.instantiate()
    muzzle_flash.global_position = barrel_position.global_position
    muzzle_flash.rotation = barrel_position.global_rotation
    get_parent().add_child(muzzle_flash)

    # 屏幕震动（仅本地）
    if player_input_synchronizer_component.is_multiplayer_authority():
        GameCamera.shake(1)

    weapon_stream_player.play()
\`\`\`

> **设计说明**：
> - \`unreliable\` 用于高频效果，允许丢包
> - 屏幕震动仅本地执行，因为只有本地玩家需要震动反馈
> - 枪口闪光创建为本地节点，不用同步

### 受击效果

\`\`\`gdscript
@rpc("authority", "call_local")
func play_hit_effects():
    if player_input_synchronizer_component.is_multiplayer_authority():
        GameCamera.shake(1)
        hit_stream_player.play()

    # 粒子效果
    var hit_particles: Node2D = ground_particles_scene.instantiate()
    background_node.add_child(hit_particles)
    hit_particles.global_position = global_position

    # 闪烁效果
    hurtbox_component.disable_collisions = true
    var tween := create_tween()
    tween.set_loops(10)
    tween.tween_property(visuals, "visible", false, .05)
    tween.tween_property(visuals, "visible", true, .05)
\`\`\`

> **设计说明**：受击时通过 tween 实现受伤闪烁，同时禁用碰撞防止连续受伤。

## 攻击冷却

\`\`\`gdscript
func try_fire():
    if !fire_rate_timer.is_stopped():
        return

    # ... 开火逻辑

    fire_rate_timer.wait_time = get_fire_rate()
    fire_rate_timer.start()

func get_fire_rate() -> float:
    var fire_rate_count := UpgradeManager.get_peer_upgrade_count(
        player_input_synchronizer_component.get_multiplayer_authority(),
        "fire_rate"
    )
    return BASE_FIRE_RATE * (1 - (.1 * fire_rate_count))
\`\`\`

> **设计说明**：根据玩家升级计算射速。\`get_multiplayer_authority()\` 获取当前玩家的 peer ID，用于查询该玩家的升级属性。

## 网络同步要点

| 方面 | 实现方式 |
|------|----------|
| 伤害计算 | 仅服务器 (is_multiplayer_authority) |
| 效果播放 | RPC call_local |
| 位置同步 | MultiplayerSynchronizer |
| 可靠性 | 关键操作用 reliable，开火效果用 unreliable |

## 设计模式总结

1. **组件化设计**: 分离攻击、伤害、效果
2. **源追溯**: source_peer_id 追踪伤害来源
3. **权威验证**: 所有伤害计算在服务器执行
4. **效果分离**: 视觉效果与逻辑分离，通过 RPC 同步

## 相关文档

- [网络架构概述](/tutorial/mutiplayer_tutorial/network-architecture-overview)
- [玩家输入同步机制](/tutorial/mutiplayer_tutorial/player-input-synchronization)
- [敌人AI与状态管理](/tutorial/mutiplayer_tutorial/enemy-ai-and-state-management)
`,s=`---
title: CallableStateMachine 实现
description: 使用Godot Callable实现自定义状态机
sticky: false
star: false
category:
  - 教程
tag:
  - 多人游戏
  - 设计模式
date: 2026-03-16
---

## 状态机概述

项目使用 \`Callable\` 实现了一个轻量级的状态机，用于管理敌人 AI 的行为状态。这种方式比传统的状态模式更灵活。

## 类定义

\`\`\`gdscript
class_name CallableStateMachine

var state_dictionary = {}
var current_state: String
\`\`\`

> **设计说明**：字典存储状态，字符串记录当前状态名。

## 核心数据结构

\`\`\`gdscript
state_dictionary[状态方法名] = {
    "normal": Callable,    # 状态持续时每帧调用
    "enter": Callable,     # 进入状态时调用
    "leave": Callable      # 离开状态时调用
}
\`\`\`

> **设计说明**：每个状态有三个 Callable，分别对应进入、持续、离开三个时机。

## 添加状态

\`\`\`gdscript
func add_states(
    normal_state_callable: Callable,
    enter_state_callable: Callable,
    leave_state_callable: Callable
):
    state_dictionary[normal_state_callable.get_method()] = {
        "normal": normal_state_callable,
        "enter": enter_state_callable,
        "leave": leave_state_callable
    }
\`\`\`

> **设计说明**：用 \`get_method()\` 获取方法名作为字典键，实现方法名到状态的映射。

## 设置初始状态

\`\`\`gdscript
func set_initial_state(state_callable: Callable):
    var state_name = state_callable.get_method()
    if state_dictionary.has(state_name):
        _set_state(state_name)
    else:
        push_warning("No state with name " + state_name)
\`\`\`

> **设计说明**：初始状态只调用进入回调，不调用离开回调（因为没有上一个状态）。

## 状态更新

\`\`\`gdscript
func update():
    if current_state != null:
        (state_dictionary[current_state].normal as Callable).call()
\`\`\`

> **设计说明**：每帧调用当前状态的 \`normal\` 回调，执行状态逻辑。

## 状态切换

\`\`\`gdscript
func change_state(state_callable: Callable):
    var state_name = state_callable.get_method()
    if state_dictionary.has(state_name):
        _set_state.call_deferred(state_name)
    else:
        push_warning("No state with name " + state_name)
\`\`\`

> **设计说明**：使用 \`call_deferred\` 延迟到下一帧切换状态，避免在状态回调中立即切换导致栈溢出。

## 内部状态切换

\`\`\`gdscript
func _set_state(state_name: String):
    # 调用上一个状态的 leave
    if current_state:
        var leave_callable = state_dictionary[current_state].leave as Callable
        if !leave_callable.is_null():
            leave_callable.call()

    # 切换状态
    current_state = state_name

    # 调用新状态的 enter
    var enter_callable = state_dictionary[current_state].enter as Callable
    if !enter_callable.is_null():
        enter_callable.call()
\`\`\`

> **设计说明**：
> - 先调用旧状态的 \`leave\`（清理工作）
> - 再切换 \`current_state\`
> - 最后调用新状态的 \`enter\`（初始化工作）
> - 使用 \`is_null()\` 检查 Callable 是否为空（可选回调）

## 使用示例

### 定义状态方法

\`\`\`gdscript
# enemy.gd
func _notification(what: int) -> void:
    if what == NOTIFICATION_SCENE_INSTANTIATED:
        state_machine.add_states(state_spawn, enter_state_spawn, Callable())
        state_machine.add_states(state_normal, enter_state_normal, leave_state_normal)
        state_machine.add_states(state_charge_attack, enter_state_charge_attack, leave_state_charge_attack)
        state_machine.add_states(state_attack, enter_state_attack, leave_state_attack)
\`\`\`

> **设计说明**：
> - \`NOTIFICATION_SCENE_INSTANTIATED\` 在节点完全创建后触发
> - \`Callable()\` 创建空 Callable，用于不需要 leave 回调的状态

### 状态方法定义

\`\`\`gdscript
# 状态持续逻辑（每帧调用）
func state_spawn():
    pass

func state_normal():
    if is_multiplayer_authority():
        velocity = global_position.direction_to(target_position) * 40

# 进入状态时调用
func enter_state_spawn():
    var tween := create_tween()
    tween.tween_property(visuals, "scale", Vector2.ONE, .4)\\
        .from(Vector2.ZERO)\\
        .set_ease(Tween.EASE_OUT)\\
        .set_trans(Tween.TRANS_BACK)
    tween.finished.connect(func ():
        state_machine.change_state(state_normal)
    )

# 离开状态时调用
func leave_state_normal():
    animation_player.play("RESET")
\`\`\`

> **设计说明**：
> - \`state_*\` 方法是每帧执行的逻辑（返回值为 void）
> - \`enter_state_*\` 做初始化，如播放动画、设置计时器
> - \`leave_state_*\` 做清理，如停止动画、重置状态

### 更新状态机

\`\`\`gdscript
func _process(_delta: float) -> void:
    state_machine.update()
    if is_multiplayer_authority():
        move_and_slide()
\`\`\`

> **设计说明**：状态机每帧更新，通常在 \`_process\` 中调用。

### 切换状态

\`\`\`gdscript
func state_normal():
    if is_multiplayer_authority():
        # 检查切换条件
        if can_attack && distance_to_target < 150:
            state_machine.change_state(state_charge_attack)
\`\`\`

> **设计说明**：在 \`normal\` 回调中检查切换条件，满足时调用 \`change_state\`。

## 优势分析

### 1. 简洁的语法

使用 \`Callable\` 不需要创建大量状态类：

\`\`\`gdscript
# 传统方式
state_machine.change_state(EnemyStateSpawn.new())

# Callable 方式
state_machine.change_state(state_spawn)
\`\`\`

### 2. 灵活的状态定义

\`\`\`gdscript
# 可以使用 Callable.create() 创建带参数的状态
state_machine.add_states(
    state_normal,
    enter_state_normal,
    Callable.create(self, "leave_state_normal", ["param1", "param2"])
)
\`\`\`

### 3. 延迟切换

\`\`\`gdscript
func change_state(state_callable: Callable):
    _set_state.call_deferred(state_name)  # 延迟到下一帧执行
\`\`\`

## 与传统状态模式对比

| 方面 | CallableStateMachine | 传统状态模式 |
|------|---------------------|-------------|
| 代码量 | 较少 | 较多 |
| 灵活性 | 高 | 中 |
| 状态类数量 | 不需要 | 每个状态一个类 |
| 学习成本 | 中 | 高 |

## 注意事项

1. **状态方法命名**: 状态方法名必须与调用时一致
2. **空 Callable**: 使用 \`is_null()\` 检查空的可调用对象
3. **NOTIFICATION_SCENE_INSTANTIATED**: 确保在节点实例化时添加状态

\`\`\`gdscript
func _notification(what: int) -> void:
    if what == NOTIFICATION_SCENE_INSTANTIATED:
        # 添加状态定义
\`\`\`

## 相关文档

- [敌人AI与状态管理](/tutorial/mutiplayer_tutorial/enemy-ai-and-state-management)
- [网络架构概述](/tutorial/mutiplayer_tutorial/network-architecture-overview)
`,_=`---
title: 敌人AI与状态管理
description: 敌人状态机设计和服务器权威AI
sticky: false
star: false
category:
  - 教程
tag:
  - 多人游戏
  - AI
  - 状态机
date: 2026-03-16
---

## 敌人架构概览

项目使用 **CallableStateMachine** 实现敌人状态管理，配合服务器权威模式。

### 敌人状态列表

\`\`\`
SPAWN (出生) → NORMAL (普通) → CHARGE_ATTACK (冲锋) → ATTACK (攻击)
\`\`\`

## 状态机实现

### CallableStateMachine 类

\`\`\`gdscript
# callable_state_machine.gd
class_name CallableStateMachine

var state_dictionary = {}
var current_state: String

func add_states(
    normal_state_callable: Callable,
    enter_state_callable: Callable,
    leave_state_callable: Callable
):
    state_dictionary[normal_state_callable.get_method()] = {
        "normal": normal_state_callable,
        "enter": enter_state_callable,
        "leave": leave_state_callable
    }
\`\`\`

> **设计说明**：用字典存储状态的三个回调函数，键是方法名。\`Callable\` 可以直接绑定对象方法，比传统状态模式更简洁。

### 敌人状态定义

\`\`\`gdscript
# enemy.gd
func _notification(what: int) -> void:
    if what == NOTIFICATION_SCENE_INSTANTIATED:
        state_machine.add_states(state_spawn, enter_state_spawn, Callable())
        state_machine.add_states(state_normal, enter_state_normal, leave_state_normal)
        state_machine.add_states(state_charge_attack, enter_state_charge_attack, leave_state_charge_attack)
        state_machine.add_states(state_attack, enter_state_attack, leave_state_attack)
\`\`\`

> **设计说明**：\`NOTIFICATION_SCENE_INSTANTIATED\` 在场景实例化后触发，确保节点完全准备好后再添加状态。

## 敌人状态详解

### 1. SPAWN 状态（出生）

\`\`\`gdscript
func enter_state_spawn():
    var tween := create_tween()
    tween.tween_property(visuals, "scale", Vector2.ONE, .4)\\
        .from(Vector2.ZERO)\\
        .set_ease(Tween.EASE_OUT)\\
        .set_trans(Tween.TRANS_BACK)
    tween.finished.connect(func ():
        state_machine.change_state(state_normal)
    )
\`\`\`

> **设计说明**：使用弹性动画让敌人从无到有出现，完成后自动切换到普通状态。

### 2. NORMAL 状态（普通）

\`\`\`gdscript
func enter_state_normal():
    animation_player.play("run")
    if is_multiplayer_authority():
        acquire_target()
        target_acquisition_timer.start()

func state_normal():
    if is_multiplayer_authority():
        # 移动向目标
        velocity = global_position.direction_to(target_position) * 40

        # 定时获取新目标
        if target_acquisition_timer.is_stopped():
            acquire_target()
            target_acquisition_timer.start()

        # 检查攻击条件
        var can_attack := attack_cooldown_timer.is_stopped() || global_position.distance_to(target_position) < 16
        if can_attack && global_position.distance_to(target_position) < 150:
            state_machine.change_state(state_charge_attack)
\`\`\`

> **设计说明**：AI 逻辑仅在服务器执行（\`is_multiplayer_authority()\`）。定时更新目标位置（每 0.5 秒），避免每帧计算。当玩家进入攻击范围且冷却结束时，切换到冲锋状态。

### 3. CHARGE_ATTACK 状态（冲锋准备）

\`\`\`gdscript
func enter_state_charge_attack():
    if is_multiplayer_authority():
        acquire_target()
        charge_attack_timer.start()

    # 显示警告图标
    alert_tween = create_tween()
    alert_tween.tween_property(alert_sprite, "scale", Vector2.ONE, .2)

func state_charge_attack():
    if is_multiplayer_authority():
        # 减速
        velocity = velocity.lerp(Vector2.ZERO, 1.0 - exp(-15 * get_process_delta_time()))
        if charge_attack_timer.is_stopped():
            state_machine.change_state(state_attack)
\`\`\`

> **设计说明**：冲锋前有短暂准备时间（通过计时器），期间显示警告图标给玩家反应。

### 4. ATTACK 状态（攻击）

\`\`\`gdscript
func enter_state_attack():
    if is_multiplayer_authority():
        # 启用碰撞体
        collision_mask = 1 << 0
        collision_layer = 0
        hitbox_collision_shape.disabled = false
        # 冲刺
        velocity = global_position.direction_to(target_position) * 400

func state_attack():
    if is_multiplayer_authority():
        # 减速
        velocity = velocity.lerp(Vector2.ZERO, 1.0 - exp(-3 * get_process_delta_time()))
        if velocity.length() < 25:
            state_machine.change_state(state_normal)

func leave_state_attack():
    if is_multiplayer_authority():
        # 恢复碰撞层
        collision_mask = default_collision_mask
        collision_layer = default_collision_layer
        hitbox_collision_shape.disabled = true
        attack_cooldown_timer.start()
\`\`\`

> **设计说明**：攻击时临时改变碰撞层——攻击方的碰撞层设为 0（不与玩家碰撞），碰撞掩码设为检测玩家层。离开状态时恢复。

## 目标获取

\`\`\`gdscript
func acquire_target():
    var players = get_tree().get_nodes_in_group("player")
    var nearest_player: Player = null

    for player in players:
        if nearest_player == null:
            nearest_player = player
        else:
            if player.global_position.distance_squared_to(global_position) < \\
               nearest_player.global_position.distance_squared_to(global_position):
                nearest_player = player

    if nearest_player != null:
        target_position = nearest_player.global_position
\`\`\`

> **设计说明**：使用 \`distance_squared_to\` 避免开方运算，提高性能。选择最近玩家作为目标。

## 网络同步处理

### 权威判断

\`\`\`gdscript
func _ready():
    if is_multiplayer_authority():
        health_component.died.connect(_on_died)
        state_machine.set_initial_state(state_spawn)
        hurtbox_component.hit_by_hitbox.connect(_on_hit_by_hitbox)
\`\`\`

> **设计说明**：仅服务器创建敌人，所以直接检查 \`is_multiplayer_authority()\` 即可。客户端不创建敌人实例，也不执行 AI。

### 动画和效果

\`\`\`gdscript
@rpc("authority", "call_local")
func spawn_hit_effects():
    hit_stream_player.play()
    var hit_particles: Node2D = impact_particles_scene.instantiate()
    hit_particles.global_position = hurtbox_component.global_position
    get_parent().add_child(hit_particles)
\`\`\`

> **设计说明**：\`call_local\` 让服务器执行后本地也播放效果，不需要额外 RPC。

## 敌人管理器 (EnemyManager)

### 服务器权威

所有敌人生成由服务器控制：

\`\`\`gdscript
# enemy_manager.gd
func start():
    if is_multiplayer_authority():
        begin_round()

func spawn_enemy():
    var enemy = enemy_scene.instantiate() as Node2D
    enemy.global_position = get_random_spawn_position()
    enemy_spawn_root.add_child(enemy, true)
    spawned_enemies += 1
\`\`\`

> **设计说明**：第二个参数 \`true\` 表示在服务器端实例化，确保多人和单机都能正常工作。

### 回合同步

\`\`\`gdscript
func synchronize(to_peer_id: int = -1):
    if !is_multiplayer_authority():
        return

    var data = {
        "round_timer_is_running": !round_timer.is_stopped(),
        "round_timer_time_left": round_timer.time_left,
        "round_count": round_count
    }

    if to_peer_id > -1 && to_peer_id != 1:
        _synchronize.rpc_id(to_peer_id, data)
    else:
        _synchronize.rpc(data)
\`\`\`

> **设计说明**：
> - \`to_peer_id = -1\` 表示广播给所有客户端
> - \`to_peer_id > 0 && to_peer_id != 1\` 表示发给特定客户端（不含服务器自己）
> - 服务器自己不需要同步，所以用 \`rpc_id\` 发给其他客户端，用 \`rpc\` 发给所有（含自己）

## 敌人死亡处理

\`\`\`gdscript
func _on_died():
    spawn_death_particles.rpc()
    GameEvents.emit_enemy_died()
    queue_free()
\`\`\`

\`\`\`gdscript
# game_events.gd
extends Node

signal enemy_died

func emit_enemy_died():
    enemy_died.emit()
\`\`\`

\`\`\`gdscript
# enemy_manager.gd
func _on_enemy_died():
    spawned_enemies -= 1
    check_round_completed()
\`\`\`

> **设计说明**：使用信号跨节点通信。EnemyManager 订阅 GameEvents 的 \`enemy_died\` 信号，敌人死亡时自动减少计数并检查回合是否完成。

## 设计要点总结

| 设计点 | 实现方式 |
|--------|----------|
| 状态管理 | CallableStateMachine 自定义状态机 |
| AI逻辑 | 仅服务器执行（is_multiplayer_authority） |
| 动画同步 | 通过 RPC 本地执行 |
| 目标选择 | 最近玩家优先 |
| 碰撞检测 | 动态启用/禁用碰撞层 |

## 相关文档

- [网络架构概述](/tutorial/mutiplayer_tutorial/network-architecture-overview)
- [攻击与碰撞系统](/tutorial/mutiplayer_tutorial/attack-and-collision-system)
- [CallableStateMachine实现](/tutorial/mutiplayer_tutorial/callable-state-machine-implementation)
`,c=`---
title: 大厅与连接管理
description: 多人游戏大厅系统和连接管理机制
sticky: false
star: false
category:
  - 教程
tag:
  - 多人游戏
  - 大厅系统
date: 2026-03-16
---

## 连接建立

### 主机创建服务器

\`\`\`gdscript
# multiplayer_menu.gd
func _on_host_pressed() -> void:
    var server_peer := ENetMultiplayerPeer.new()
    var error := server_peer.create_server(MultiplayerConfig.port)

    if error != Error.OK:
        show_error(false)
        return

    multiplayer.multiplayer_peer = server_peer
    get_tree().change_scene_to_packed(main_scene)
\`\`\`

> **设计说明**：创建 ENet 服务器并设置为当前 multiplayer peer。服务器默认 peer ID 为 1。

### 客户端加入服务器

\`\`\`gdscript
func _on_join_pressed() -> void:
    var client_peer := ENetMultiplayerPeer.new()
    var error := client_peer.create_client(MultiplayerConfig.ip_address, MultiplayerConfig.port)

    if error != Error.OK:
        show_error(true)
        return

    is_connecting = true
    multiplayer.multiplayer_peer = client_peer
    validate()
\`\`\`

> **设计说明**：客户端创建时只需要服务器 IP 和端口，peer ID 由服务器分配。

### 连接状态回调

\`\`\`gdscript
multiplayer.connected_to_server.connect(_on_connected_to_server)
multiplayer.connection_failed.connect(_on_connection_failed)
\`\`\`

> **设计说明**：Godot 内置的多人信号，连接成功或失败时触发，用于处理 UI 切换。

## LobbyManager 大厅管理

### 功能职责

- 管理玩家准备状态
- 追踪已连接的 peer
- 协调游戏开始

### 核心数据结构

\`\`\`gdscript
var _ready_peer_ids: Array[int] = []
var _is_lobby_closed: bool
\`\`\`

> **设计说明**：\`_ready_peer_ids\` 存储已准备好的玩家 peer ID，\`_is_lobby_closed\` 防止大厅关闭后新玩家加入。

### 信号系统

\`\`\`gdscript
signal all_peers_ready       # 所有玩家准备完毕
signal self_peer_ready        # 本地玩家准备
signal lobby_closed          # 大厅关闭
signal peer_ready_states_changed(peers_ready: int, peers_total: int)
\`\`\`

> **设计说明**：大厅状态变化时发出信号，UI 层订阅这些信号更新界面（如显示"X/Y 玩家已准备"）。

## 玩家准备机制

### 玩家发送准备状态

\`\`\`gdscript
func _input(event: InputEvent) -> void:
    if event.is_action_pressed("lobby_ready"):
        # RPC 调用服务器端的准备方法
        request_peer_ready.rpc_id(MultiplayerPeer.TARGET_PEER_SERVER)
\`\`\`

> **设计说明**：\`rpc_id(1, ...)\` 明确指定发送给服务器（peer ID 1）。\`TARGET_PEER_SERVER\` = 1，表示服务器。

### 服务器处理准备

\`\`\`gdscript
@rpc("any_peer", "call_local", "reliable")
func request_peer_ready():
    if !is_multiplayer_authority() || is_lobby_closed:
        return

    var sender_id := multiplayer.get_remote_sender_id()

    if !ready_peer_ids.has(sender_id):
        ready_peer_ids.append(sender_id)
        emit_peer_ready_states_changed()

    # 通知该玩家其准备状态已被接受
    set_peer_ready.rpc(sender_id)
    try_all_peers_ready()
\`\`\`

> **设计说明**：
> - \`get_remote_sender_id()\` 获取发起请求的玩家 ID
> - 使用 \`call_local\` 让服务器也执行（注册状态）
> - 再调用 \`set_peer_ready.rpc(sender_id)\` 通知该玩家已准备

### 检查所有玩家准备

\`\`\`gdscript
func check_all_peers_ready() -> bool:
    var all_peers := multiplayer.get_peers()
    all_peers.append(MultiplayerPeer.TARGET_PEER_SERVER)

    for peer_id in all_peers:
        if !ready_peer_ids.has(peer_id):
            return false
    return true
\`\`\`

> **设计说明**：服务器也作为一个 peer，需要加入检查列表。

## 玩家加入流程

### 主场景初始化

\`\`\`gdscript
# main.gd
func _ready():
    multiplayer_spawner.spawn_function = func(data):
        var player = player_scene.instantiate() as Player
        player.set_display_name(data.display_name)
        player.name = str(data.peer_id)
        player.input_multiplayer_authority = data.peer_id
        player.global_position = center_position.global_position

        # 本地玩家连接UI
        if multiplayer.get_unique_id() == data.peer_id:
            game_ui.connect_player(player)

        # 服务器监听玩家死亡
        if is_multiplayer_authority():
            player.died.connect(_on_player_died.bind(data.peer_id))

        player_dictionary[data.peer_id] = player
        return player

    # 发送准备信号
    peer_ready.rpc_id(1, MultiplayerConfig.display_name)
\`\`\`

> **设计说明**：
> - \`spawn_function\` 定义如何创建玩家，data 由 \`multiplayer_spawner.spawn()\` 传入
> - \`get_unique_id()\` 获取本地 peer ID，用于判断是否本地玩家
> - 服务器端订阅 \`died\` 信号来追踪玩家死亡

### 玩家加入 RPC

\`\`\`gdscript
@rpc("any_peer", "call_local", "reliable")
func peer_ready(display_name: String):
    var sender_id = multiplayer.get_remote_sender_id()
    player_name_dictionary[sender_id] = display_name

    # 生成玩家实体
    multiplayer_spawner.spawn({
        "peer_id": sender_id,
        "display_name": player_name_dictionary[sender_id],
        "is_respawning": false
    })

    # 同步敌人状态
    enemy_manager.synchronize(sender_id)
\`\`\`

> **设计说明**：新玩家加入时，调用 \`spawn()\` 触发 \`spawn_function\` 创建玩家实例。同时同步当前回合状态给该玩家。

## 断线处理

### 客户端断线

\`\`\`gdscript
multiplayer.server_disconnected.connect(_on_server_disconnected)

func _on_server_disconnected():
    end_game()
\`\`\`

> **设计说明**：服务器断开时，客户端直接返回主菜单。

### 玩家断线

\`\`\`gdscript
multiplayer.peer_disconnected.connect(_on_peer_disconnected)

func _on_peer_disconnected(peer_id: int):
    if player_dictionary.has(peer_id):
        var player := player_dictionary[peer_id]
        if is_instance_valid(player):
            player.kill()
        player_dictionary.erase(peer_id)
\`\`\`

> **设计说明**：服务器检测到某玩家断开，销毁其玩家实体并从字典移除。

### 大厅中断线

\`\`\`gdscript
func _on_peer_disconnected(peer_id: int):
    if is_lobby_closed:
        return

    if ready_peer_ids.has(peer_id):
        ready_peer_ids.erase(peer_id)
        emit_peer_ready_states_changed()
    try_all_peers_ready()
\`\`\`

> **设计说明**：大厅阶段玩家断开，需要从准备列表移除，并重新检查是否所有玩家都已准备。

## 游戏开始流程

\`\`\`gdscript
lobby_manager.all_peers_ready.connect(_on_all_peers_ready)

func _on_all_peers_ready():
    lobby_manager.close_lobby()
    enemy_manager.start()
\`\`\`

> **设计说明**：所有玩家准备完毕后，关闭大厅（禁止新玩家加入），并开始第一回合。

## 设计要点总结

| 功能 | 实现方式 |
|------|----------|
| 连接管理 | ENetMultiplayerPeer |
| 准备状态 | RPC 调用 + 数组追踪 |
| 玩家生成 | MultiplayerSpawner |
| 断线处理 | peer_disconnected 信号 |

## 相关文档

- [网络架构概述](/tutorial/mutiplayer_tutorial/network-architecture-overview)
- [场景管理与玩家生命周期](/tutorial/mutiplayer_tutorial/scene-management-and-player-lifecycle)
`,d=`---
title: 多人游戏教程概览
description: Godot 4 多人游戏教程系列介绍与学习路径
sticky: false
star: false
category:
  - 教程
tag:
  - 多人游戏
  - 入门指南
date: 2026-03-16
---

## 教程系列介绍

本系列教程基于 **2D Multiplayer Course** 项目，深入讲解 Godot 4 多人游戏开发的核心概念和实现方式。

## 视频教程

强烈建议先观看配套视频教程，再结合本文档进行实践：

<div style="position: relative; padding-bottom: 56.25%; height: 0; overflow: hidden;">
  <iframe style="position: absolute; top: 0; left: 0; width: 100%; height: 100%;" src="https://www.youtube.com/embed/F3P5uWgJrj0" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
</div>

> **视频来源**: [2D Multiplayer Course](https://www.youtube.com/watch?v=F3P5uWgJrj0)

## 学习路径

建议按以下顺序学习：

1. **网络架构概述** - 理解多人游戏的基础架构
2. **大厅与连接管理** - 学习如何建立和管理多人连接
3. **玩家输入同步机制** - 掌握玩家输入的同步方法
4. **场景管理与玩家生命周期** - 了解玩家加入、离开、复活的处理
5. **攻击与碰撞系统** - 战斗系统的网络同步
6. **敌人AI与状态管理** - 敌人行为的服务器权威实现
7. **CallableStateMachine实现** - 自定义状态机的使用

## 核心技术点

| 技术点 | 描述 |
|--------|------|
| ENetMultiplayerPeer | Godot 4 推荐的多人网络库 |
| MultiplayerSynchronizer | 自动同步变量到所有客户端 |
| MultiplayerSpawner | 同步创建和销毁节点 |
| RPC 通信 | 远程过程调用实现客户端间通信 |
| 服务器权威 | 服务器负责关键逻辑验证 |

## 项目源码

通过网盘分享的文件：Godot 4 在线合作多人游戏开发全流程实战教程-资料.zip
链接: https://pan.baidu.com/s/1zDExg_QBZLDamGVqqo7Ekg?pwd=5rur 提取码: 5rur

## 相关文档

- [网络架构概述](/tutorial/mutiplayer_tutorial/network-architecture-overview)
- [大厅与连接管理](/tutorial/mutiplayer_tutorial/lobby-and-connection-management)
- [玩家输入同步机制](/tutorial/mutiplayer_tutorial/player-input-synchronization)
`,p=`---
title: 网络架构概述
description: Godot多人游戏中的网络架构设计模式
sticky: false
star: false
category:
  - 教程
tag:
  - 多人游戏
  - 网络同步
date: 2026-03-16
---

## 项目概述

该项目是一个基于 **Godot 4.x** 的 2D 多人射击游戏，采用 **P2P with Server Authority**（具有服务器权威的 P2P）架构。使用 \`ENetMultiplayerPeer\` 实现 TCP/UDP 连接。

## 核心架构设计

### 1. 权威模式 (Authority Model)

项目采用 **服务器权威 (Server Authority)** 模式：

- **服务器 (Server)**: 管理游戏状态、敌人生成、回合同步
- **客户端 (Client)**: 负责玩家输入，其他玩家位置由服务器同步

\`\`\`gdscript
# multiplayer_menu.gd - 服务器创建
var server_peer := ENetMultiplayerPeer.new()
server_peer.create_server(MultiplayerConfig.port)

# 客户端连接
var client_peer := ENetMultiplayerPeer.new()
client_peer.create_client(MultiplayerConfig.ip_address, MultiplayerConfig.port)
\`\`\`

> **设计说明**：ENetMultiplayerPeer 是 Godot 4 推荐的多人网络库，支持可靠和不可靠传输。服务器创建时监听指定端口，客户端通过 IP:端口 连接。

### 2. 网络配置

\`\`\`gdscript
# multiplayer_config.gd
static var ip_address: String = "127.0.0.1"
static var port: int = 3000
static var display_name: String
\`\`\`

> **设计说明**：使用静态变量存储配置，因为多人游戏需要跨场景共享这些设置（如大厅设置的游戏房间参数）。

### 3. 多重授权 (Multiplayer Authority)

通过 \`input_multiplayer_authority\` 实现玩家对自身实体的控制：

\`\`\`gdscript
# player.gd
var input_multiplayer_authority: int

func _ready():
    player_input_synchronizer_component.set_multiplayer_authority(input_multiplayer_authority)
\`\`\`

> **设计说明**：\`input_multiplayer_authority\` 存储当前玩家对应的 peer ID。服务器为每个连接的 peer 创建玩家实例时，将该 peer 的 ID 设为输入授权，实现"每个玩家控制自己的角色"。

## 关键设计模式

### MultiplayerSynchronizer 组件

使用 Godot 内置的 \`MultiplayerSynchronizer\` 节点进行状态同步：

\`\`\`gdscript
# player_input_synchronizer_component.gd
class_name PlayerInputSynchronizerComponent
extends MultiplayerSynchronizer

var movement_vector: Vector2 = Vector2.ZERO
var aim_vector: Vector2 = Vector2.RIGHT
var is_attack_pressed: bool

func _process(_delta: float):
    if is_multiplayer_authority():
        gather_input()
\`\`\`

> **设计说明**：继承 MultiplayerSynchronizer 后，声明的变量会自动同步到所有客户端。\`is_multiplayer_authority()\` 判断当前节点是否归本地玩家控制——只有控制者才采集输入，避免多个客户端采集同一份输入。

### RPC 通信模式

项目使用三种 RPC 调用模式：

| 模式 | 用途 | 示例 |
|------|------|------|
| \`@rpc("authority", "call_local")\` | 权威方执行，本地调用 | \`play_hit_effects()\` |
| \`@rpc("authority", "call_remote")\` | 权威方执行，远程调用 | \`_synchronize()\` |
| \`@rpc("any_peer", "call_local", "reliable")\` | 任意端可调用 | \`peer_ready()\` |

> **设计说明**：
> - \`authority\` 表示由具有该节点权威的一方执行（通常是服务器）
> - \`call_local\` 表示执行后在本地也调用，适合视觉效果
> - \`call_remote\` 表示只发给其他客户端
> - \`any_peer\` 允许任意客户端发起请求
> - \`reliable\` 确保数据必达（用于重要操作如加入游戏），\`unreliable\` 允许丢包（用于高频更新如移动）

## 网络层级结构

\`\`\`
┌─────────────────────────────────────┐
│           UI Layer (UI)             │
├─────────────────────────────────────┤
│        Game Logic (Main)            │
│  - LobbyManager (大厅管理)           │
│  - EnemyManager (敌人管理)           │
├─────────────────────────────────────┤
│      Entity Layer (Entities)         │
│  - Player (玩家)                    │
│  - Enemy (敌人)                     │
│  - Bullet (子弹)                    │
├─────────────────────────────────────┤
│    Component Layer (组件)            │
│  - PlayerInputSynchronizer          │
│  - HealthComponent                  │
│  - HitboxComponent / HurtboxComponent│
├─────────────────────────────────────┤
│       Network Layer                  │
│  - ENetMultiplayerPeer              │
│  - MultiplayerSpawner               │
│  - MultiplayerSynchronizer          │
└─────────────────────────────────────┘
\`\`\`

## 相关文档

- [玩家输入同步机制](/tutorial/mutiplayer_tutorial/player-input-synchronization)
- [大厅与连接管理](/tutorial/mutiplayer_tutorial/lobby-and-connection-management)
- [敌人AI与状态管理](/tutorial/mutiplayer_tutorial/enemy-ai-and-state-management)
- [攻击与碰撞系统](/tutorial/mutiplayer_tutorial/attack-and-collision-system)
- [场景管理与玩家生命周期](/tutorial/mutiplayer_tutorial/scene-management-and-player-lifecycle)
`,u=`---
title: 玩家输入同步机制
description: 多人游戏中的玩家输入同步机制
sticky: false
star: false
category:
  - 教程
tag:
  - 多人游戏
  - 输入同步
date: 2026-03-16
---

## 核心组件：PlayerInputSynchronizerComponent

这是实现玩家输入同步的核心组件，继承自 \`MultiplayerSynchronizer\`。

### 工作原理

\`\`\`gdscript
class_name PlayerInputSynchronizerComponent
extends MultiplayerSynchronizer

var movement_vector: Vector2 = Vector2.ZERO
var aim_vector: Vector2 = Vector2.RIGHT
var is_attack_pressed: bool
\`\`\`

> **设计说明**：继承 MultiplayerSynchronizer 后，这些变量会自动同步给所有客户端。本地玩家修改这些值后，其他客户端会收到更新。

## 输入采集

\`\`\`gdscript
func gather_input():
    # 方向输入
    movement_vector = Input.get_vector("move_left", "move_right", "move_up", "move_down")

    # 瞄准方向（鼠标位置）
    aim_vector = aim_root.global_position.direction_to(aim_root.get_global_mouse_position())

    # 攻击输入
    is_attack_pressed = Input.is_action_pressed("attack")
\`\`\`

> **设计说明**：
> - \`Input.get_vector\` 获取方向键输入，返回归一化的向量
> - 瞄准方向通过计算自身到鼠标位置的方向向量得到
> - 攻击使用"按下"状态而非"按下瞬间"，实现按住连发

## 授权机制

\`\`\`gdscript
func _ready():
    # 设置网络授权
    player_input_synchronizer_component.set_multiplayer_authority(input_multiplayer_authority)

    # 非本地玩家禁用激活区域碰撞
    activation_area_collision_shape.disabled =\\
        !player_input_synchronizer_component.is_multiplayer_authority()
\`\`\`

> **设计说明**：\`input_multiplayer_authority\` 在玩家生成时由服务器传入（对应连接的 peer ID）。调用 \`set_multiplayer_authority\` 后，\`is_multiplayer_authority()\` 会对应该玩家返回 true。非本地玩家的碰撞体禁用，避免本地客户端处理不该自己处理的碰撞。

### 关键判断方法

| 方法 | 用途 |
|------|------|
| \`is_multiplayer_authority()\` | 判断当前节点是否具有权威 |
| \`is_multiplayer_authority()\` 在 Player 中 | 判断是否本地玩家 |

## 移动同步

\`\`\`gdscript
func _process(delta: float) -> void:
    var movement_vector := player_input_synchronizer_component.movement_vector

    if is_multiplayer_authority():
        # 仅权威方计算移动
        var target_velocity = movement_vector * get_movement_speed()
        velocity = velocity.lerp(target_velocity, 1 - exp(-25 * delta))
        move_and_slide()

        # 攻击逻辑仅在权威方执行
        if player_input_synchronizer_component.is_attack_pressed:
            try_fire()
\`\`\`

> **设计说明**：只有具有 authority 的玩家（即自己）才计算移动和执行攻击。其他客户端通过 MultiplayerSynchronizer 同步 \`movement_vector\`，但不做物理计算，只做插值动画。

**使用插值实现平滑移动：**
\`\`\`gdscript
velocity = velocity.lerp(target_velocity, 1 - exp(-25 * delta))
\`\`\`

> **设计说明**：使用指数衰减插值实现平滑加速，参数 25 控制响应速度。\`exp(-25 * delta)\` 约等于 0.99997，每帧逼近目标速度。

## 动画同步

动画播放基于 \`movement_vector\`：

\`\`\`gdscript
if is_equal_approx(movement_vector.length_squared(), 0):
    animation_player.play("RESET")
else:
    animation_player.play("run")
\`\`\`

> **设计说明**：因为所有客户端都同步了 \`movement_vector\`，所以每个客户端都能独立判断并播放相同动画，实现无需 RPC 的动画同步。

## 瞄准同步

\`\`\`gdscript
func update_aim_position():
    var aim_vector = player_input_synchronizer_component.aim_vector
    var aim_position = weapon_root.global_position + aim_vector

    # 角色朝向
    visuals.scale = Vector2.ONE if aim_vector.x > 0 else Vector2(-1, 1)

    # 武器旋转
    weapon_root.look_at(aim_position)
\`\`\`

> **设计说明**：通过同步 \`aim_vector\`，所有客户端都能独立计算瞄准方向，实现武器和角色朝向的同步。

## 显示名称处理

\`\`\`gdscript
var is_single_player = multiplayer.multiplayer_peer is OfflineMultiplayerPeer
var is_client_authority = player_input_synchronizer_component.is_multiplayer_authority()

if is_single_player || is_client_authority:
    display_name_label.visible = false  # 本地玩家隐藏名称
else:
    display_name_label.text = display_name  # 显示其他玩家名称
\`\`\`

> **设计说明**：本地玩家不需要看到自己的名字（FPS 视角），但可以看到其他玩家的名字用于识别。单机模式也隐藏名称。

## 设计模式总结

1. **输入所有权**: 每个玩家控制自己的输入采集
2. **状态预测**: 本地玩家立即响应输入
3. **权威验证**: 服务器验证并执行关键操作（攻击、伤害）
4. **自动同步**: MultiplayerSynchronizer 自动同步输入状态给其他客户端

## 相关文档

- [网络架构概述](/tutorial/mutiplayer_tutorial/network-architecture-overview)
- [攻击与碰撞系统](/tutorial/mutiplayer_tutorial/attack-and-collision-system)
- [场景管理与玩家生命周期](/tutorial/mutiplayer_tutorial/scene-management-and-player-lifecycle)
`,m=`---
title: 场景管理与玩家生命周期
description: 场景切换、玩家生成、复活的实现机制
sticky: false
star: false
category:
  - 教程
tag:
  - 多人游戏
  - 场景管理
date: 2026-03-16
---

## 场景结构

### 主场景层级

\`\`\`
Main (Node)
├── MultiplayerSpawner
├── CenterPosition (Marker2D)
├── EnemyManager
│   ├── SpawnIntervalTimer
│   └── RoundTimer
├── BackgroundEffects (Node2D)
├── BackgroundMask (Sprite2D)
├── GameUI
├── PauseMenu
└── LobbyManager
\`\`\`

## MultiplayerSpawner 使用

### 配置

\`\`\`gdscript
# main.gd
var player_scene: PackedScene = preload("uid://c5dfksrt3u5km")

@onready var multiplayer_spawner: MultiplayerSpawner = $MultiplayerSpawner

func _ready():
    multiplayer_spawner.spawn_function = func(data):
        return spawn_player(data)
\`\`\`

> **设计说明**：\`MultiplayerSpawner\` 是 Godot 4 的节点，用于在多人游戏中同步创建和销毁节点。\`spawn_function\` 定义如何根据 data 创建节点。

### 生成函数

\`\`\`gdscript
func spawn_player(data: Dictionary) -> Player:
    var player = player_scene.instantiate() as Player
    player.set_display_name(data.display_name)
    player.name = str(data.peer_id)  # 节点名称设为 peer ID
    player.input_multiplayer_authority = data.peer_id
    player.global_position = center_position.global_position

    # 本地玩家连接 UI
    if multiplayer.get_unique_id() == data.peer_id:
        game_ui.connect_player(player)

    # 服务器监听死亡
    if is_multiplayer_authority():
        if data.is_respawning:
            player.is_respawn = true
        player.died.connect(_on_player_died.bind(data.peer_id))

    player_dictionary[data.peer_id] = player
    return player
\`\`\`

> **设计说明**：
> - 节点名称必须设为 peer ID，便于 \`get_tree().get_node(str(peer_id))\` 获取
> - \`input_multiplayer_authority\` 设置后，该玩家能控制自己的角色
> - 仅服务器订阅死亡信号（因为只有服务器执行游戏逻辑）
> - \`is_respawning\` 标记是否是复活，复活时初始血量设为 1

## 玩家加入流程

\`\`\`gdscript
@rpc("any_peer", "call_local", "reliable")
func peer_ready(display_name: String):
    var sender_id = multiplayer.get_remote_sender_id()
    player_name_dictionary[sender_id] = display_name

    # 生成玩家
    multiplayer_spawner.spawn({
        "peer_id": sender_id,
        "display_name": player_name_dictionary[sender_id],
        "is_respawning": false
    })

    # 同步敌人状态
    enemy_manager.synchronize(sender_id)
\`\`\`

> **设计说明**：
> - \`get_remote_sender_id()\` 获取发起请求的客户端 peer ID
> - \`spawn()\` 触发 MultiplayerSpawner 自动同步创建玩家到所有客户端
> - 新玩家加入时立即同步当前敌人状态

## 玩家断开处理

\`\`\`gdscript
# 服务器端处理
func _on_peer_disconnected(peer_id: int):
    if player_dictionary.has(peer_id):
        var player := player_dictionary[peer_id]
        if is_instance_valid(player):
            player.kill()
        player_dictionary.erase(peer_id)
\`\`\`

> **设计说明**：服务器检测到玩家断开，调用 \`kill()\` 销毁玩家节点。客户端会收到同步的节点销毁。

## 玩家死亡与复活

### 死亡处理

\`\`\`gdscript
# 玩家死亡
func _on_died():
    kill()

func kill():
    if !is_multiplayer_authority():
        push_error("Cannot call kill on non-server client")
        return

    _kill.rpc()
    await get_tree().create_timer(.5).timeout

    died.emit()
    queue_free()

@rpc("authority", "call_local", "reliable")
func _kill():
    is_dying = true
    player_input_synchronizer_component.public_visibility = false
\`\`\`

> **设计说明**：
> - \`kill()\` 仅服务器可调用
> - \`_kill.rpc()\` 通知所有客户端玩家死亡
> - 延迟 0.5 秒给死亡动画播放时间

### 服务器监听死亡

\`\`\`gdscript
func _on_player_died(peer_id: int):
    dead_peers.append(peer_id)
    check_game_over()
\`\`\`

> **设计说明**：服务器追踪死亡玩家到 \`dead_peers\` 数组。

### 游戏结束检查

\`\`\`gdscript
func check_game_over():
    var is_game_over := true

    for peer_id in get_all_peers():
        if !dead_peers.has(peer_id):
            is_game_over = false
            break

    if is_game_over:
        end_game()
\`\`\`

> **设计说明**：所有玩家都死亡时游戏结束。

### 回合复活

\`\`\`gdscript
func _on_round_completed():
    respawn_dead_peers()

func respawn_dead_peers():
    var all_peers := get_all_peers()
    for peer_id in dead_peers:
        if !all_peers.has(peer_id):
            continue
        multiplayer_spawner.spawn({
            "peer_id": peer_id,
            "display_name": player_name_dictionary[peer_id],
            "is_respawning": true
        })
    dead_peers.clear()
\`\`\`

> **设计说明**：每回合结束时，复活本回合死亡的玩家。使用 \`multiplayer_spawner.spawn()\` 同步创建复活玩家。

## 场景切换

### 游戏结束切换

\`\`\`gdscript
func end_game():
    get_tree().paused = false
    # 断开网络连接
    multiplayer.multiplayer_peer = OfflineMultiplayerPeer.new()
    # 返回主菜单
    get_tree().change_scene_to_file(MAIN_MENU_SCENE_PATH)
\`\`\`

> **设计说明**：
> - 设置为 \`OfflineMultiplayerPeer\` 断开网络，转为单机模式
> - 切换回主菜单场景

### 菜单到游戏

\`\`\`gdscript
# multiplayer_menu.gd
func _on_host_pressed():
    var server_peer := ENetMultiplayerPeer.new()
    server_peer.create_server(MultiplayerConfig.port)
    multiplayer.multiplayer_peer = server_peer
    get_tree().change_scene_to_packed(main_scene)

func _on_connected_to_server():
    get_tree().change_scene_to_packed(main_scene)
\`\`\`

> **设计说明**：主机直接切换，客户端等待连接成功后切换。

## 游戏流程状态机

\`\`\`
┌──────────────────┐
│    主菜单         │
└────────┬─────────┘
         │
    ┌────┴────┐
    ▼          ▼
┌───────┐  ┌───────┐
│ Host  │  │ Join  │
└───┬───┘  └───┬───┘
    │          │
    └────┬─────┘
         ▼
┌──────────────────┐
│   大厅等待        │
│ (等待玩家准备)    │
└────────┬─────────┘
         │ 所有玩家准备
         ▼
┌──────────────────┐
│   游戏进行        │
│ - 敌人生成        │
│ - 战斗           │
│ - 回合结算        │
└────────┬─────────┘
         │
    ┌────┴────┐
    ▼          ▼
┌───────┐  ┌───────┐
│ 游戏  │  │ 游戏  │
│ 完成  │  │ 结束  │
└───┬───┘  └───┬───┘
    │          │
    └────┬─────┘
         ▼
┌──────────────────┐
│    主菜单         │
└──────────────────┘
\`\`\`

## 数据管理

### 玩家字典

\`\`\`gdscript
var player_dictionary: Dictionary[int, Player] = {}
var player_name_dictionary: Dictionary[int, String] = {}
var dead_peers: Array[int] = []
\`\`\`

> **设计说明**：使用 peer ID 作为键存储玩家引用，便于通过 ID 查找。

### 获取所有 Peer

\`\`\`gdscript
func get_all_peers() -> PackedInt32Array:
    var all_peers := multiplayer.get_peers()
    all_peers.push_back(multiplayer.get_unique_id())
    return all_peers
\`\`\`

> **设计说明**：\`get_peers()\` 只返回客户端，不含服务器自己，需要手动添加。

## 设计要点总结

| 功能 | 实现方式 |
|------|----------|
| 玩家生成 | MultiplayerSpawner |
| 玩家追踪 | Dictionary[int, Player] |
| 死亡追踪 | dead_peers Array |
| 场景切换 | change_scene_to_file / change_scene_to_packed |
| 断线处理 | peer_disconnected 信号 |

## 相关文档

- [网络架构概述](/tutorial/mutiplayer_tutorial/network-architecture-overview)
- [大厅与连接管理](/tutorial/mutiplayer_tutorial/lobby-and-connection-management)
- [敌人AI与状态管理](/tutorial/mutiplayer_tutorial/enemy-ai-and-state-management)
`,g=`---\r
title: 导航系统以及A星算法\r
sticky: false\r
star: false\r
author: \r
  - 玄溟\r
  - 阿鱼\r
category:\r
  - 教程\r
tag:\r
  - 功能教程\r
date: 2025-10-13\r
---\r
\r
\`\`\` ::close 教程使用环境\r
\r
- Godot v4.5.1-stable\r
- Windows 10\r
\`\`\`\r
\r
## Godot导航系统的基本概念和组件\r
\r
### Godot导航系统基本概念\r
\r
当你完成了游戏中的角色控制，可以操作像素小人肆意的奔跑在绿色草地上的时候，也许你会想：为什么我要亲自动手？他能不能自己跑起来？那么好，Godot确实为你这个想法提供了支持，他就是导航系统。\r
\r
让一个像素小人从一个点移动到另一个点，听起来一点也不复杂，甚至你完全可以自己编写代码让你的像素小人一格一格移动到目标点。\r
\r
实际上导航系统一点也不简单，你需要考虑场景中障碍物位置和可行走区域，确保导航路径生成时能正确避开障碍物，需要考虑计算路径的时间成本，还有动态障碍物、如何便于调试等等问题。\r
\r
所以Godot帮我们考虑过了这些事情，我们只需要花几分钟学习如何使用，然后就能轻松的让你的像素小人自动移动起来。\r
\r
#### 名词释义\r
\r
- **自动寻路**：只给出两个点，让角色自动找到两个点的有效路径\r
\r
- **烘焙(Bake)**：事先计算好的意思\r
\r
- **导航代理(NavigationAgent)**：简单理解为“AI的导航员”，帮助角色规划从当前位置到目标点的最优路径，并控制角色沿路径移动，同时避开障碍物\r
\r
- **导航网格(NavigationMesh)**：定义场景中可行走的区域的“虚拟地图”，由**多边形**组成\r
\r
- **导航区域(NavigationRegion)**:场景中标记需要生成导航数据的区域，通常包含障碍物和可行走地形\r
\r
- **A*算法(AStar)**：也叫A星算法，经典寻路算法，通过网格或点连接计算最短路径\r
\r
- **动态避障(Dynamic Obstacle Avoidance)**：代理在移动中实时避开其他动态物体（如玩家或移动障碍）\r
\r
- **路径点(PathPoint)**：A*算法中用于连接的可遍历位置，代理通过路径点序列移动\r
\r
- **代价(Cost)**：路径规划中不同区域的权重（如草地比沼泽更易通行）\r
\r
### Godot导航系统核心组件\r
这里列举了Godot官方提供的导航节点，详细的内容和方法可以直接查看官方文档，我们这里只简单讨论一下，侧重示例演示。\r
\r
注意：下面列出的4个节点在当前版本被标记为实验性，也就是说他有可能在将来的版本改动或者弃用，请考虑清楚后使用。\r
\r
#### NavigationAgent2D节点\r
用于寻路至某个位置并且能够躲避障碍物的 2D 代理。\r
\r
通常情况下，对需要根据导航移动的精灵节点添加上这个节点，就可以实现自动寻路逻辑了\r
\`\`\`gdscript\r
extends CharacterBody2D\r
\r
@onready var navigation_agent_2d: NavigationAgent2D = $NavigationAgent2D\r
const SPEED = 300.0\r
\r
func _physics_process(_delta: float) -> void:\r
	var dir = get_nav_dir()\r
	# 没有目的地\r
	if not dir:\r
		return\r
	# 已经到目的地附近\r
	if navigation_agent_2d.is_navigation_finished():\r
		velocity = Vector2.ZERO\r
		move_and_slide()\r
		return\r
	velocity = dir * SPEED\r
	move_and_slide()\r
\r
# 将点击的位置传入导航代理，并将获取到的下一个移动节点转本地坐标\r
func get_nav_dir():\r
	if not clickPosition:\r
		return\r
	navigation_agent_2d.target_position = clickPosition\r
	return to_local(navigation_agent_2d.get_next_path_position()).normalized()\r
\`\`\`\r
如图：\r
![点击导航](/images/tutorial/navigation/点击导航.gif)\r
\r
需要注意的是，追击的时候如果单位碰撞范围>target_desired_distance，会导致单位永远到不了目的地,然后抖动\r
如图：\r
![碰撞抖动](/images/tutorial/navigation/碰撞抖动.gif)\r
\r
当正确设置好碰撞范围，则不会出现抖动的情况\r
如图：\r
![碰撞停止抖动](/images/tutorial/navigation/碰撞停止抖动.gif)\r
\r
\r
#### NavigationObstacle2D节点\r
用于导航的 2D 障碍物，能够影响导航网格的烘焙、约束启用了避障处理的代理的速度。\r
\r
简单来说就是这个节点可以为当前的障碍物添加一个更大范围的碰撞，代理移动的时候会被这个大一些的碰撞体阻挡住。\r
如图：\r
![避障](/images/tutorial/navigation/避障.gif)\r
\r
#### NavigationRegion2D节点\r
可达的 2D 地区，NavigationAgent2D 能够将其用于寻路。\r
\r
- 可以自定义一个导航区域\r
如图：\r
![自定义导航区域](/images/tutorial/navigation/自定义导航区域.gif)\r
\r
- 根据障碍物自动烘焙可导航区域\r
创建导航区域后，设置source_geometry_mode为Group Expicit，然后复制source_geometry_group_name中的值新建一个分组，将指定障碍物图层设置到这个分组下，就可以进行自动烘焙了\r
如图：\r
![自定义烘焙导航区域](/images/tutorial/navigation/自定义烘焙导航区域.gif)\r
\r
!!! note 注意\r
需要烘焙的那个障碍物图层如果是手动设置的物理层，需要注意多个图块尽可能连接准确，如果出现不同图块物理层连接错位，有可能导致最终烘焙的导航区域出现重合的边发生错误\r
!!!\r
\r
- TileMapLayer设置TileSet资源的时候也可以新增导航区域，这时只要把图块放到游戏中，指定的图块就自动设置为导航区域了\r
如图：\r
![TileMapLayer添加导航图块](/images/tutorial/navigation/TileMapLayer添加导航图块.png)\r
\r
\r
#### NavigationLink2D节点\r
暂无说明\r
\r
\r
\r
## A*算法\r
\r
本教程主要是介绍Godot中内置的算法类\r
\r
### A*算法简介\r
\r
A*算法由Peter Hart、Nils Nilsson和Bertram Raphael于1968年提出，最初用于解决图搜索问题。其设计灵感来源于Dijkstra算法和启发式搜索的结合，通过引入启发式函数（如曼哈顿距离）显著提升了搜索效率。该算法在早期人工智能和机器人导航领域得到验证，后逐渐成为路径规划的经典方法。\r
\r
在Godot中A*算法主要用于NPC寻路、动态障碍物避让等场景。\r
\r
他的优势是提供一个启发函数，（比如曼哈顿距离）估算当前节点到目标的“直线距离”，算法总是优先计算“实际已走距离+直线距离”总和最小的节点。简单来说就是不会绕路。这点相比Dijkstra算法全搜索最少提升30%的效率。并且支持自定义代价函数，可以适应不同的地形，例如：高地地形比平原地形难走，可以通过自定义代价函数来实现。\r
当然，A*算法的局限性也在启发函数上，就像你用现实生活中的导航软件，导航有时候会给你导航到一条看似近的路，实际上路上在封路施工，导致你最终绕更远的路，这或许就是所谓成也萧何败萧何，在使用的时候如果遇到性能下降的情况，可以考虑是否是启发函数的问题，我们这里暂时就不做深入讨论了。\r
\r
### Godot4内置的A*算法\r
\r
#### AStar2D\r
用于在二维空间的连通图中，寻找两个顶点之间的最短路径。\r
\r
需要手动添加路径点，并且建立点的连接。这个类适用任何类型的图形（四边形，六边形等）。当然，如果你的游戏是六边形网格，也有相应的插件提供导航功能，例如：AStarHexGrid2D，这不在本次教程的讨论范围，感兴趣的可以自行学习。\r
\r
#### AStarGrid2D\r
AStarGrid2D 是 AStar2D 的变种，针对疏松 2D 网格进行了优化。因为不需要手动创建点并进行连接，所以用起来更加简单。这个类还支持使用不同的启发方法、斜向移动模式、跳跃模式，从而加速运算。\r
\r
相对于AStar2D，他是一个矩形网格，限制了网格的点有4个相邻点，可以直接用于矩形网格游戏导航。\r
\r
只需提供导航区域、单元格大小。其他内容引擎会自行处理。\r
\r
\r
### 本教程所用工程文件\r
[Github仓库地址](https://github.com/JessieLR/Godot_Navigation/tree/main)\r
\r
\r
## 在Godot使用避障提升性能\r
\r
### 说在前面\r
\r
本文使用的方法中用到的节点在4.5版本中被标记为实验性，如果您是在更新的版本中看到了该方法，则下文节点有被未来版本删除的可能性，不过godot也许会有相应的替代节点或是别的实现方式。（没有就当笔者没说）\r
\r
### 为什么要提升性能\r
\r
因为遇到了性能问题，或是预见了性能问题一定会成为阻拦开发的障碍。如果没有遇到性能问题也没预见自己之后的开发会遭遇何种性能瓶颈的话，笔者推荐暂时不用急着考虑如何将优化做到完美。\r
\r
### 为什么使用避障\r
\r
以笔者为例，笔者在开发类幸存者原型的时候因为设计了多种敌人，出于快速实现原型的需求，笔者让这些敌人简单的朝着玩家所在的点加速移动直到最高速度，并且给每个不同强度的敌人使用了不同的加速度与最大速度，同时希望这些（同种）敌人在追逐玩家时不要因为玩家带着敌人绕圈而汇聚成“一坨的敌人”，这样无论是何种攻击方式都基本将攻击到近乎所有的尾随敌人。所以笔者一开始为此给所有的敌人使用了相同大小的圆形碰撞体，旨在让敌人追逐玩家时可以自然的散成“茫茫的敌人”，而攻击方式的数值和范围也可以得到合理的展现。\r
\r
但是，问题出现的想象的要快，屏幕上超过300个怪物在追逐玩家的时候，因为每种怪物的最大速度不一致导致敌人之间的彼此碰撞明显增多，游戏明显变得卡顿。经由大佬提醒，（2D项目中）可以使用NavigationRegion2D节点+敌人身上附着NavigationAgent2D节点来实现对敌人内部实现避障，从根本上解决过多的碰撞检测带来的性能开销。\r
\r
### 性能提升效果\r
\r
测试项目的内容为每帧生成一个会追击鼠标最后点击的位置的小鸡并打印当前小鸡数量。\r
\r
在一个只有简单结构的小鸡身上，使用4px半径避障时在全图小鸡数量达到2k以上的时候，笔者电脑的调试帧数才出现些许下降。（60帧于小鸡数量2k时下降至30帧左右）\r
\r
![避障小鸡](/images/tutorial/navigation/避障小鸡.png)\r
\r
![避障性能](/images/tutorial/navigation/避障性能.png)\r
\r
而使用4px圆形碰撞体实现效果时，仅在900只小鸡数量的时候，笔者电脑的调试帧数就从60狂跌到10帧。（60帧于小鸡数量900时下降至10帧左右)\r
\r
![碰撞小鸡](/images/tutorial/navigation/碰撞小鸡.png)\r
\r
![碰撞性能](/images/tutorial/navigation/碰撞性能.png)\r
\r
### 如何使用避障（2D项目）\r
\r
我们需要知道避障的本质是选择一个更合理的不会撞到（敌人的）同伴的路径，所以避障算是寻路/导航的一部份，关于这部分内容则可以查询新手村的另一篇教程“导航系统以及A星算法”。\r
\r
所以在我们的游戏中，想要实现避障需要解决两个问题：应该在哪里避障（导航）？谁需要避障（导航）？\r
\r
#### 1、应该在哪里避障（导航）？\r
\r
在2D界面以下三个节点中我们应最常用到的是NavigationRegion2D。简单来说，该Region的范围便是处于该导航层（navigation_layers）上的需要被导航的对象（NavigationAgent2D）会使用到的范围。\r
\r
而NavigationObstacle2D和NavigationLink2D，笔者也还不熟悉。~~（老陌帮我想想怎么糊弄过去）~~ ~~（老陌也不会）~~\r
\r
![应该在哪里避障](/images/tutorial/navigation/应该在哪里避障.png)\r
\r
一般我们使用NavigationRegion2D划定的方式大多为以下两种\r
\r
##### 1·直接使用3个及以上的点构成的多边形作为导航多边形\r
\r
如图，只需要在选择模式(Q)中画出自己想要的形状便可以完成多边形。\r
\r
![自行划定多边形](/images/tutorial/navigation/自行划定多边形.png)\r
\r
![画多边形](/images/tutorial/navigation/画多边形.png)\r
\r
##### 2·使用tileset的Navigation Layers设置完成地图的构建后烘焙成完整多边形\r
\r
如图，tileset的方式会稍微复杂一些，需要设置好了tileset并完成了tilemap之后点击“烘焙导航多边形”才行，不过好处是tileset里面做完了之后无论是新拓展什么瓦片组合的地形，只需要做好之后重新烘焙便可完成调整。\r
\r
![tileset导航](/images/tutorial/navigation/tileset导航.png)\r
\r
![tilemap烘焙多边形](/images/tutorial/navigation/tilemap烘焙多边形.png)\r
\r
#### 2、谁需要避障（导航）？\r
\r
一言以蔽之，NavigationAgent2D。在设置好navigation_layers并对需要的属性进行调整之后，只需要在需要使用避障的会移动的节点（以CharacterBody2D举例）上挂载NavigationAgent2D节点，并设置其的target_position，在每个物理帧中使用调用get_next_path_position()得到的向量进行归一化并乘上节点的速度后由NavigationAgent2D的velocity_computed信号来执行move_and_slide()即可。\r
\r
![谁需要避障](/images/tutorial/navigation/谁需要避障.png)\r
\r
### 避障做不到的事\r
\r
说是导航+避障，但其实不在导航内的区域如果是不想让节点去的地方的话，其实并不代表不会被太多数量的对象挤过去，这部分需求应当还是使用NavigationObstacle2D或是碰撞达成。同时就算是使用了避障导航避免碰撞的计算问题，但导航本身大量使用也是有性能开销的，如果需要更加大量的对象进行分散式追逐的话，应该需要寻找别的解决方案。\r
\r
`,y=`---\r
title: 其他B站UP主视频列表\r
description: 展示B站优秀的Godot教程视频\r
category:\r
  - 教程\r
date: 2025-10-11\r
---\r
\r
## 说明\r
B站作为国内最大的学习平台之一，汇聚了众多优秀的Godot教学资源。这些教程内容丰富多样，既有结构完整的系统性课程，也有针对特定功能的精品专题教程，能够满足从初学者到进阶开发者的不同学习需求。\r
\r
在这里，我们精心整理了一批在Godot教学领域表现突出的UP主，他们凭借深厚的技术功底和优秀的教学能力，为广大学习者提供了高质量的学习内容。无论你是刚接触游戏开发的新手，还是希望深入掌握Godot高级特性的开发者，都能在这些教程中找到适合自己的学习路径。\r
\r
<!-- more -->\r
\r
## timothyqiu\r
[timothyqiu](https://space.bilibili.com/7092) 是B站知名的Godot游戏引擎教程创作者，以高质量、系统性的Godot 4教学视频著称，其《Godot 4 教程《勇者传说》》系列从基础项目到完整游戏开发共28个章节，涵盖角色控制、战斗系统、UI界面、存档读档等完整功能，同时他还制作插件开发和寻路算法等进阶内容，并参与Godot官方引擎开发，是Godot中文社区的重要贡献者和优质学习资源。\r
<BiliBlackboard width="750px" link="BV1SP411m7aj"/>\r
\r
\r
## 码客二十二\r
[码客二十二](https://space.bilibili.com/204475293) 是B站知名的Godot游戏引擎教程创作者，以系统全面的Godot 4教学视频著称，其"小狐狸"系列从零基础入门到完整2D游戏开发，涵盖安装配置、场景搭建、GD Script编程、碰撞检测、信号系统、UI界面、音乐音效等核心功能，同时还有进阶的3D系列教程包括角色创建、摄像机控制、动画系统、粒子效果、材质纹理等3D开发内容，以及丰富的Shader案例教程，所有内容均为原创且提供完整项目素材，被众多Godot学习者视为优质入门资源。\r
<BiliBlackboard width="750px" link="BV1fuCrYFEoG"/>\r
<BiliBlackboard width="750px" link="BV13DaAzZELp"/>\r
<BiliBlackboard width="750px" link="BV1fn4y1R7yg"/>\r
\r
## xcount\r
[xcount](https://space.bilibili.com/351607965) 是B站上专注于Godot游戏引擎教程搬运的up主，他大量搬运YouTube上的优质Godot教学视频，数量庞大约750+，内容主题极其丰富多样，涵盖了Godot开发的各个方面，从基础入门到高级技巧都有涉及，虽然内容庞杂但为需要深入学习特定技术细节的开发者提供了宝贵的资源库，是Godot学习者查找特定解决方案和进阶技术的重要参考来源。`,v=`---
title: 着色器简介
author: 陌上竹
date: 2026-03-23
category:
  - 教程
tag:
  - 着色器
---

\`\`\` ::close 链接
着色器相关文档：[Godot文档-着色器简介](https://docs.godotengine.org/zh-cn/4.x/tutorials/shaders/introduction_to_shaders.html)
\`\`\`

## 什么是 Shader

Shader（着色器）是一种运行在 GPU 上的特殊程序，负责决定如何处理网格模型的数据（如顶点位置、颜色、法线等）以及如何将它们绘制到屏幕上。

Godot 引擎的 Shader 语言基于流行的 OpenGL Shading Language (GLSL)，但进行了简化和扩展，提供了更方便开发者使用的接口。

<!-- more -->

## 为什么使用 Shader

- **高性能**：GPU 并行处理，适合处理大量像素
- **程序化生成**：可以动态生成纹理、颜色、效果
- **视觉效果**：实现光照、阴影、材质等复杂效果
- **实时反馈**：支持时间相关的动态效果

## Godot 中的 Shader 类型

Godot 4 支持 5 种 Shader 类型：

### canvas_item（2D 着色器）

用于 2D 场景中的渲染，如 Sprite2D、Control 组件、ColorRect 等。

\`\`\`gdshader
shader_type canvas_item;

void vertex() {
    // 顶点着色器
}

void fragment() {
    // 片段着色器
    COLOR = vec4(1.0, 0.0, 0.0, 1.0); // 输出红色
}
\`\`\`

### spatial（3D 着色器）

用于 3D 场景中的物体渲染，如网格、地形等。

\`\`\`gdshader
shader_type spatial;

void vertex() {
    // 顶点着色器：处理每个顶点
}

void fragment() {
    // 片段着色器：处理每个像素
}

void light() {
    // 光照着色器：处理光照计算
}
\`\`\`

### particles（粒子着色器）

用于粒子系统的渲染。

\`\`\`gdshader
shader_type particles;

void start() {
    // 粒子产生时调用
}

void process() {
    // 粒子每帧更新时调用
}

void fragment() {
    // 粒子片段着色器
}
\`\`\`

### sky（天空着色器）

用于渲染天空盒和大气效果。

\`\`\`gdshader
shader_type sky;

void sky() {
    // 天空着色器主函数
}

void fragment() {
    // 片段处理
}
\`\`\`

### fog（雾效着色器）

用于体积雾效的渲染。

\`\`\`gdshader
shader_type fog;

void fog() {
    // 雾效着色器主函数
}
\`\`\`

## 数据类型

Godot Shader 支持绝大多数 GLSL ES 3.0 数据类型：

### 基础类型

| 类型 | 描述 |
|------|------|
| \`void\` | 空类型 |
| \`bool\` | 布尔类型 |
| \`int\` | 有符号整型 |
| \`float\` | 浮点数 |
| \`bvec2/3/4\` | 2/3/4 维布尔向量 |
| \`ivec2/3/4\` | 2/3/4 维整型向量 |
| \`vec2/3/4\` | 2/3/4 维浮点向量 |

### 矩阵类型

| 类型 | 描述 |
|------|------|
| \`mat2\` | 2x2 矩阵 |
| \`mat3\` | 3x3 矩阵 |
| \`mat4\` | 4x4 矩阵 |

### 纹理类型

| 类型 | 描述 |
|------|------|
| \`sampler2D\` | 2D 纹理采样器 |
| \`samplerCube\` | 立方体纹理采样器 |
| \`sampler3D\` | 3D 纹理采样器 |

### 代码示例

\`\`\`gdshader
// 向量定义
vec2 uv = vec2(0.5, 0.5);
vec3 normal = vec3(0.0, 1.0, 0.0);
vec4 color = vec4(1.0, 0.0, 0.0, 1.0);

// 矩阵定义
mat2 rot2d = mat2(1.0, 0.0, 0.0, 1.0);
mat3 basis = mat3(MODEL_MATRIX);

// Swizzling（分量重排）
vec4 a = vec4(1.0, 2.0, 3.0, 4.0);
vec3 b = a.xyz;  // b = vec3(1.0, 2.0, 3.0)
float c = b.x;   // 可以，只取 x 分量
\`\`\`

### 精度修饰符

\`\`\`gdshader
lowp float a;    // 低精度
mediump vec2 b;  // 中精度
highp int c;     // 高精度

// 也可以使用关键词
precision lowp float;
\`\`\`

## Uniform 变量声明与使用

Uniform 变量用于从 CPU 端向 GPU 端传递数据，在整个绘制过程中保持不变。

### 基本语法

\`\`\`gdshader
shader_type canvas_item;

uniform float my_float = 1.0;        // 带默认值
uniform vec4 my_color;               // 不带默认值
uniform sampler2D my_texture;        // 纹理采样器
\`\`\`

### Hint 提示

Hint 用于在编辑器中提供更好的 UI 交互和默认值提示。

| Hint | 描述 | 示例 |
|------|------|------|
| \`hint_range(min, max)\` | 范围提示 | \`uniform float amount : hint_range(0, 1) = 0.5;\` |
| \`source_color\` | 颜色提示 | \`uniform vec4 color : source_color;\` |
| \`hint_albedo\` | 纹理提示（默认白色） | \`uniform sampler2D tex : hint_albedo;\` |
| \`hint_normal\` | 法线纹理提示 | \`uniform sampler2D normal_map : hint_normal;\` |
| \`hint_black\` | 黑色值提示 | \`uniform sampler2D black_tex : hint_black;\` |
| \`hint_white\` | 白色值提示 | \`uniform sampler2D white_tex : hint_white;\` |

### 代码示例

\`\`\`gdshader
shader_type canvas_item;

// 颜色uniform
uniform vec4 hurt_color : source_color;
// 范围uniform
uniform float hurt_intensity : hint_range(0.0, 1.0) = 0.0;
// 带步长的范围uniform
uniform float progress : hint_range(0.0, 1.0, 0.1) = 0.5;
// 纹理uniform
uniform sampler2D light_vector;

void fragment() {
    vec4 color = texture(TEXTURE, UV);
    color = mix(color, hurt_color, hurt_intensity * color.a);
    COLOR = color;
}
\`\`\`

### GDScript 端设置 Uniform

\`\`\`gdscript
# 获取 ShaderMaterial
var material = $Sprite2D.material as ShaderMaterial
# 设置 uniform 参数
material.set_shader_parameter("hurt_intensity", 0.5)
material.set_shader_parameter("hurt_color", Color(1, 0, 0))
\`\`\`

## 内置全局变量

### 数学常量

| 变量 | 描述 | 值 |
|------|------|-----|
| \`PI\` | 圆周率 | 3.14159265359 |
| \`TAU\` | 2倍圆周率 | 6.28318530718 |
| \`E\` | 自然常数 e | 2.71828182846 |
| \`ROOT2\` | 根号2 | 1.41421356237 |
| \`INFINITY\` | 无穷大 | 无穷大 |

### 时间相关

| 变量 | 描述 |
|------|------|
| \`TIME\` | 从游戏开始经过的时间（秒） |
| \`FRAME\` | 当前帧编号 |

### UV 坐标

| 变量 | 描述 |
|------|------|
| \`UV\` | 当前像素的 UV 坐标（0.0 ~ 1.0） |

### 代码示例

\`\`\`gdshader
shader_type canvas_item;

void fragment() {
    // 使用 TIME 创建动画效果
    float wave = sin(UV.y * 10.0 + TIME * 5.0) * 0.1;

    // 使用 UV 创建渐变
    vec4 color = texture(TEXTURE, UV);
    color.rgb *= UV.x; // 水平渐变

    COLOR = color;
}
\`\`\`

## 各类型内置变量

### canvas_item 内置变量

**顶点着色器 (vertex)**

| 变量 | 描述 |
|------|------|
| \`VERTEX\` | 顶点位置 |
| \`UV\` | 顶点 UV 坐标 |
| \`COLOR\` | 顶点颜色 |

**片段着色器 (fragment)**

| 变量 | 描述 |
|------|------|
| \`UV\` | 片段 UV 坐标 |
| \`COLOR\` | 输出颜色 |
| \`TEXTURE\` | 当前纹理（canvas_item 使用） |
| \`TEXTURE_PIXEL_SIZE\` | 纹理像素大小 |

### spatial 内置变量

**顶点着色器 (vertex)**

| 变量 | 描述 |
|------|------|
| \`VERTEX\` | 顶点位置（局部空间） |
| \`UV\` | 顶点 UV 坐标 |
| \`NORMAL\` | 顶点法线 |
| \`MODEL_MATRIX\` | 模型矩阵 |
| \`MODEL_NORMAL_MATRIX\` | 模型法线矩阵 |
| \`VIEW_MATRIX\` | 视图矩阵 |
| \`INV_VIEW_MATRIX\` | 视图矩阵逆矩阵 |

**片段着色器 (fragment)**

| 变量 | 描述 |
|------|------|
| \`NORMAL\` | 法线 |
| \`ALBEDO\` | 基础颜色 |
| \`METALLIC\` | 金属度 |
| \`ROUGHNESS\` | 粗糙度 |
| \`EMISSION\` | 自发光 |
| \`ALPHA\` | 透明度 |

**光照着色器 (light)**

| 变量 | 描述 |
|------|------|
| \`LIGHT\` | 光照颜色 |
| \`LIGHT_POSITION\` | 光照位置 |
| \`LIGHT_DIRECTION\` | 光照方向 |
| \`LIGHT_ENERGY\` | 光照强度 |

### particles 内置变量

| 变量 | 描述 |
|------|------|
| \`VELOCITY\` | 粒子速度 |
| \`LIFETIME\` | 粒子生命周期 |
| \`DELTA\` | 帧间隔时间 |
| \`NUMBER\` | 粒子编号 |
| \`INDEX\` | 粒子索引 |

## 渲染模式（render_mode）

渲染模式用于改变着色器的默认行为，位于 \`shader_type\` 声明之后。

### canvas_item 渲染模式

| 模式 | 描述 |
|------|------|
| \`unshaded\` | 禁用光照，直接输出 COLOR |
| \`lightless\` | 不处理光照 |
| \`blend_disabled\` | 禁用混合 |
| \`blend_add\` | 加法混合 |
| \`blend_sub\` | 减法混合 |
| \`blend_mul\` | 乘法混合 |
| \`cull_disabled\` | 禁用背面剔除 |
| \`cull_back\` | 背面剔除 |
| \`cull_front\` | 正面剔除 |

### spatial 渲染模式

| 模式 | 描述 |
|------|------|
| \`unshaded\` | 无阴影 |
| \`cull_front\` | 正面剔除 |
| \`cull_back\` | 背面剔除（默认） |
| \`cull_disabled\` | 禁用剔除 |
| \`depth_draw_opaque\` | 不透明物体深度绘制 |
| \`depth_draw_always\` | 总是绘制深度 |
| \`depth_draw_never\` | 从不绘制深度 |

### particles 渲染模式

| 模式 | 描述 |
|------|------|
| \`blend_add\` | 加法混合 |
| \`blend_sub\` | 减法混合 |
| \`blend_mul\` | 乘法混合 |

### 代码示例

\`\`\`gdshader
// 2D 无光照着色器
shader_type canvas_item;
render_mode unshaded;

uniform vec4 tint_color : source_color = vec4(1.0);

void fragment() {
    COLOR = texture(TEXTURE, UV) * tint_color;
}

// 3D 双面渲染着色器
shader_type spatial;
render_mode cull_disabled, unshaded;

void fragment() {
    ALBEDO = vec3(1.0, 0.0, 0.0);
}
\`\`\`

## VisualShader 简介

VisualShader 是 Godot 提供的可视化着色器编辑器，无需编写代码即可创建复杂的着色器效果。

### VisualShader 的优势

- **无需编写代码**：通过节点连接实现功能
- **实时预览**：即时看到效果
- **易于学习**：适合初学者理解 Shader 逻辑
- **调试方便**：可以逐步查看节点输出

### 创建 VisualShader

1. 在节点上创建 \`ShaderMaterial\`
2. 选择新建「着色器」，类型选择 \`VisualShader\`
3. 选择模式：\`Canvas Item\`（2D）或 \`spatial\`（3D）

### 常用 VisualShader 节点

**2D (Canvas Item)**

- \`ColorParameter\`：颜色参数节点
- \`BooleanParameter\`：布尔参数节点
- \`TextureParameter\`：纹理参数节点
- \`FloatParameter\`：浮点数参数节点
- \`UV\`：UV 坐标节点
- \`Texture\`：纹理采样节点
- \`Multiply\`：乘法运算节点
- \`Mix\`：混合节点
- \`If\`：条件判断节点

### VisualShader 示例

创建一个简单的颜色叠加效果：

1. 添加 \`ColorParameter\` 节点，命名为 \`tint_color\`
2. 添加 \`TextureParameter\` 节点，命名为 \`main_tex\`
3. 添加 \`Multiply\` 节点
4. 连接：\`tint_color\` -> \`Multiply\` -> \`输出 Color\`

对应的代码：

\`\`\`gdshader
shader_type canvas_item;

uniform vec4 tint_color : source_color;
uniform sampler2D main_tex;

void fragment() {
    COLOR = texture(main_tex, UV) * tint_color;
}
\`\`\`

## 完整示例

### 示例 1：2D 闪烁效果

\`\`\`gdshader
shader_type canvas_item;

uniform vec4 flash_color : source_color = vec4(1.0, 1.0, 1.0, 1.0);
uniform float flash_intensity : hint_range(0.0, 1.0) = 0.0;

void fragment() {
    vec4 tex_color = texture(TEXTURE, UV);
    vec4 final_color = mix(tex_color, flash_color, flash_intensity);
    COLOR = final_color;
}
\`\`\`

### 示例 2：2D 描边效果

\`\`\`gdshader
shader_type canvas_item;

uniform vec4 outline_color : source_color = vec4(0.0, 0.0, 0.0, 1.0);
uniform float outline_width : hint_range(0.0, 50.0) = 2.0;

void vertex() {
    VERTEX += (UV * 2.0 - 1.0) * outline_width * TEXTURE_PIXEL_SIZE;
}

void fragment() {
    vec4 up = texture(TEXTURE, UV + vec2(0.0, TEXTURE_PIXEL_SIZE.y));
    vec4 down = texture(TEXTURE, UV - vec2(0.0, TEXTURE_PIXEL_SIZE.y));
    vec4 left = texture(TEXTURE, UV - vec2(TEXTURE_PIXEL_SIZE.x, 0.0));
    vec4 right = texture(TEXTURE, UV + vec2(TEXTURE_PIXEL_SIZE.x, 0.0));

    float outline = up.a + down.a + left.a + right.a;
    outline = clamp(outline, 0.0, 1.0);

    vec4 tex_color = texture(TEXTURE, UV);
    COLOR = mix(tex_color, outline_color, outline * (1.0 - tex_color.a));
}
\`\`\`

### 示例 3：3D 菲涅尔效果

\`\`\`gdshader
shader_type spatial;

uniform vec4 fresnel_color : source_color = vec4(1.0, 1.0, 1.0, 1.0);
uniform float fresnel_power : hint_range(0.0, 10.0) = 3.0;

void fragment() {
    float fresnel = pow(1.0 - dot(NORMAL, vec3(0.0, 0.0, 1.0)), fresnel_power);
    ALBEDO = mix(ALBEDO, fresnel_color.rgb, fresnel);
}
\`\`\`

## 实用技巧

### 从 GDScript 获取/设置 Shader 参数

\`\`\`gdscript
# 获取 ShaderMaterial
var shader_material = $Sprite2D.material as ShaderMaterial

# 设置参数
shader_material.set_shader_parameter("flash_intensity", 0.5)

# 获取参数
var intensity = shader_material.get_shader_parameter("flash_intensity")
\`\`\`

### 常用内置函数

\`\`\`gdshader
// 三角函数
sin(x), cos(x), tan(x)
asin(x), acos(x), atan(x)

// 数学函数
abs(x), floor(x), ceil(x), round(x)
min(x, y), max(x, y), clamp(x, min, max)
mix(x, y, a)  // 线性插值

// 实用函数
step(edge, x)        // 阶梯函数
smoothstep(a, b, x)  // 平滑阶梯
discard              // 丢弃片段
\`\`\`

## 着色器获取

除了自己编写程序以外，也可以直接在网上获取已经写好的shader。

### Godot Shaders
[Godot Shaders](https://godotshaders.com/) 是专门为 Godot 设计的着色器库，可以直接使用。

- 社区驱动的着色器资源库
- 包含材质、动画、后处理等各种效果
- 无需修改，直接下载使用

### Shadertoy
[Shadertoy](https://www.shadertoy.com/) 是全球最大的 GLSL 着色器平台，需要迁移到 Godot 格式。

- 海量的 GLSL 着色器示例
- 实时预览效果
- 包含各种创意和艺术效果

迁移方法参考：[Godot 着色器迁移指南](https://docs.godotengine.org/zh-cn/4.x/tutorials/shaders/converting_glsl_to_godot_shaders.html)

## 额外学习

### The Book of Shaders
[The Book of Shaders](https://thebookofshaders.com/?lan=ch) 是学习片段着色器的经典教程。

- 从基础概念到高级技巧的完整学习路径
- 包含大量交互式示例
- 涵盖算法绘画、生成设计、图像处理等主题
- 提供中文版本，便于学习

### DX11龙书
《Introduction to 3D Game Programming with DirectX 11》是游戏编程的经典教材，群文件内可下载学习。

- 深入讲解3D游戏编程原理
- 包含大量着色器实现细节
- 适合进阶学习使用

## 相关教程

- [着色器常用数学](./shader-math) - Shader 编程中的数学基础
- [着色器实现思路](./shader-implementation) - UV 操作、颜色处理、噪声应用等
- [着色器常见案例](./shader-common-cases) - 描边、像素化、灰度、溶解等实战案例
`,f=`---
title: 着色器常见案例
author: 陌上竹
date: 2026-03-23
category:
  - 教程
tag:
  - 着色器
  - 实战案例
---

本文整理了 Godot 4 中常见的 Shader 特效案例，涵盖描边、像素化、灰度、溶解、水波、模糊、发光的实现。

<!-- more -->

## 1. 描边着色器（轮廓检测）

### 效果描述

为精灵或字体添加外轮廓效果，常用于 UI 元素、选中状态或卡通风格的描边效果。

### 实现思路

- 在原图的基础上，对上下左右四个方向进行像素采样
- 将采样结果混合到透明区域形成描边
- 使用 \`mix\` 函数根据原图 alpha 值决定显示描边还是原图

### 核心代码

\`\`\`gdshader
shader_type canvas_item;

uniform float outline_width = 1.0;
uniform vec4 outline_color: source_color = vec4(1, 0, 0, 1);

void fragment() {
    vec2 uv = UV;
    // 上下左右四个方向偏移采样
    vec2 uv_up = uv + vec2(0, TEXTURE_PIXEL_SIZE.y) * outline_width;
    vec2 uv_down = uv + vec2(0, -TEXTURE_PIXEL_SIZE.y) * outline_width;
    vec2 uv_left = uv + vec2(TEXTURE_PIXEL_SIZE.x, 0) * outline_width;
    vec2 uv_right = uv + vec2(-TEXTURE_PIXEL_SIZE.x, 0) * outline_width;

    // 采样四个方向的像素
    vec4 color_up = texture(TEXTURE, uv_up);
    vec4 color_down = texture(TEXTURE, uv_down);
    vec4 color_left = texture(TEXTURE, uv_left);
    vec4 color_right = texture(TEXTURE, uv_right);

    // 合并描边颜色
    vec4 outline = color_down + color_up + color_left + color_right;
    outline.rgb = outline_color.rgb;

    // 获取原始颜色
    vec4 original_color = texture(TEXTURE, UV);

    // 根据 alpha 混合：透明区域显示描边，不透明区域显示原图
    COLOR = mix(outline, original_color, original_color.a);
}
\`\`\`

---

## 2. 像素化着色器

### 效果描述

将画面处理成像素风格，模拟复古游戏的视觉效果。

### 实现思路

- 根据指定的像素大小，计算每个像素块内的坐标
- 将 UV 坐标向下取整，实现像素对齐
- 通过调整像素块大小控制像素化程度

### 核心代码

\`\`\`gdshader
shader_type canvas_item;

uniform float pixel_size = 4.0;  // 像素块大小

void fragment() {
    // 计算像素化后的 UV
    vec2 uv = floor(UV * pixel_size) / pixel_size;
    COLOR = texture(TEXTURE, uv);
}
\`\`\`

---

## 3. 灰度/复古色着色器

### 效果描述

将图片转换为灰度图或带有复古色调，常用于实现老照片、怀旧风格或特殊滤镜效果。

### 实现思路

- 使用 Luma 公式（亮度公式）将 RGB 转换为灰度值
- 灰度系数：\`gray = R * 0.299 + G * 0.587 + B * 0.114\`
- 保留原始 alpha 通道以支持透明背景

### 核心代码

\`\`\`gdshader
shader_type canvas_item;

void fragment() {
    vec4 color = texture(TEXTURE, UV);
    // Luma 公式计算灰度
    float gray = dot(color.rgb, vec3(0.299, 0.587, 0.114));
    COLOR = vec4(vec3(gray), color.a);
}
\`\`\`

---

## 4. 溶解/燃烧效果

### 效果描述

实现物体逐渐溶解消失的效果，常用于角色死亡、物体销毁等场景。溶解边缘带有燃烧的色泽。

### 实现思路

- 使用噪声纹理作为溶解遮罩
- 通过 \`dissolve_value\` 控制溶解进度
- 使用 \`smoothstep\` 创建边缘过渡效果
- 在溶解边界混合燃烧颜色

### 核心代码

\`\`\`gdshader
shader_type canvas_item;

uniform sampler2D dissolve_texture: source_color;
uniform float dissolve_value: hint_range(0, 1) = 1.0;
uniform float burn_size: hint_range(0.0, 1.0, 0.01) = 0.1;
uniform vec4 burn_color: source_color = vec4(1.0, 0.3, 0.0, 1.0);

void fragment() {
    vec4 main_texture = texture(TEXTURE, UV);
    vec4 noise_texture = texture(dissolve_texture, UV);

    // 计算燃烧边缘过渡
    float burn_size_step = burn_size * step(0.001, dissolve_value) * step(dissolve_value, 0.999);
    float threshold = smoothstep(noise_texture.x - burn_size_step, noise_texture.x, dissolve_value);
    float border = smoothstep(noise_texture.x, noise_texture.x + burn_size_step, dissolve_value);

    // 应用溶解 alpha 和燃烧颜色
    COLOR.a *= threshold;
    COLOR.rgb = mix(burn_color.rgb, main_texture.rgb, border);
}
\`\`\`

### 使用说明

1. 创建 \`NoiseTexture2D\` 作为 \`dissolve_texture\`
2. 使用 "Simplex Smooth" 噪声类型
3. 调整 \`dissolve_value\`（0 = 完全溶解，1 = 未溶解）控制进度

---

## 5. 水波/折射效果

### 效果描述

模拟水面的波纹效果，包含波动和扭曲效果。

### 实现思路

- 使用噪声纹理驱动波纹动画
- 通过 \`cos(TIME)\` 生成随时间变化的波动
- 对 UV 坐标添加扰动实现折射效果
- 结合多方向波纹叠加

### 核心代码

\`\`\`gdshader
shader_type canvas_item;

uniform sampler2D noise_tex;
uniform float speed = 0.25;      // 波纹速度
uniform float strength = 0.02;    // 波纹强度
uniform vec4 tint_color: source_color = vec4(1.0, 1.0, 1.0, 1.0);

void fragment() {
    vec2 uv = UV;

    // 生成波纹扰动
    float noise = texture(noise_tex, uv + vec2(TIME * speed, 0.0)).r;
    float noise2 = texture(noise_tex, uv + vec2(0.0, TIME * speed * 0.5)).r;

    // 扰动 UV 实现折射
    vec2 offset = vec2(
        cos(TIME * speed + uv.y * 10.0) * strength,
        sin(TIME * speed + uv.x * 10.0) * strength
    ) * (noise + noise2);

    vec4 color = texture(TEXTURE, uv + offset);
    COLOR = color * tint_color;
}
\`\`\`

---

## 6. 模糊效果

### 效果描述

对图像进行模糊处理，常用于景深效果、UI 毛玻璃效果或运动模糊。

### 实现思路

- 对当前像素及其周围像素进行加权平均
- 使用高斯分布权重，中心像素权重最大
- 通过调整模糊半径控制模糊程度

### 核心代码

\`\`\`gdshader
shader_type canvas_item;

uniform float blur_radius = 2.0;  // 模糊半径
uniform vec2 blur_direction = vec2(1.0, 0.0);  // 模糊方向

const float MATRIX[5] = float[](0.227027, 0.1945946, 0.1216216, 0.054054, 0.016216);

void fragment() {
    vec2 uv = UV;
    vec2 texel_size = TEXTURE_PIXEL_SIZE;

    // 采样中心像素
    vec3 result = texture(TEXTURE, uv).rgb * MATRIX[0];

    // 采样周围像素（高斯权重）
    for (int i = 1; i < 5; i++) {
        float offset = float(i) * blur_radius;
        result += texture(TEXTURE, uv + texel_size * offset * blur_direction).rgb * MATRIX[i];
        result += texture(TEXTURE, uv - texel_size * offset * blur_direction).rgb * MATRIX[i];
    }

    vec4 original = texture(TEXTURE, uv);
    COLOR = vec4(result, original.a);
}
\`\`\`

---

## 7. 发光/光晕效果

### 效果描述

为图像添加发光效果，常用于霓虹灯、魔法特效或 UI 高亮显示。

### 实现思路

- 采样中心像素及其周围像素
- 将采样结果叠加到原始颜色上
- 通过 \`mix\` 函数控制发光强度

### 核心代码

\`\`\`gdshader
shader_type canvas_item;

uniform float glow_intensity = 0.5;  // 发光强度
uniform float glow_radius = 3.0;    // 发光半径

void fragment() {
    vec2 uv = UV;
    vec2 texel = TEXTURE_PIXEL_SIZE * glow_radius;

    // 采样中心
    vec4 color = texture(TEXTURE, uv);

    // 采样周围像素并叠加
    vec3 glow = vec3(0.0);
    for (float x = -2.0; x <= 2.0; x += 1.0) {
        for (float y = -2.0; y <= 2.0; y += 1.0) {
            vec2 offset = vec2(x, y) * texel;
            glow += texture(TEXTURE, uv + offset).rgb;
        }
    }
    glow /= 25.0;

    // 混合发光效果
    COLOR = color + vec4(glow * glow_intensity, 0.0);
}
\`\`\`

### 流光效果（发光变体）

\`\`\`gdshader
shader_type canvas_item;

uniform sampler2D light_vector;      // 流光引导图
uniform float width = 0.08;          // 流光宽度
uniform vec4 flowlight = vec4(0.3, 0.3, 0.0, 0.3);

void fragment() {
    vec4 color = texture(TEXTURE, UV);

    if (color.a != 0.0) {
        float v = texture(light_vector, UV).r;
        float diff = v - cos(TIME * 0.5);

        if (abs(diff) < width) {
            color = color + mix(flowlight, vec4(0.0), abs(diff) / width);
        }
    }

    COLOR = color;
}
\`\`\`

---

## 8. 卡通渲染（Cel Shading）

### 效果描述

模拟卡通/动漫风格的渲染效果，通过离散的色调过渡创造手绘感。

### 实现思路

- 将光照强度离散化为多个阶梯
- 使用 \`step\` 或 \`smoothstep\` 创建色调边界
- 可选添加描边效果增强卡通感

### 核心代码

\`\`\`gdshader
shader_type canvas_item;

uniform float levels = 3.0;  // 色调层级数
uniform vec4 light_color: source_color = vec4(1.0, 1.0, 1.0, 1.0);
uniform vec4 shadow_color: source_color = vec4(0.2, 0.2, 0.3, 1.0);

void fragment() {
    vec4 color = texture(TEXTURE, UV);

    // 简化光照计算（实际使用时替换为真实光照）
    float light = dot(color.rgb, vec3(0.33));

    // 离散化为多个层级
    light = floor(light * levels) / levels;

    // 根据光照层级混合颜色
    COLOR = mix(shadow_color, color * light_color, light);
}
\`\`\`

---

## 9. 噪声纹理应用

### 效果描述

程序化生成各种自然效果，如云朵、烟雾、地形等。

### 实现思路

- 使用 Simplex 噪声或 Perlin 噪声
- 通过 fBM（分形布朗运动）叠加多层噪声
- 调整频率和振幅控制细节

### 核心代码

\`\`\`gdshader
shader_type canvas_item;

uniform int octaves = 4;
uniform float frequency = 1.0;
uniform float amplitude = 0.5;
uniform float persistence = 0.5;

// 随机值
float rand(vec2 coord) {
    return fract(sin(dot(coord, vec2(56, 78))) * 1000.0);
}

// 噪声函数
float noise(vec2 coord) {
    vec2 i = floor(coord);
    vec2 f = fract(coord);

    float a = rand(i);
    float b = rand(i + vec2(1.0, 0.0));
    float c = rand(i + vec2(0.0, 1.0));
    float d = rand(i + vec2(1.0, 1.0));

    vec2 cubic = f * f * (3.0 - 2.0 * f);
    return mix(a, b, cubic.x) + (c - a) * cubic.y * (1.0 - cubic.x) + (d - b) * cubic.x * cubic.y;
}

// fBM 分形布朗运动
float fbm(vec2 coord) {
    float value = 0.0;
    float scale = 0.5;

    for (int i = 0; i < octaves; i++) {
        value += noise(coord) * scale;
        coord *= 2.0;
        scale *= persistence;
    }

    return value;
}

void fragment() {
    vec2 uv = UV * frequency;
    float n = fbm(uv);
    COLOR = vec4(vec3(n), 1.0);
}
\`\`\`

---

## 10. 混合模式（Blend Modes）

### 效果描述

实现 Photoshop 风格的混合模式，如正片叠底、滤色、叠加等。

### 实现思路

- 根据混合模式类型使用不同的数学公式
- 常见模式：Multiply（正片叠底）、Screen（滤色）、Overlay（叠加）

### 核心代码

\`\`\`gdshader
shader_type canvas_item;

uniform sampler2D blend_texture: source_color;
uniform float opacity: hint_range(0.0, 1.0) = 1.0;
uniform int blend_mode = 0;  // 0=Multiply, 1=Screen, 2=Overlay

vec3 multiply(vec3 base, vec3 blend) {
    return base * blend;
}

vec3 screen(vec3 base, vec3 blend) {
    return 1.0 - (1.0 - base) * (1.0 - blend);
}

vec3 overlay(vec3 base, vec3 blend) {
    return mix(
        2.0 * base * blend,
        1.0 - 2.0 * (1.0 - base) * (1.0 - blend),
        step(0.5, base)
    );
}

void fragment() {
    vec4 base = texture(TEXTURE, UV);
    vec4 blend = texture(blend_texture, UV);

    vec3 result;
    if (blend_mode == 0) {
        result = multiply(base.rgb, blend.rgb);
    } else if (blend_mode == 1) {
        result = screen(base.rgb, blend.rgb);
    } else {
        result = overlay(base.rgb, blend.rgb);
    }

    COLOR = vec4(mix(base.rgb, result, opacity * blend.a), base.a);
}
\`\`\`

---

## 相关教程

- [着色器简介](./intro) - Shader 基础概念与语法
- [着色器常用数学](./shader-math) - Shader 编程中的数学基础
- [着色器实现思路](./shader-implementation) - UV 操作、颜色处理、噪声应用等
`,h=`---
title: 着色器实现思路
author: 陌上竹
date: 2026-03-23
category:
  - 教程
tag:
  - 着色器
  - 实现思路
---

Shader（着色器）是一种运行在 GPU 上的特殊程序，用于控制渲染管线的各个阶段，实现各种视觉效果。在 Godot 4 中，Shader 编程是一个重要的技能，无论是 2D 还是 3D 项目都能广泛应用。

<!-- more -->

## Shader 编程工作流

从需求到实现的标准工作流：

1. **明确需求** - 确定要实现的视觉效果
2. **选择 Shader 类型** - Canvas Item（2D）、Spatial（3D）或粒子着色器
3. **设计算法** - 确定数学模型和实现思路
4. **编写代码** - 使用 GDScript 风格的 Shader 语言
5. **调试优化** - 测试效果并优化性能

## Shader 类型

Godot 4 主要支持三种 Shader 类型：

| 类型            | 用途    | 应用节点                       |
| ------------- | ----- | -------------------------- |
| \`canvas_item\` | 2D 渲染 | 2D 精灵、Control 节点、ColorRect |
| \`spatial\`     | 3D 渲染 | 3D 网格、体积                   |
| \`particles\`   | 粒子系统 | GPUParticles2D/3D          |

\`\`\`gdshader
# Canvas Item Shader (2D)
shader_type canvas_item;

# Spatial Shader (3D)
shader_type spatial;

# Particle Shader
shader_type particles;
\`\`\`

---

## UV 操作模式

UV 坐标是 Shader 编程的基础，用于纹理采样。UV 范围通常为 (0,0) 到 (1,1)。

### 1. 缩放 (Scale)

\`\`\`gdshader
shader_type canvas_item;

uniform vec2 scale = vec2(2.0, 2.0);
uniform vec2 pivot = vec2(0.5, 0.5);

void vertex() {
    UV -= pivot;
    UV /= scale;
    UV += pivot;
}
\`\`\`

### 2. 旋转 (Rotate)

\`\`\`gdshader
shader_type canvas_item;
render_mode unshaded;

uniform float angular_speed = 1.0;
uniform vec2 pivot = vec2(0.5, 0.5);

void vertex() {
    UV -= pivot;
    float rot = TIME * angular_speed;
    UV *= mat2(
        vec2(sin(rot), -cos(rot)),
        vec2(cos(rot), sin(rot))
    );
    UV += pivot;
}
\`\`\`

### 3. 偏移/滚动 (Offset/Scroll)

\`\`\`gdshader
shader_type canvas_item;

uniform vec2 scroll_speed = vec2(1, 0.0);

void fragment() {
    vec2 offset_uv = UV + cos(TIME) * scroll_speed;
    COLOR = texture(TEXTURE, offset_uv);
}
\`\`\`

### 4. 扭曲 (Distortion)

\`\`\`gdshader
shader_type canvas_item;

uniform float distortion_strength = 0.1;

void fragment() {
    vec2 distorted_uv = UV;
    distorted_uv.x += sin(UV.y * 10.0 + TIME) * distortion_strength;
    distorted_uv.y += cos(UV.x * 10.0 + TIME) * distortion_strength;
    COLOR = texture(TEXTURE, distorted_uv);
}
\`\`\`

---

## 颜色操作模式

### 1. 灰度处理

\`\`\`gdshader
shader_type canvas_item;

void fragment() {
    vec4 color = texture(TEXTURE, UV);
    float gray = dot(color.rgb, vec3(0.299, 0.587, 0.114));
    COLOR = vec4(vec3(gray), color.a);
}
\`\`\`

### 2. 颜色混合

\`\`\`gdshader
shader_type canvas_item;

uniform vec4 tint_color: source_color = vec4(1.0, 1.0, 1.0, 1.0);
uniform float intensity = 0.5;

void fragment() {
    vec4 color = texture(TEXTURE, UV);
    COLOR = mix(color, color * tint_color, intensity);
}
\`\`\`

### 3. 颜色调整（亮度、对比度、饱和度）

\`\`\`gdshader
shader_type canvas_item;

uniform float brightness = 0.0;  // -1.0 到 1.0
uniform float contrast = 1.0;    // 0.0 到 2.0
uniform float saturation = 1.0;  // 0.0 到 2.0

void fragment() {
    vec4 color = texture(TEXTURE, UV);

    // 亮度
    color.rgb += brightness;

    // 对比度
    color.rgb = (color.rgb - 0.5) * contrast + 0.5;

    // 饱和度
    float gray = dot(color.rgb, vec3(0.299, 0.587, 0.114));
    color.rgb = mix(vec3(gray), color.rgb, saturation);

    COLOR = color;
}
\`\`\`

### 4. 流水效果

\`\`\`gdshader
shader_type canvas_item;

uniform sampler2D light_vector;
uniform float width = 0.08;
uniform vec4 flowlight = vec4(0.3, 0.3, 0.0, 0.3);

void fragment() {
    vec4 color = texture(TEXTURE, UV);
    if (color.a != 0.0) {
        float v = texture(light_vector, UV).r;
        float diff = v - cos(TIME * 0.5);
        if (abs(diff) < width) {
            color = color + mix(flowlight, vec4(0.0), abs(diff) / width);
        }
    }
    COLOR = color;
}
\`\`\`

---

## 噪声应用

噪声是程序化纹理和视觉效果的核心工具。

### 1. Perlin Noise（柏林噪声）

适用于自然、有机的纹理效果，如云朵、烟雾、地形。

\`\`\`gdshader
// 简化的 Perlin Noise 实现
float perlin_noise(vec2 p) {
    vec2 i = floor(p);
    vec2 f = fract(p);
    f = f * f * (3.0 - 2.0 * f);

    float a = fract(sin(dot(i, vec2(127.1, 311.7))) * 43758.5453);
    float b = fract(sin(dot(i + vec2(1.0, 0.0), vec2(127.1, 311.7))) * 43758.5453);
    float c = fract(sin(dot(i + vec2(0.0, 1.0), vec2(127.1, 311.7))) * 43758.5453);
    float d = fract(sin(dot(i + vec2(1.0, 1.0), vec2(127.1, 311.7))) * 43758.5453);

    return mix(mix(a, b, f.x), mix(c, d, f.x), f.y);
}
\`\`\`

### 2. Voronoi / Worley Noise

适用于细胞状、破碎的纹理效果。

\`\`\`gdshader
float worley_noise(vec2 p) {
    vec2 i = floor(p);
    vec2 f = fract(p);
    float min_dist = 1.0;

    for (int x = -1; x <= 1; x++) {
        for (int y = -1; y <= 1; y++) {
            vec2 neighbor = vec2(float(x), float(y));
            vec2 point = fract(sin(dot(i + neighbor, vec2(127.1, 311.7))) * 43758.5453);
            float dist = length(neighbor + point - f);
            min_dist = min(min_dist, dist);
        }
    }
    return min_dist;
}
\`\`\`

### 3. FBM (Fractal Brownian Motion)

通过叠加多层噪声创建更复杂的自然纹理。

\`\`\`gdshader
float fbm(vec2 p, int octaves) {
    float value = 0.0;
    float amplitude = 0.5;
    float frequency = 1.0;

    for (int i = 0; i < octaves; i++) {
        value += amplitude * perlin_noise(p * frequency);
        amplitude *= 0.5;
        frequency *= 2.0;
    }
    return value;
}
\`\`\`

---

## 距离场 SDF 的使用

SDF（Signed Distance Field，有符号距离场）是一种强大的 2D 图形表示方法。

### 1. 圆形 SDF

\`\`\`gdshader
float sdCircle(vec2 p, float r) {
    return length(p) - r;
}

void fragment() {
    vec2 uv = UV * 2.0 - 1.0;
    float d = sdCircle(uv, 0.5);
    vec3 color = mix(vec3(1.0), vec3(0.0), smoothstep(0.0, 0.01, d));
    COLOR = vec4(color, 1.0);
}
\`\`\`

### 2. 矩形 SDF

\`\`\`gdshader
shader_type canvas_item;

float sdBox(vec2 p, vec2 b) {
    vec2 d = abs(p) - b;
    return length(max(d, 0.0)) + min(max(d.x, d.y), 0.0);
}

void fragment() {
    vec2 uv = UV * 2.0 - 1.0;
    float d = sdBox(uv, vec2(0.5));
    vec3 color = mix(vec3(1.0), vec3(0.0), smoothstep(0.0, 0.01, d));
    COLOR = vec4(color, 1.0);
}
\`\`\`

### 3. 圆角矩形

\`\`\`gdshader
shader_type canvas_item;

uniform float radius : hint_range(0, 0.5) = 0.1;
uniform bool hide_top_left = false;
uniform bool hide_top_right = false;
uniform bool hide_bottom_left = false;
uniform bool hide_bottom_right = false;

void fragment() {
    vec2 uv = UV;
    vec4 color = texture(TEXTURE, uv);

    // 左上角处理
    if (hide_top_left && uv.x < radius && uv.y < radius) {
        vec2 center = vec2(radius, radius);
        float dist = length(uv - center);
        if (dist > radius) {
            color.a = 0.0;
        }
    }

    // 其他角的处理类似...

    COLOR = color;
}
\`\`\`

### 4. SDF 描边效果

\`\`\`gdshader
shader_type spatial;
render_mode cull_front, unshaded;

uniform vec4 outline_color : source_color = vec4(1.0, 0.0, 0.0, 1.0);
uniform float grow = 0.05;

void vertex() {
    VERTEX += NORMAL * grow;
}

void fragment() {
    ALBEDO = outline_color.rgb;
}
\`\`\`

---

## 性能优化技巧

### 1. 减少纹理采样

- **纹理缓存** - 将 \`texture(TEXTURE, UV)\` 的结果存储到局部变量，避免重复采样
- **单通道多效果** - 在一个 shader 中合并多个效果，而不是使用多个材质

\`\`\`gdshader
// 错误：多次采样
void fragment() {
    float r = texture(TEXTURE, UV).r;
    float g = texture(TEXTURE, UV + vec2(0.1, 0.0)).g;
    float b = texture(TEXTURE, UV + vec2(0.2, 0.0)).b;
}

// 正确：单次采样
void fragment() {
    vec4 tex = texture(TEXTURE, UV);
    float r = tex.r;
    float g = tex.g;
    float b = tex.b;
}
\`\`\`

### 2. 避免分支语句

GPU 并行处理的特性使其对分支语句不友好。

\`\`\`gdshader
// 避免
if (condition) {
    color = a;
} else {
    color = b;
}

// 推荐：使用 step 或 mix
color = mix(a, b, step(threshold, value));
\`\`\`

### 3. 合理使用精度

\`\`\`gdshader
// 高精度用于位置计算
highp vec3 world_position;

// 低精度用于颜色和临时计算
lowp float temp;

// 中等精度（默认）
mediump vec2 uv;
\`\`\`

### 4. MultiMesh 实例优化

对于需要渲染大量相同物体的场景（如草地、粒子），使用 MultiMesh：

\`\`\`gdscript
# GDScript
var multimesh = MultiMesh.new()
multimesh.mesh = preload("res://grass.mesh")
multimesh.instance_count = 1000

# 设置每个实例的变换
for i in range(1000):
    var transform = Transform3D()
    transform.origin = Vector3(randf() * 100, 0, randf() * 100)
    multimesh.set_instance_transform(i, transform)
\`\`\`

### 5. 其他优化建议

| 优化项 | 说明 |
|--------|------|
| 减少 uniform 数量 | uniform 变量在 CPU 和 GPU 间传输有开销 |
| 使用 Constants | 编译时常量比 uniform 更快 |
| 避免动态分支 | 使用 \`step()\`、\`smoothstep()\` 等数学函数替代 \`if\` |
| 纹理格式 | 使用合适的纹理压缩格式（ETC2、ASTC） |
| 减少 overdraw | 合理设置渲染队列，避免不必要的透明叠加 |

---

## Shader 调试方法

### 1. 简单调试技巧

\`\`\`gdshader
// 临时输出中间值到屏幕
void fragment() {
    vec4 color = texture(TEXTURE, UV);
    float debug_value = some_calculation;
    COLOR = vec4(debug_value, 0.0, 0.0, 1.0);  // 红色通道显示调试值
}
\`\`\`

### 2. 使用 Monitor 调试

在 Godot 编辑器中使用 **Debugger > Monitor** 面板：
- 观察 \`fps\`、\`draw_calls\` 指标
- 检查材质数量和渲染状态

### 3. 逐步注释法

将 shader 代码分段落注释，逐步取消注释定位问题：

\`\`\`gdshader
void fragment() {
    vec4 color = texture(TEXTURE, UV);
    // COLOR = color; return;  // 逐步取消注释调试

    color.r *= 1.2;
    // COLOR = color; return;

    color.g *= 0.8;
    // COLOR = color; return;

    COLOR = color;
}
\`\`\`

### 4. 外部工具

- **RenderDoc** - GPU 帧分析工具
- **NSight Graphics** - NVIDIA 显卡调试工具

---

## 从代码版到 VisualShader 的转换

Godot 提供 VisualShader 编辑器，适合不熟悉代码的开发者。

### 转换方法

1. **手动转换** - 根据代码逻辑在 VisualShader 编辑器中连接对应节点

2. **VisualShader 节点对应关系**：

| 代码 Shader | VisualShader 节点 |
|------------|------------------|
| \`texture(TEXTURE, UV)\` | Texture |
| \`TIME\` | Time |
| \`sin()\` / \`cos()\` | ScalarOp (选择对应运算) |
| \`mix(a, b, c)\` | Mix |
| \`step(a, b)\` | ScalarOp > Step |
| \`smoothstep(a, b, c)\` | ScalarOp > Smoothstep |

### VisualShader 优势

- 可视化节点编辑，所见即所得
- 内置常用节点库
- 易于分享和复用

### VisualShader 局限

- 复杂算法实现繁琐
- 代码更灵活、性能更好

---

## 常见应用场景

| 场景 | 推荐技术 |
|------|----------|
| 2D 精灵特效 | Canvas Item Shader |
| UI 效果 | Canvas Item Shader |
| 地形生成 | FBM + Perlin Noise |
| 水面效果 | Sine Wave + Noise |
| 描边/轮廓 | Normal Expansion 或 SDF |
| 溶解效果 | Noise + Alpha Cutoff |
| 雾效 | Depth Buffer + Noise |

---

## 参考资源

- [Godot Shaders 社区库](https://godotshaders.com/)
- [Godot 官方文档 - Shading Language](https://docs.godotengine.org/)
- [ShaderToy - 学习 Shader 的优秀资源](https://www.shadertoy.com/)

---

## 相关教程

- [着色器简介](./intro) - Shader 基础概念与语法
- [着色器常用数学](./shader-math) - Shader 编程中的数学基础
- [着色器常见案例](./shader-common-cases) - 描边、像素化、灰度、溶解等实战案例
`,b=`---
title: 着色器常用数学
author: 陌上竹
date: 2026-03-23
category:
  - 教程
tag:
  - 着色器
  - 数学
---

本文整理了 Godot 4.x 官方文档中的 Shading Language 参考，是游戏开发中 Shader 编程的数学基础汇总。

<!-- more -->

## 1. 基础数学函数

### 1.1 绝对值与取整

#### abs() - 绝对值

返回参数的绝对值。

\`\`\`gdshader
float abs(float x)
vec2 abs(vec2 x)
vec3 abs(vec3 x)
vec4 abs(vec4 x)
\`\`\`

**示例**：
\`\`\`gdshader
void fragment() {
    float value = abs(-5.0);           // 结果: 5.0
    vec2 dir = abs(vec2(-1.0, 2.0));    // 结果: vec2(1.0, 2.0)
}
\`\`\`

**应用场景**：计算距离、确保值为正、镜像效果。

---

#### floor() - 向下取整

返回小于或等于参数的最大整数。

\`\`\`gdshader
float floor(float x)
vec2 floor(vec2 x)
\`\`\`

**示例**：
\`\`\`gdshader
void fragment() {
    float f = floor(3.7);   // 结果: 3.0
    float g = floor(-2.3);  // 结果: -3.0
}
\`\`\`

**应用场景**：创建像素化效果、网格对齐、步进动画。

---

#### ceil() - 向上取整

返回大于或等于参数的最小整数。

\`\`\`gdshader
float ceil(float x)
vec2 ceil(vec2 x)
\`\`\`

**示例**：
\`\`\`gdshader
void fragment() {
    float c = ceil(2.1);   // 结果: 3.0
    float d = ceil(-4.8);  // 结果: -4.0
}
\`\`\`

---

#### round() - 四舍五入

返回最接近参数的整数。

\`\`\`gdshader
float round(float x)
\`\`\`

**示例**：
\`\`\`gdshader
void fragment() {
    float r = round(2.5);  // 结果: 2.0 (Banker's rounding)
    float s = round(2.4);  // 结果: 2.0
}
\`\`\`

> 注意：Godot 使用 Banker's rounding（银行家舍入），即对 .5 的情况舍入到偶数。

---

#### fract() - 取小数部分

返回参数的小数部分。

\`\`\`gdshader
float fract(float x)
\`\`\`

**公式**：\`fract(x) = x - floor(x)\`

**示例**：
\`\`\`gdshader
void fragment() {
    float f = fract(3.14159);  // 结果: 0.14159
    float g = fract(-1.5);     // 结果: 0.5
}
\`\`\`

**应用场景**：创建重复图案、UV动画、环形渐变。

---

### 1.2 极值与限制

#### min() - 最小值

返回两个值中的较小者。

\`\`\`gdshader
float min(float x, float y)
vec2 min(vec2 x, vec2 y)
\`\`\`

**示例**：
\`\`\`gdshader
void fragment() {
    float m = min(3.0, 5.0);  // 结果: 3.0
    vec2 v = min(vec2(1.0, 3.0), vec2(2.0, 0.0)); // 结果: vec2(1.0, 0.0)
}
\`\`\`

---

#### max() - 最大值

返回两个值中的较大者。

\`\`\`gdshader
float max(float x, float y)
\`\`\`

**示例**：
\`\`\`gdshader
void fragment() {
    float m = max(3.0, 5.0);  // 结果: 5.0
}
\`\`\`

---

#### clamp() - 限制范围

将值限制在指定范围内。

\`\`\`gdshader
float clamp(float x, float min_val, float max_val)
\`\`\`

**公式**：\`clamp(x, min, max) = min(max(x, min_val), max_val)\`

**示例**：
\`\`\`gdshader
void fragment() {
    float c = clamp(0.8, 0.0, 1.0);   // 结果: 0.8
    float d = clamp(-0.5, 0.0, 1.0);  // 结果: 0.0
    float e = clamp(2.0, 0.0, 1.0);   // 结果: 1.0
}
\`\`\`

**应用场景**：颜色插值、强度限制、动画平滑。

---

#### step() - 阶梯函数

如果 \`x >= edge\` 返回 1.0，否则返回 0.0。

\`\`\`gdshader
float step(float edge, float x)
\`\`\`

**示例**：
\`\`\`gdshader
void fragment() {
    float s = step(0.5, 0.8);  // 结果: 1.0
    float t = step(0.5, 0.3);  // 结果: 0.0
}
\`\`\`

**应用场景**：创建硬边缘过渡、阈值效果、卡通渲染。

---

### 1.3 插值函数

#### mix() - 线性插值

在两个值之间进行线性插值。

\`\`\`gdshader
float mix(float x, float y, float alpha)
vec2 mix(vec2 x, vec2 y, float alpha)
\`\`\`

**公式**：\`mix(x, y, a) = x * (1 - a) + y * a\`

**参数说明**：
- \`x\`: 起始值
- \`y\`: 结束值
- \`alpha\`: 插值因子，0.0 返回 x，1.0 返回 y

**示例**：
\`\`\`gdshader
void fragment() {
    vec3 color_start = vec3(1.0, 0.0, 0.0);  // 红色
    vec3 color_end = vec3(0.0, 0.0, 1.0);    // 蓝色
    vec3 blended = mix(color_start, color_end, 0.5); // 紫色
}
\`\`\`

**应用场景**：颜色渐变、位置插值、动画过渡。

---

#### lerp() - 线性插值

与 \`mix()\` 功能相同，是 GLSL ES 3.0 的别名。

\`\`\`gdshader
float lerp(float x, float y, float alpha)
\`\`\`

---

#### smoothstep() - 平滑插值

使用 Hermite 插值实现平滑过渡。

\`\`\`gdshader
float smoothstep(float edge0, float edge1, float x)
\`\`\`

**特性**：
- 当 \`x <= edge0\` 时返回 0.0
- 当 \`x >= edge1\` 时返回 1.0
- 在 \`edge0\` 和 \`edge1\` 之间进行平滑插值

**公式**：
\`\`\`
t = clamp((x - edge0) / (edge1 - edge0), 0.0, 1.0)
result = t * t * (3.0 - 2.0 * t)
\`\`\`

**示例**：
\`\`\`gdshader
void fragment() {
    float s = smoothstep(0.0, 1.0, 0.3);  // 结果: ~0.216
    float t = smoothstep(0.0, 1.0, 0.7);  // 结果: ~0.784
}
\`\`\`

**应用场景**：抗锯齿边缘、柔和渐变、卡通描边。

---

## 2. 三角函数

### 2.1 基础三角函数

#### sin() - 正弦

返回角度的 sine 值。

\`\`\`gdshader
float sin(float angle)
\`\`\`

**参数**：角度（弧度）

**示例**：
\`\`\`gdshader
void fragment() {
    float s = sin(3.14159 / 2.0);  // 结果: ~1.0
}
\`\`\`

---

#### cos() - 余弦

返回角度的 cosine 值。

\`\`\`gdshader
float cos(float angle)
\`\`\`

**示例**：
\`\`\`gdshader
void fragment() {
    float c = cos(0.0);  // 结果: 1.0
}
\`\`\`

---

#### tan() - 正切

返回角度的 tangent 值。

\`\`\`gdshader
float tan(float angle)
\`\`\`

---

### 2.2 反三角函数

#### asin() - 反正弦

返回 sine 值为 x 的角度。

\`\`\`gdshader
float asin(float x)  // 返回范围: [-PI/2, PI/2]
\`\`\`

---

#### acos() - 反余弦

返回 cosine 值为 x 的角度。

\`\`\`gdshader
float acos(float x)  // 返回范围: [0, PI]
\`\`\`

---

#### atan() - 反正切

返回 tangent 值为 y/x 的角度。

\`\`\`gdshader
float atan(float y, float x)  // 返回范围: [-PI, PI]
\`\`\`

**示例**：
\`\`\`gdshader
void fragment() {
    float angle = atan(1.0, 1.0);  // 结果: PI/4 (45度)
}
\`\`\`

---

### 2.3 实用技巧

#### 创建圆形

\`\`\`gdshader
shader_type canvas_item;

void fragment() {
    vec2 center = vec2(0.5, 0.5);
    float radius = 0.4;

    // 计算到中心的距离
    float dist = length(UV - center);

    // 使用 step 创建圆形边缘
    float circle = step(radius, dist);

    COLOR = vec4(vec3(circle), 1.0);
}
\`\`\`

#### 创建环形

\`\`\`gdshader
void fragment() {
    vec2 center = vec2(0.5, 0.5);
    float inner_radius = 0.2;
    float outer_radius = 0.4;

    float dist = length(UV - center);
    float ring = step(inner_radius, dist) * step(dist, outer_radius);

    COLOR = vec4(vec3(ring), 1.0);
}
\`\`\`

#### 波浪效果

\`\`\`gdshader
void fragment() {
    float wave = sin(UV.y * 10.0 + TIME * 2.0) * 0.1 + 0.5;
    COLOR = vec4(vec3(wave), 1.0);
}
\`\`\`

---

## 3. 向量运算

### 3.1 点乘 (Dot Product)

#### dot()

计算两个向量的点积。

\`\`\`gdshader
float dot(vec2 x, vec2 y)
float dot(vec3 x, vec3 y)
float dot(vec4 x, vec4 y)
\`\`\`

**公式**：
- 2D: \`dot(a, b) = a.x * b.x + a.y * b.y\`
- 3D: \`dot(a, b) = a.x * b.x + a.y * b.y + a.z * b.z\`

**几何意义**：
- \`dot(v1, v2) = |v1| * |v2| * cos(theta)\`
- 当返回值为 0 时，两向量垂直
- 当返回值 > 0 时，两向量夹角小于 90 度
- 当返回值 < 0 时，两向量夹角大于 90 度

**示例**：
\`\`\`gdshader
void fragment() {
    vec2 a = vec2(1.0, 0.0);
    vec2 b = vec2(0.707, 0.707);
    float d = dot(a, b);  // 结果: ~0.707
}
\`\`\`

**应用场景**：
- 判断方向（正面/背面）
- 光照计算（漫反射）
- 向量投影

---

### 3.2 叉乘 (Cross Product)

#### cross()

计算两个向量的叉积（仅 3D 向量）。

\`\`\`gdshader
vec3 cross(vec3 x, vec3 y)
\`\`\`

**公式**：
\`\`\`
cross(a, b) = vec3(
    a.y * b.z - a.z * b.y,
    a.z * b.x - a.x * b.z,
    a.x * b.y - a.y * b.x
)
\`\`\`

**几何意义**：
- 返回一个与两向量垂直的新向量
- 方向遵循右手定则
- 大小：\`|cross| = |a| * |b| * sin(theta)\`

**示例**：
\`\`\`gdshader
void fragment() {
    vec3 right = vec3(1.0, 0.0, 0.0);
    vec3 forward = vec3(0.0, 0.0, 1.0);
    vec3 up = cross(right, forward);  // 结果: vec3(0.0, 1.0, 0.0)
}
\`\`\`

**应用场景**：
- 计算法线
- 确定方向转向
- 3D 空间中的垂直向量

---

### 3.3 长度与归一化

#### length()

计算向量的长度（模）。

\`\`\`gdshader
float length(vec2 v)
float length(vec3 v)
float length(vec4 v)
\`\`\`

**公式**：\`length(v) = sqrt(v.x * v.x + v.y * v.y + ...)\`

**示例**：
\`\`\`gdshader
void fragment() {
    float len = length(vec2(3.0, 4.0));  // 结果: 5.0
}
\`\`\`

---

#### normalize()

返回方向相同但长度为 1 的单位向量。

\`\`\`gdshader
vec2 normalize(vec2 v)
vec3 normalize(vec3 v)
\`\`\`

**公式**：\`normalize(v) = v / length(v)\`

**示例**：
\`\`\`gdshader
void fragment() {
    vec2 dir = normalize(vec2(3.0, 4.0));  // 结果: vec2(0.6, 0.8)
}
\`\`\`

> 注意：不要对零向量使用 normalize，会导致除零错误。

**安全版本**：
\`\`\`gdshader
vec2 safe_normalize(vec2 v) {
    float len = length(v);
    return len > 0.0 ? v / len : vec2(0.0);
}
\`\`\`

---

### 3.4 距离

#### distance()

计算两点之间的距离。

\`\`\`gdshader
float distance(vec2 a, vec2 b)
float distance(vec3 a, vec3 b)
\`\`\`

**公式**：\`distance(a, b) = length(a - b)\`

**示例**：
\`\`\`gdshader
void fragment() {
    float dist = distance(vec2(0.0, 0.0), vec2(3.0, 4.0));  // 结果: 5.0
}
\`\`\`

---

### 3.5 其他向量函数

#### reflect() - 反射

\`\`\`gdshader
vec2 reflect(vec2 I, vec2 N)  // I: 入射方向, N: 法线
vec3 reflect(vec3 I, vec3 N)
\`\`\`

**公式**：\`reflect(I, N) = I - 2.0 * dot(N, I) * N\`

**示例**：
\`\`\`gdshader
void fragment() {
    vec3 I = vec3(1.0, -1.0, 0.0);   // 入射方向
    vec3 N = vec3(0.0, 1.0, 0.0);    // 法线
    vec3 R = reflect(I, N);          // 反射方向
}
\`\`\`

---

#### refract() - 折射

\`\`\`gdshader
vec3 refract(vec3 I, vec3 N, float eta)
\`\`\`

**参数**：\`eta = n1 / n2\`（折射率比值）

---

## 4. 矩阵运算基础

### 4.1 矩阵类型

Godot Shader 中的矩阵类型：

\`\`\`gdshader
mat2  // 2x2 矩阵
mat3  // 3x3 矩阵
mat4  // 4x4 矩阵
\`\`\`

### 4.2 矩阵乘法

\`\`\`gdshader
mat2 result = mat2 * mat2
vec2 result = mat2 * vec2
\`\`\`

### 4.3 常用矩阵

#### 单位矩阵

\`\`\`gdshader
mat2 identity = mat2(1.0, 0.0, 0.0, 1.0);
mat3 identity = mat3(1.0);
mat4 identity = mat4(1.0);
\`\`\`

#### 旋转矩阵

\`\`\`gdshader
// 2D 旋转
mat2 rotation(float angle) {
    float s = sin(angle);
    float c = cos(angle);
    return mat2(c, -s, s, c);
}
\`\`\`

#### 缩放矩阵

\`\`\`gdshader
// 2D 缩放
mat2 scale(vec2 s) {
    return mat2(s.x, 0.0, 0.0, s.y);
}
\`\`\`

### 4.4 变换函数

#### transform() - 向量变换

\`\`\`gdshader
vec3 transformed = transform(mat4, vec3)
\`\`\`

---

## 5. 噪声函数

### 5.1 随机数

#### 1D 随机

\`\`\`gdshader
float random(float x) {
    return fract(sin(x) * 43758.5453);
}

void fragment() {
    float r = random(1.0);  // 0.0~1.0 之间的随机值
}
\`\`\`

#### 2D 随机

\`\`\`gdshader
float random(vec2 st) {
    return fract(sin(dot(st.xy, vec2(12.9898, 78.233))) * 43758.5453);
}
\`\`\`

### 5.2 值噪声 (Value Noise)

\`\`\`gdshader
float noise(vec2 st) {
    vec2 i = floor(st);
    vec2 f = fract(st);

    // 四个角的随机值
    float a = random(i);
    float b = random(i + vec2(1.0, 0.0));
    float c = random(i + vec2(0.0, 1.0));
    float d = random(i + vec2(1.0, 1.0));

    // 平滑插值
    vec2 u = f * f * (3.0 - 2.0 * f);

    return mix(a, b, u.x) + (c - a) * u.y * (1.0 - u.x) + (d - b) * u.x * u.y;
}

void fragment() {
    float n = noise(UV * 10.0);
    COLOR = vec4(vec3(n), 1.0);
}
\`\`\`

### 5.3 梯度噪声 (Gradient Noise)

\`\`\`gdshader
vec2 hash(vec2 p) {
    p = vec2(dot(p, vec2(127.1, 311.7)), dot(p, vec2(269.5, 183.3)));
    return -1.0 + 2.0 * fract(sin(p) * 43758.5453123);
}

float gradient_noise(vec2 p) {
    vec2 i = floor(p);
    vec2 f = fract(p);

    vec2 u = f * f * (3.0 - 2.0 * f);

    return mix(mix(dot(hash(i + vec2(0.0, 0.0)), f - vec2(0.0, 0.0)),
                   dot(hash(i + vec2(1.0, 0.0)), f - vec2(1.0, 0.0)), u.x),
               mix(dot(hash(i + vec2(0.0, 1.0)), f - vec2(0.0, 1.0)),
                   dot(hash(i + vec2(1.0, 1.0)), f - vec2(1.0, 1.0)), u.x), u.y);
}
\`\`\`

### 5.4 FBM (分形布朗运动)

\`\`\`gdshader
float fbm(vec2 st) {
    float value = 0.0;
    float amplitude = 0.5;
    float frequency = 1.0;

    for (int i = 0; i < 6; i++) {
        value += amplitude * noise(st * frequency);
        frequency *= 2.0;
        amplitude *= 0.5;
    }

    return value;
}

void fragment() {
    float pattern = fbm(UV * 4.0);
    COLOR = vec4(vec3(pattern), 1.0);
}
\`\`\`

**参数说明**：
- \`octaves\`: 叠加的噪声层数（通常 4-6）
- \`amplitude\`: 振幅，每层递减
- \`frequency\`: 频率，每层倍增

**应用场景**：
- 地形生成
- 云朵效果
- 纹理混合
- 艺术化渲染

---

## 6. 空间变换

### 6.1 坐标系统

#### 世界坐标 (World Space)

物体在游戏世界中的绝对位置。

#### 局部坐标 (Local Space)

相对于父节点的相对位置。

#### UV 坐标 (UV Space)

2D 纹理上的像素位置，范围通常为 [0, 1]。

\`\`\`gdshader
void fragment() {
    // UV 从左下角 (0,0) 到右上角 (1,1)
    vec2 uv = UV;

    // 居中后的 UV，范围 [-0.5, 0.5]
    vec2 centered = UV - 0.5;
}
\`\`\`

### 6.2 坐标变换函数

#### 缩放 UV

\`\`\`gdshader
void fragment() {
    vec2 scaled_uv = UV * 2.0;  // 放大 2 倍
}
\`\`\`

#### 偏移 UV

\`\`\`gdshader
void fragment() {
    vec2 offset_uv = UV + vec2(0.1, 0.2);  // 偏移
}
\`\`\`

#### 旋转 UV

\`\`\`gdshader
void fragment() {
    float angle = TIME;
    mat2 rotation = mat2(cos(angle), -sin(angle), sin(angle), cos(angle));
    vec2 rotated_uv = rotation * (UV - 0.5) + 0.5;
}
\`\`\`

### 6.3 3D 空间变换

\`\`\`gdshader
// 在顶点着色器中使用
void vertex() {
    // 模型到世界
    vec4 world_pos = MODEL_MATRIX * vec4(VERTEX, 1.0);

    // 世界到视图
    vec4 view_pos = VIEW_MATRIX * world_pos;

    // 投影
    vec4 clip_pos = PROJECTION_MATRIX * view_pos;
}
\`\`\`

---

## 7. 实用数学技巧

### 7.1 使用 sin/cos 创建圆形

\`\`\`gdshader
shader_type canvas_item;

void fragment() {
    vec2 center = vec2(0.5, 0.5);
    vec2 pos = UV - center;

    // 使用 atan 和 sin/cos 创建圆
    float angle = atan(pos.y, pos.x);
    float radius = 0.3 + sin(angle * 6.0) * 0.05;

    float circle = 1.0 - smoothstep(radius, radius + 0.01, length(pos));

    COLOR = vec4(vec3(circle), 1.0);
}
\`\`\`

### 7.2 使用 mod 创建重复图案

#### 网格图案

\`\`\`gdshader
void fragment() {
    vec2 tiled = fract(UV * 10.0);
    float grid = step(0.95, tiled.x) + step(0.95, tiled.y);
    COLOR = vec4(vec3(grid), 1.0);
}
\`\`\`

#### 砖墙图案

\`\`\`gdshader
void fragment() {
    vec2 brick = UV * vec2(6.0, 12.0);
    brick.x += step(1.0, mod(brick.y, 2.0)) * 0.5;
    brick = fract(brick);

    float pattern = step(0.05, brick.x) * step(0.05, brick.y);
    COLOR = vec4(vec3(pattern), 1.0);
}
\`\`\`

### 7.3 极坐标变换

\`\`\`gdshader
void fragment() {
    vec2 center = vec2(0.5, 0.5);
    vec2 delta = UV - center;

    // 转换为极坐标
    float radius = length(delta);
    float angle = atan(delta.y, delta.x);

    // 归一化角度到 [0, 1]
    angle = (angle + 3.14159) / (2.0 * 3.14159);

    // 使用极坐标创建效果
    float pattern = sin(angle * 20.0) * sin(radius * 30.0);

    COLOR = vec4(vec3(pattern * 0.5 + 0.5), 1.0);
}
\`\`\`

### 7.4 平滑边缘

\`\`\`gdshader
void fragment() {
    vec2 center = vec2(0.5, 0.5);
    float radius = 0.4;

    float dist = length(UV - center);

    // 抗锯齿边缘
    float alpha = 1.0 - smoothstep(radius - 0.01, radius + 0.01, dist);

    COLOR = vec4(1.0, 0.5, 0.0, alpha);
}
\`\`\`

### 7.5 条纹动画

\`\`\`gdshader
void fragment() {
    float stripe = sin(UV.y * 20.0 + TIME * 5.0);
    stripe = stripe * 0.5 + 0.5;  // 归一化到 [0, 1]

    COLOR = vec4(vec3(stripe), 1.0);
}
\`\`\`

### 7.6 脉冲效果

\`\`\`gdshader
void fragment() {
    float pulse = sin(TIME * 3.0) * 0.5 + 0.5;

    vec2 center = vec2(0.5, 0.5);
    float dist = length(UV - center);

    // 脉冲扩散效果
    float wave = sin(dist * 20.0 - TIME * 5.0) * 0.5 + 0.5;
    float alpha = smoothstep(pulse, pulse - 0.1, wave);

    COLOR = vec4(vec3(alpha), 1.0);
}
\`\`\`

### 7.7 颜色通道分离

\`\`\`gdshader
void fragment() {
    float offset = sin(TIME * 2.0) * 0.02;

    float r = texture(TEXTURE, UV + vec2(offset, 0.0)).r;
    float g = texture(TEXTURE, UV).g;
    float b = texture(TEXTURE, UV - vec2(offset, 0.0)).b;

    COLOR = vec4(r, g, b, 1.0);
}
\`\`\`

---

## 附录：常用常量

\`\`\`gdshader
const float PI = 3.14159265359;        // 圆周率
const float TAU = 6.28318530718;       // 2 * PI
const float DEG_TO_RAD = PI / 180.0;   // 度转弧度
const float RAD_TO_DEG = 180.0 / PI;   // 弧度转度
const float SQRT2 = 1.41421356237;     // 平方根 2
const float INF = 1e10;                // 无穷大
\`\`\`

---

## 相关教程

- [着色器简介](./intro) - Shader 基础概念与语法
- [着色器实现思路](./shader-implementation) - UV 操作、颜色处理、噪声应用等
- [着色器常见案例](./shader-common-cases) - 描边、像素化、灰度、溶解等实战案例
`,x=`---\r
title: 信号系统\r
author: Moshangzhu\r
date: 2025-10-17\r
category:\r
  - 教程\r
tag:\r
  - 信号\r
---\r
\r
## 简介\r
在开发游戏的过程中，往往会出现这样的情况：\r
- 玩家受到伤害，需要让血量条发生变化\r
- 动画播放结束了，需要用代码控制播放下一个动画\r
- 要设置机关，当玩家触碰后激活某处机关\r
\r
我们可能会有以下思路：\r
- 先获得到需要触发变化的节点，然后调用节点上的函数，例如第一种情况在玩家脚本中：\`get_node("hp_bar").update_value(hp_value)\`\r
- 先通知父级节点，然后再想办法通知到对应的节点处理，例如第一种情况在玩家脚本中：\`get_parent().do_something(hp_value)\`\r
- 先把自己存放再需要变化的节点的属性里，然后每帧根据玩家的属性，例如第一种情况在玩家脚本中：\`hp_bar.player = self\`，然后在血量的process函数中\`update_value(player.hp_value)\`\r
\r
很容易发现以上代码中，传输数据是一件很不方便的事情。尤其是我们不可能将所有代码全部放在一个庞大的\`game.tscn\`场景文件中，这使得节点之间的交互更是一件复杂的事情。\r
\r
那有什么办法可以简化这种操作呢？或者说，如何将一个事件，和处理这个事件的函数解绑呢？\r
\r
答案是**信号**\`signal\`。\r
\r
<!-- more -->\r
\r
!!! note 信号机制\r
在别的语境里，信号通常是事件监听、事件委托、观察者模式、订阅者模式等名词。\r
在代码中可以声明一个事件，然后将需要处理事件的行为维护成一个数组。当发生这个事件时，就会遍历调用这些行为函数。（观察者模式）\r
或者在一个第三方的位置，设置一个事件。事件可以由事件发布者发布，再由中介者发布给所有的订阅者。（订阅者模式）\r
而这些，在Godot中就被封装成了信号系统。\r
!!!\r
## 在Godot中使用信号\r
### Godot中的Signal类\r
Godot中的信号是一种常量。类型是Signal。我们并不需要关心他的值是什么。只需要了解信号的几个重要的函数。\r
\r
可以用以下方式方便的定义信号。\r
\`\`\`gdscript\r
# 定义一个叫some_sig的信号\r
signal some_sig\r
# 定义一个带有两个参数的信号\r
signal other_sig(param_a: int, param_b: String)\r
\`\`\`\r
\r
绑定信号的方法也很简单，只需要让信号链接到一个Callable类型的变量上：\r
\`\`\`gdscript\r
# 将some_sig信号链接到一个名为_handle_func的函数上。\r
some_sig.connect(_handle_func)\r
\r
# 将other_sig信号链接到一个名为_handle_func_with_params的有参数的函数上。\r
# 因为信号和函数的参数一致，所以无需特殊处理\r
other_sig.connect(_handle_func_with_params)\r
\r
# 将some_sig信号链接到_handle_func_with_params并传入参数。\r
# 由于some_sig无法提供_handle_func_with_params需要的两个参数，\r
# 所以使用bind为_handle_func_with_params绑定两个固定的参数生成一个新的Callable。\r
some_sig.connect(_handle_func_with_params.bind(1, "来自some_sig的固定参数"))\r
\r
func _handle_func():\r
    print("触发无参数函数")\r
\r
func _handle_func_with_params(param_a: int, param_b: String):\r
    print("触发有参数函数: %d %s" % [param_a, param_b])\r
\`\`\`\r
\r
\`\`\` ::close Callable与bind\r
**Callable**\r
\`Callable\`是一种变量类型，指的是可以被调用的函数。使用\`func\`关键字声明的具名函数和匿名函数都是\`Callable\`类型的变量。\r
例如上文中的：\r
\`\`\`gdscript\r
func _handle_func_with_params(param_a: int, param_b: String):\r
    print("触发有参数函数: %d %s" % [param_a, param_b])\r
\`\`\`\r
\`_handle_func_with_params\`就是一个\`Callable\`类型。\r
\r
信号可以与\`Callable\`类型链接。所以在\`Signal.connect\`函数中传入类似\`_handle_func_with_params\`的函数名即可。\r
\r
**bind**\r
bind可以返回一个函数的副本，并为其绑定一个或多个固定的参数。\r
例如上文中的：\r
\`\`\`gdscript\r
_handle_func_with_params.bind(1, "来自some_sig的固定参数")\r
\`\`\`\r
是调用了\`_handle_func_with_params\`这个\`Callable\`变量上的\`bind\`方法。返回了一个新的\`Callable\`类型的变量。**这个变量和原来的函数不是一个值。**\r
返回的副本被\`bind\`函数绑定了两个固定的值，所以再使用无参数的信号连接时，就不会出现问题。\r
\`\`\`\r
\r
触发信号可以使用信号的emit函数。如果信号带有参数，也可以在emit函数中传参。\r
\r
\`\`\`gdscript\r
# 触发无参数信号\r
some_sig.emit()\r
# 触发有参数信号并传参\r
other_sig.emit(2, "触发信号")\r
\`\`\`\r
有了以上的内容，我们就可以声明并使用信号了。\r
\r
在编辑器中，则可以选择一个带有信号的节点，在界面右侧检查旁找到节点面板，选择信号面板，双击想要连接的信号，然后在弹出的连接信号面板中选择一个处理信号的脚本，并选取或新建一个处理方法。点击连接按钮后即可将信号连接到方法。\r
![](/images/tutorial/signal/signal_connect_in_editor.png)\r
\r
### 变体写法\r
\r
除了上述的信号链接方法，Godot还提供了另一种写法。我们可以通过节点的\`connect\`函数来链接信号：\r
\r
\`\`\`gdscript\r
# 使用字符串形式的信号名称来链接信号\r
self.connect("some_sig", _handle_func)\r
\`\`\`\r
\r
这种写法和\`some_sig.connect(_handle_func)\`的效果是一样的。区别在于：\r
- 前者使用字符串\`"some_sig"\`来指定信号名称\r
- 后者直接使用信号常量\`some_sig\`本身\r
\r
使用字符串形式的好处是可以动态地指定信号名称，但缺点是容易因为拼写错误导致问题，且IDE无法提供代码补全和类型检查。因此在大多数情况下，推荐使用直接访问信号常量的方式。\r
\r
### 信号标志\r
\r
在链接信号时，我们可以传入一个可选的标志参数，来控制信号的特殊行为。这些标志定义在\`Object.ConnectFlags\`枚举中。\r
\r
使用方式如下：\r
\`\`\`gdscript\r
# 连接信号时传入标志参数\r
some_sig.connect(_handle_func, CONNECT_ONE_SHOT)\r
\`\`\`\r
\r
Godot提供了以下几种信号标志：\r
\r
#### 延迟触发 CONNECT_DEFERRED\r
\r
\`\`\`gdscript\r
some_sig.connect(_handle_func, CONNECT_DEFERRED)\r
\`\`\`\r
\r
使用延迟标志后，信号触发时不会立即调用函数，而是等到当前帧的末尾（空闲时）才触发。这在某些需要等待所有逻辑执行完毕后再处理的场景中很有用。\r
\r
#### 一次性连接 CONNECT_ONE_SHOT\r
\r
\`\`\`gdscript\r
some_sig.connect(_handle_func, CONNECT_ONE_SHOT)\r
\`\`\`\r
\r
一次性连接会在第一次触发后自动断开，不需要手动调用\`disconnect\`。适合只需要响应一次的事件，例如游戏开始时的初始化信号。\r
\r
#### 持久连接 CONNECT_PERSIST\r
\r
\`\`\`gdscript\r
some_sig.connect(_handle_func, CONNECT_PERSIST)\r
\`\`\`\r
\r
持久连接会在场景保存时一起被序列化存储。在编辑器中通过"节点"面板手动创建的信号连接默认就是持久的。这意味着场景加载时，这些连接会自动恢复。\r
\r
#### 引用计数 CONNECT_REFERENCE_COUNTED\r
\r
\`\`\`gdscript\r
some_sig.connect(_handle_func, CONNECT_REFERENCE_COUNTED)\r
\`\`\`\r
\r
允许同一个信号和函数多次建立连接，每次连接都会增加内部计数器。每当触发一次就会减少一次计数，当全部计数都触发后，即完全断开连接。需要调用相同次数的\`disconnect\`才能完全断开连接。\r
\r
#### 追加源对象 CONNECT_APPEND_SOURCE_OBJECT\r
\r
\`\`\`gdscript\r
some_sig.connect(_handle_func, CONNECT_APPEND_SOURCE_OBJECT)\r
\`\`\`\r
\r
实例化场景时，会自动将发出信号的对象追加到回调函数的参数末尾。这在需要知道信号来源的场景中很有用。\r
\r
部分信号标志可以在Godot编辑器的信号连接窗口中选择（需要勾选高级按钮）：\r
![](/images/tutorial/signal/signal_flags_in_editor.png)\r
\r
!!! tip 组合使用标志\r
多个标志可以使用按位或运算符\`|\`组合使用：\r
\`\`\`gdscript\r
some_sig.connect(_handle_func, CONNECT_DEFERRED | CONNECT_ONE_SHOT)\r
\`\`\`\r
!!!\r
\r
### 断开信号\r
\r
当不再需要响应某个信号时，可以使用\`disconnect\`函数断开连接：\r
\r
\`\`\`gdscript\r
# 断开信号连接\r
some_sig.disconnect(_handle_func)\r
\`\`\`\r
\r
断开信号连接在节点被删除或功能被禁用时很有必要，可以避免触发已经失效的函数。\r
\r
## 全局事件总线\r
如果你已经过一段游戏开发经验了，会发现在使用信号时总会有些麻烦：在使用某个节点的信号时需要先想办法获取到这个节点，然后才能通过节点连接信号处理事件。可不可以用一种方法能更方便的使用信号呢？\r
\r
全局事件总线是一个依赖于自动加载的统一管理信号的架构方式。它作为一个完全独立，又可以在任何地方轻易的访问，且全局唯一（单例）的节点，可以作为一个事件转发的枢纽。\r
\r
我们可以在一个自动加载的脚本中，新建一些信号，然后事件触发方直接触发全局脚本上的信号，事件接收方则将处理函数连接在全局脚本的信号上。这样两个节点不需要知道对方是否存在以及所在的位置，只需要关心事件是否被触发以及触发后的逻辑。减少了项目上场景与场景之间的依赖，实现了解耦的目的。\r
\r
以下是一个全局事件总线的示例：\r
\`\`\`gdscript\r
# event_bus.gd\r
# 自动加载，名称为EventBus\r
extends Node\r
\r
signal player_hp_changed(hp: int)\r
\r
\r
# player.gd\r
# 玩家脚本\r
class_name Player\r
extends CharacterBody2D\r
\r
var max_hp: int = 100\r
var hp: int = 100\r
\r
func hurt(damage: int):\r
    # 玩家受伤，扣除血量\r
    hp = clamp(hp - damage, 0, max_hp)\r
    # 血量变化，发送信号\r
    EventBus.player_hp_changed.emit(hp)\r
\r
# game_manager.gd\r
# 游戏控制器\r
class_name GameManager\r
extends Node\r
\r
func _ready():\r
    # 连接玩家血量变化信号\r
    EventBus.player_hp_changed(_handle_player_hp_changed)\r
\r
func _handle_player_hp_changed(player_hp: int):\r
    # 判断玩家血量是否为0\r
    if player_hp == 0:\r
        game_over()\r
\r
func game_over():\r
    # 退出游戏\r
    get_tree().quit()\r
\r
# ui.gd\r
# 游戏UI\r
extends Control\r
# 进度条节点，用来展示玩家血量\r
@onready var player_hp_progress_bar: ProgressBar = %PlayerHpProgressBar\r
\r
var player_hp\r
\r
func _ready():\r
    # 连接玩家血量变化信号\r
    EventBus.player_hp_changed(_handle_player_hp_changed)\r
\r
# 血量变化后更新ui\r
func _handle_player_hp_changed(current_player_hp: int):\r
    player_hp = current_player_hp\r
    update_ui()\r
\r
# 更新ui\r
func update_ui():\r
    player_hp_progress_bar.value = float(player_hp) / 100.0\r
\`\`\`\r
在这个例子中：\r
- **EventBus** 作为全局事件总线，定义了\`player_hp_changed\`信号\r
- **Player** 在血量变化时通过\`EventBus.player_hp_changed.emit(hp)\`发送信号\r
- **GameManager** 监听该信号，当玩家血量为0时结束游戏\r
- **UI** 同样监听该信号，实时更新血量显示\r
\r
可以看到，Player不需要知道GameManager和UI的存在，GameManager和UI也不需要获取Player节点的引用，所有的通信都通过EventBus完成。这种方式使得各个模块之间的耦合度大大降低，代码更加清晰易维护。\r
\r
!!! tip 多个事件总线\r
在大型项目中，可以根据功能模块创建多个事件总线，而不是把所有信号都放在一个EventBus中。例如：\r
- **UIEventBus** - 管理UI相关的事件（按钮点击、面板切换等）\r
- **GameEventBus** - 管理游戏逻辑事件（关卡完成、任务触发等）\r
- **AudioEventBus** - 管理音频事件（播放音效、切换BGM等）\r
\r
这样可以让代码结构更加清晰，也方便团队协作时的分工管理。\r
!!!\r
\r
!!! warning 注意事项\r
使用全局事件总线时需要注意：\r
- 避免滥用。不是所有的信号都适合放在事件总线中，只有需要跨场景、跨模块通信的事件才建议使用\r
- 信号命名要清晰，避免命名冲突\r
- 记得在节点销毁时断开不再需要的信号连接，避免内存泄漏\r
!!!`,S=`---\r
title: 全局加载与单例模式\r
author: Moshangzhu\r
date: 2025-10-18\r
category:\r
  - 教程\r
tag:\r
  - 设计模式\r
---\r
\r
## 简介\r
想象一下这样的场景：你正在制作一款RPG游戏，玩家在村庄地图收集了100枚金币，然后进入了森林地图。此时你发现玩家的金币数据丢失了，因为场景切换后，原来的节点树被销毁了。\r
\r
或者你想制作一个背景音乐播放器，希望音乐能在不同场景间连续播放。但每次切换场景，音乐都会重新开始，因为\`AudioStreamPlayer\`节点跟随场景一起被重新创建了。\r
\r
再比如，你想在游戏的任何地方都能调用一个暂停功能，或者记录游戏设置。如果把这些功能分散在各个场景中，不仅代码会重复，还很难保证数据的一致性。\r
\r
这些问题的本质都是一样的：**如何创建一个在整个游戏生命周期中始终存在的节点？**\r
\r
在Godot中，这个问题有一个优雅的解决方案——**自动加载（AutoLoad）**。\r
\r
<!-- more -->\r
\r
!!! note 自动加载与单例模式\r
自动加载是Godot提供的一个特殊机制，可以让指定的脚本或场景在游戏启动时自动创建，并在整个游戏运行期间持续存在，不会随场景切换而销毁。\r
\r
这其实是**单例模式**的一种实现。单例模式是软件工程中的经典设计模式，用于确保某个类在整个程序中只有一个实例，并提供全局访问点。在其他编程语言中实现单例往往需要编写不少样板代码，而Godot将这个过程简化成了在项目设置中的简单配置。\r
\r
通过自动加载，我们可以轻松创建全局管理器、数据存储、事件总线等关键组件，让它们在任何场景、任何脚本中都能被访问。\r
!!!\r
\r
## 在Godot中使用自动加载\r
\r
### 创建自动加载\r
\r
在Godot中设置自动加载非常简单。自动加载可以是一个**脚本文件**（.gd）、**场景文件**（.tscn）。下面我们通过一个简单的例子来演示。\r
\r
首先，创建一个脚本文件 \`global_data.gd\`：\r
\r
\`\`\`gdscript\r
# global_data.gd\r
extends Node\r
\r
# 玩家的金币数量\r
var coins: int = 0\r
\r
# 玩家的等级\r
var player_level: int = 1\r
\r
func add_coins(amount: int):\r
    coins += amount\r
    print("获得 %d 金币，当前金币: %d" % [amount, coins])\r
\r
func level_up():\r
    player_level += 1\r
    print("升级！当前等级: %d" % player_level)\r
\`\`\`\r
\r
然后将这个脚本添加到自动加载列表中：\r
\r
1. 打开菜单栏：**项目 → 项目设置**\r
2. 在左侧选择 **全局 → 自动加载** 标签页\r
3. 点击路径输入框右侧的文件夹图标，选择刚才创建的 \`global_data.gd\` 文件\r
4. 在"节点名称"输入框中，输入一个全局访问的名称，例如 \`GlobalData\`\r
5. 点击 **添加** 按钮\r
\r
![](/images/tutorial/single/autoload_setting.png)\r
\r
完成这些步骤后，\`GlobalData\` 就成为了一个全局单例节点。\r
\r
!!! tip 节点名称规范\r
节点名称建议使用大驼峰命名法（PascalCase），如 \`GlobalData\`、\`GameManager\`、\`EventBus\` 等。这样可以与普通变量区分开来，一眼就能看出这是一个全局单例。\r
!!!\r
\r
### 访问自动加载节点\r
\r
设置好自动加载后，就可以在项目的任何脚本中直接通过节点名称访问它了：\r
\r
\`\`\`gdscript\r
# player.gd\r
extends CharacterBody2D\r
\r
func _ready():\r
    # 直接通过名称访问自动加载节点\r
    print("游戏开始时的金币: %d" % GlobalData.coins)\r
\r
func collect_coin():\r
    # 调用自动加载节点的方法\r
    GlobalData.add_coins(1)\r
\r
func defeat_boss():\r
    GlobalData.add_coins(100)\r
    GlobalData.level_up()\r
\`\`\`\r
\r
\`\`\`gdscript\r
# shop.gd\r
extends Control\r
\r
@onready var coin_label: Label = %CoinLabel\r
\r
func _ready():\r
    # 在商店界面显示玩家的金币\r
    update_coin_display()\r
\r
func update_coin_display():\r
    coin_label.text = "金币: %d" % GlobalData.coins\r
\r
func buy_item(price: int):\r
    if GlobalData.coins >= price:\r
        GlobalData.coins -= price\r
        update_coin_display()\r
        print("购买成功！")\r
    else:\r
        print("金币不足！")\r
\`\`\`\r
\r
可以看到，无论是在玩家场景还是商店场景中，都可以直接使用 \`GlobalData\` 访问同一个数据。场景切换时数据不会丢失，因为 \`GlobalData\` 节点始终存在于场景树的根部，不会被销毁。\r
\r
### 自动加载场景\r
\r
除了脚本，我们也可以将一个**场景**设置为自动加载。这在某些情况下很有用，比如创建一个全局的音频管理器。\r
\r
首先创建一个场景 \`audio_manager.tscn\`，结构如下：\r
\`\`\`\r
AudioManager (Node)\r
├─ BGMPlayer (AudioStreamPlayer)\r
└─ SFXContainer (Node)\r
\`\`\`\r
\r
为根节点添加脚本 \`audio_manager.gd\`：\r
\r
\`\`\`gdscript\r
# audio_manager.gd\r
extends Node\r
\r
@onready var bgm_player: AudioStreamPlayer = $BGMPlayer\r
@onready var sfx_container: Node = $SFXContainer\r
\r
func play_bgm(music: AudioStream):\r
	bgm_player.stream = music\r
	bgm_player.play()\r
\r
func stop_bgm():\r
	bgm_player.stop()\r
\r
func play_sfx(sound: AudioStream):\r
	var sfx_player = AudioStreamPlayer.new()\r
	sfx_player.stream = sound\r
	sfx_player.finished.connect(sfx_player.queue_free)\r
	sfx_container.add_child(sfx_player)\r
	sfx_player.play()\r
\r
\`\`\`\r
\r
然后在项目设置中添加这个**场景文件** \`audio_manager.tscn\` 为自动加载，节点名称设为 \`AudioManager\`。\r
\r
这样，即使场景切换，背景音乐也会持续播放，因为 \`AudioManager\` 及其子节点不会被销毁。\r
\r
\`\`\` ::close 脚本 vs 场景：如何选择？\r
**使用脚本作为自动加载：**\r
- 仅需要存储数据和逻辑，不需要子节点\r
- 代码结构简单，易于维护\r
- 例如：全局数据存储、游戏设置、事件总线\r
\r
**使用场景作为自动加载：**\r
- 需要包含子节点（如多个AudioStreamPlayer、Timer等）\r
- 需要在编辑器中可视化配置节点属性\r
- 需要使用场景树的层级结构\r
- 例如：音频管理器、UI层管理、调试工具面板\r
\r
大多数情况下，简单的脚本就足够了。只有在确实需要节点树结构时，才使用场景。\r
\`\`\`\r
\r
### 自动加载的原理\r
\r
自动加载的本质是在游戏启动时实例化节点并添加到 \`/root\` 下：\r
\r
- **脚本文件**：Godot根据 \`extends\` 关键字创建对应类型的节点，然后附加脚本\r
  \`\`\`gdscript\r
  # extends Node → 创建 Node 节点并附加脚本\r
  # extends Control → 创建 Control 节点并附加脚本\r
  \`\`\`\r
\r
- **场景文件**：直接实例化场景\r
\r
### 自动加载的生命周期\r
\r
- **创建时机**：游戏启动时，主场景加载之前，按项目设置中的顺序创建\r
- **生命周期**：整个游戏运行期间都存在，不会随场景切换而销毁\r
- **销毁时机**：游戏退出时销毁\r
\r
可以通过以下代码验证：\r
\r
\`\`\`gdscript\r
# global_data.gd\r
extends Node\r
\r
func _ready():\r
    print("GlobalData 节点已创建")\r
    print("场景树中的位置: ", get_path())  # 输出: /root/GlobalData\r
\r
func _exit_tree():\r
    print("GlobalData 节点被销毁")\r
\`\`\`\r
\r
运行游戏后会发现，无论如何切换场景，\`_ready()\` 只会在游戏启动时调用一次，\`_exit_tree()\` 只会在游戏退出时调用一次。\r
\r
!!! warning 注意自动加载的顺序\r
如果有多个自动加载节点，并且它们之间存在依赖关系，需要注意它们在项目设置中的顺序。\r
\r
例如，如果 \`EventBus\` 需要使用 \`GlobalData\`，那么 \`GlobalData\` 应该排在 \`EventBus\` 之前。因为在 \`EventBus\` 的 \`_ready()\` 函数中，排在它前面的自动加载节点已经准备好了，而排在它后面的可能还没有初始化。\r
!!!\r
\r
## 使用静态方法的单例模式\r
\r
并非所有的全局功能都需要使用自动加载。如果只是一些**纯逻辑的工具函数**，不需要保存状态、不需要访问节点树，那么使用**静态方法**会是更好的选择。\r
\r
### 创建静态工具类\r
\r
使用 \`class_name\` 定义一个全局类，并使用 \`static\` 关键字定义静态方法：\r
\r
\`\`\`gdscript\r
# utils.gd\r
class_name Utils\r
\r
# 格式化时间戳\r
static func format_time(seconds: int) -> String:\r
    var minutes = seconds / 60\r
    var secs = seconds % 60\r
    return "%02d:%02d" % [minutes, secs]\r
\r
# 随机选择数组元素\r
static func random_choice(array: Array):\r
    if array.is_empty():\r
        return null\r
    return array[randi() % array.size()]\r
\`\`\`\r
\r
定义后可以在任何地方直接使用，**无需添加到自动加载**：\r
\r
\`\`\`gdscript\r
# player.gd\r
func _ready():\r
    var formatted_time = Utils.format_time(125)  # 直接调用\r
    print("游戏时间: ", formatted_time)  # 输出: 02:05\r
\`\`\`\r
\r
### 静态方法 vs 自动加载\r
\r
| 特性 | 静态方法 | 自动加载 |\r
|------|---------|---------|\r
| **保存状态** | ❌ 不能 | ✅ 可以 |\r
| **使用信号** | ❌ 不能 | ✅ 可以 |\r
| **访问节点树** | ❌ 不能 | ✅ 可以 |\r
| **适用场景** | 纯函数、工具类 | 数据管理、事件总线 |\r
\r
!!! tip 如何选择\r
- **静态方法**：工具函数、数学计算、格式转换等无状态逻辑\r
- **自动加载**：需要保存数据、发送信号、访问节点树的场景\r
!!!\r
\r
两种方式可以组合使用，例如自动加载节点可以调用静态工具类的方法。\r
\r
## 场景级别的单例模式\r
\r
有时我们需要在某个场景内保证某个节点只有一个实例，但不需要全局存在。这种情况可以使用**场景级别的单例模式**。\r
\r
\`\`\`gdscript\r
# level_manager.gd\r
class_name LevelManager\r
extends Node\r
\r
# 静态变量保存单例实例\r
static var instance: LevelManager = null\r
\r
func _ready():\r
    # 如果实例已存在，销毁当前节点\r
    if instance != null:\r
        push_warning("当前节点树中已经存在单例实例")\r
        queue_free()\r
        return\r
    # 否则将自己设为实例\r
    instance = self\r
\r
func _exit_tree():\r
    # 节点被销毁时清空实例引用\r
    if instance == self:\r
        instance = null\r
\`\`\`\r
\r
这种模式的特点：\r
- 在场景运行期间只允许存在一个实例\r
- 如果尝试创建第二个实例，会自动销毁\r
- 可以通过 \`LevelManager.instance\` 访问当前实例\r
- 场景切换时实例会被清空，不会全局持久化\r
\r
**使用示例：**\r
\r
\`\`\`gdscript\r
# 在其他脚本中访问单例\r
func _ready():\r
    if LevelManager.instance:\r
        LevelManager.instance.start_level()\r
    else:\r
        push_warning("LevelManager 实例不存在")\r
\`\`\`\r
\r
!!! tip 使用场景\r
适合用于在**单个关卡或场景内**需要唯一存在的管理器，例如：\r
- 关卡管理器（LevelManager）\r
- 波次生成器（WaveSpawner）\r
- 场景内的UI管理器\r
\r
如果需要跨场景持久化，应该使用自动加载而不是场景级单例。\r
!!!`,C=`---\r
title: 瓦片地图详解\r
author: \r
  - Goodman\r
  - 陌上竹\r
date: 2025-10-23\r
category:\r
  - 教程\r
tag:\r
  - 瓦片地图\r
---\r
\r
## 简介\r
\r
图块地图（Tile Map）是Godot中功能强大的2d地图制作节点，由于TileMap在Godot4.5已废弃，且与TileMapLayer使用其实差异不大，故本篇主要讲解 TileMapLayer。本篇为快速教程，指在方便用户快速上手TileMapLayer。\r
\r
!!! note 在Godot 4.3之后的版本添加了\`TilemapLayer\`替代原有的\`Tilemap\`节点，使每层瓦片地图都可以使用不同的\`tileset\`以及物理数据等，解决了之前版本中\`Tilemap\`在某些复杂场景下的局限性。\r
\r
在本教程中，如果没有特殊说明，以下知识默认均使用\`TilemapLayer\`讲解。\`Tilemap\`与\`TilemapLayer\`除了API有部分区别外，其余部分均可通用。\r
!!!\r
## 基本概念\r
- **瓦片地图（Tilemap）**: 就像是一张巨大的拼图画板，你可以在上面摆放各种瓦片来构建游戏世界。比如制作一个RPG游戏的地图，草地、道路、房屋都摆放在这个画板上。\r
- **瓦片地图层（TilemapLayer）**: 就像Photoshop中的图层概念，每一层都是独立的画布。比如制作游戏场景时，你可以把地面放在底层，装饰物放在中层，云朵放在顶层，这样方便管理和修改。多个TilemapLayer可以堆叠使用，实现前景、中景、背景的分层效果。\r
- **瓦片集合（TileSet）**: 相当于你的素材库或工具箱，里面装着所有可以使用的瓦片资源。就像乐高玩具盒，里面有各种形状、颜色的积木供你选择。\r
- **图集（AtlasSource）**: 一张大图片，上面整齐排列着许多小瓦片素材。就像一本贴纸册，把所有地形素材（草地、石头、水面等）都印在一张大纸上，使用时按坐标裁剪。\r
- **场景集合（Scene Collection）**: 预先制作好的场景模板集合。比如一棵完整的树（包含树干、树冠、阴影等多个节点），可以直接作为瓦片重复使用。\r
- **瓦片（Tile）**: TileMap中的最小单位，就像拼图中的一小块。每个瓦片可以是一块草地、一块砖墙，或者一个装饰物。\r
- **瓦片数据（Tile Data）**: 每个瓦片携带的详细信息，包括它的图片位置、是否有碰撞、能否行走等。就像产品说明书，记录着这块瓦片的所有属性。\r
![Tilemap相关类关系图](/images/tutorial/tilemap/tilemap_classes.png)\r
\r
### 代码常用API\r
#### TilemapLayer\r
对于TilemapLayer节点，有以下属性及方法可以方便的获取或设置数据。\r
- 属性\r
	- tileset: TileSet \r
		获得TilemapLayer设置的tileset\r
- 方法\r
	- \`TileData get_cell_tile_data(coords: Vector2i)\`\r
		用于获取某个坐标瓦片的瓦片数据tile_data，大部分的功能都要围绕这个API展开\r
	- \`void _tile_data_runtime_update(coords: Vector2i, tile_data: TileData)\`\r
		虚函数，重写可在游戏中修改tile的tile_data\r
	- \`bool _use_tile_data_runtime_update(coords: Vector2i)\`\r
		虚函数，重写可用于激活在游戏中修改tile_data\r
	- \`Vector2i get_neighbor_cell(coords: Vector2i, neighbor: CellNeighbor)\`\r
		用于获取某个坐标的瓦片的相邻瓦片，对于某些异形地图处理瓦片遍历很有用\r
	- \`Array[Vector2i] get_used_cells()\`\r
		获取地图中所有瓦片的坐标\r
	- \`Rect2i get_used_rect()\`\r
		获取地图所有包围瓦片的包围矩形\r
	- \`Vector2 map_to_local(map_position: Vector2i)\`\r
		从地图坐标转换为本地坐标，配合local_to_map可以快速的坐标转换操作\r
	- \`Vector2i local_to_map(local_position: Vector2)\`\r
		从本地坐标转换为地图坐标，配合map_to_local可以快速的坐标转换操作\r
	- \`void set_cell(coords: Vector2i, source_id: int = -1, atlas_coords: Vector2i = Vector2i(-1, -1), alternative_tile: int = 0)\`\r
		在对应的坐标设置对应的瓦片，自动生成地图需要大量的使用本API，source_id和atlas_coords应从tile_set中获取\r
	- \`void set_cells_terrain_connect(cells: Array[Vector2i], terrain_set: int, terrain: int, ignore_empty_terrains: bool = true)\`\r
		更新cells中所有的瓦片为terrain_set中的terrain地形。并按照规则连接地形\r
#### TileSet\r
对于瓦片集，虽然这个对象很重要，但是在正常使用下，几乎不需要考虑其中的API。维护tileset的工作一般在引擎中进行。\r
如果你需要自己开发有关Tileset的插件，则需要使用它的函数。请自行在[官方文档-TileSet](https://docs.godotengine.org/zh-cn/4.x/classes/class_tileset.html#class-tileset)中查阅。\r
- 属性\r
	- tile_size: Vector2i 用于设置瓦片的大小\r
- 方法\r
	如上，不再介绍\r
\r
#### TileData\r
瓦片信息中存储着瓦片的数据，默认情况下，某一个tile在tilemapLayer中不管使用多少次，仍使用的是同一个tile_data。如果修改这个tile_data会影响到所有的这个瓦片。如果需要单独修改某个瓦片，应使用\`_tile_data_runtime_update\`和\`_use_tile_data_runtime_update\`做特殊处理。\r
\r
瓦片数据涉及的属性和方法基本都是插件才需要用到的。以下仅展示比较重要的部分。\r
\r
可以判断瓦片地图是否为空来判断瓦片地图的某个位置是否有瓦片\r
\r
- 属性\r
	- material: Material\r
		材质，可以设置shader等\r
	- modulate: Color\r
		叠加的颜色调制\r
- 方法\r
	- \`Variant get_custom_data(layer_name: String)\`\r
		获取自定义数据\r
	- \`void set_custom_data(layer_name: String, value: Variant)\`\r
		设置自定义数据\r
\r
\r
## 使用瓦片地图\r
下面我们将从几方面分别讲解如何使用瓦片地图\r
\r
### 构建地图\r
1.创建TileMaplayer节点（可根据需求添加多个TileMapLayer节点）\r
![步骤一](/images/tutorial/tilemap/add-tilemap-node.png){style=width:50%}\r
2.创建的一个新的TileSet（在TileMap节点的检查器中），然后选中某一个瓦片地图，即可在下方看到TileSet面板和TileMap面板，其中TileSet面板是用来设置TileSet中的数据的，而TileMap面板则是选取Tile并在场景中绘制的。选中TileSet面板，并在图块源中拖入一个图集图片，会弹出一个弹框询问是否按照TileSet设置的大小自动生成瓦片，可以选择同意。如果不自动生成，可以在设置中点选暗色的瓦片激活。生成的瓦片才可以绘制到Tilemap中。\r
!!! note 鼠标点击可以创建一个瓦片。\r
按住ctrl并鼠标拖动可以快速创建多个瓦片。\r
按住shift并鼠标拖动可以快速创建一个更大的瓦片。\r
!!!\r
![步骤二](/images/tutorial/tilemap/tilesetpanel.png){style=width:50%}\r
3.选择TileMap面板，然后选中一个瓦片，确保当前是选择工具而非移动工具，然后在页面中开始绘制。默认情况下鼠标左键绘制，右键删除。\r
![输入图片说明](/images/tutorial/tilemap/tilemap-panel.png){style=width:50%}\r
\r
### 地形\r
地形系统就像是给瓦片贴上标签，将具有相同属性的瓦片（比如都是草地、都是水域）归类为同一种地形。设置地形后，当你绘制地图时，这些瓦片会自动连接和匹配，不需要手动一个个调整边缘，大大提高了绘制效率。比如制作一片草地时，只需要选中草地地形工具涂抹，系统就会自动帮你选择合适的草地瓦片并处理好边界过渡。\r
\r
#### 自动地形设置方法\r
1.打开TileMapLayer的检查器，在TileSet的地形设置（TerrainSets）添加一个地形集，然后再添加一个地形，注意设置Mode为Match Corners（不同的匹配模式适合不同的纹理集，此处使用匹配角举例），同一个地形集使用的是同一种匹配模式。\r
![输入图片说明](/images/tutorial/tilemap/terrainset.png){style=width:50%}\r
2.在TileSet中，选择绘制属性/地形/选择创建好的地形，在素材中进行绘制\r
!!! note **绘制**面板可以同时设置多个瓦片的属性，**选择**面板只可以设置单个瓦片的属性。\r
!!!\r
![输入图片说明](/images/tutorial/tilemap/tileset-draw-terrain.png){style=width:50%}\r
3.回到TileMap面板中，选择地形，然后在场景中使用画笔进行绘制，此时可以发现，图块已经可以链接到一起了。\r
![输入图片说明](/images/tutorial/tilemap/use-tileset-terrain.png){style=width:50%}\r
\r
#### 地形原理\r
地形系统的核心工作原理可以理解为"智能拼图识别"。每个瓦片都携带了一张"身份卡"（地形位掩码），记录着它四周（上下左右及四个角）分别连接着什么类型的地形。\r
\r
当你在地图上绘制时，系统会实时检查当前瓦片周围的地形情况，就像拼图时会看相邻的拼图片边缘是否匹配一样。比如一个草地瓦片，它的上方是草地、右侧是水域，系统就会自动选择一个"上边是草地、右边是水域"的边界瓦片来放置，保证地形的自然过渡。\r
\r
**匹配模式**决定了系统如何判断"匹配"：\r
- **Match Corners（匹配角）**：主要关注四个角的连接情况，适合45度角的纹理或需要精确角点过渡的场景\r
![匹配角示例](/images/tutorial/tilemap/match_corner_example.png){style=width:50%}\r
\r
- **Match Sides（匹配边）**：主要关注上下左右四条边的连接，适合简单的矩形瓦片\r
![匹配角示例](/images/tutorial/tilemap/match_sides_example.png){style=width:50%}\r
\r
- **Match Corners and Sides（匹配角和边）**：同时考虑角和边，提供最精确的匹配，适合复杂的地形纹理\r
![匹配边和角示例](/images/tutorial/tilemap/match_corner_and_sides_example.png){style=width:50%}\r
![匹配边和角](/images/tutorial/tilemap/match_corner_and_sides.png){style=width:50%}\r
\r
由上面的示例中可以发现，匹配边和角可以展示出最复杂的地形，其缺点就是对美术要求较高。具体使用哪种地形可以根据已有的图集情况选择。\r
\r
通过这种自动匹配机制，你只需要像涂色一样大面积绘制，系统就能智能地为你选择正确的边界瓦片，避免了手动拼接的繁琐工作。\r
\r
### 物理\r
物理系统就像是给瓦片地图添加"无形的墙"和"禁区标识"。在游戏中，玩家不能穿墙而过，角色不能掉出地图边界，这些都需要通过物理碰撞来实现。TileMap的物理系统让你可以直接在瓦片上绘制碰撞形状，比如给墙壁、岩石、地图边界等瓦片设置碰撞体，角色在移动时就会自动被这些"无形的墙"阻挡。这样你就不需要手动创建大量的碰撞体节点，大大简化了场景搭建的工作。\r
\r
首先需要在tileset中添加一个物理层。并设置物理层和物理遮罩层。需要的话也可以设置物理材质。\r
![物理层](/images/tutorial/tilemap/tilemap_physics_layer.png)\r
然后在TileSet面板中切换到绘制面板，选择刚才新建的物理层，在下方添加形状后点击瓦片开始绘制。物理形状可以有多个。尽量不要重叠。\r
![绘制物理形状](/images/tutorial/tilemap/tilemap_physics_draw.png)\r
\r
!!! warning\r
物理绘制必须像素对齐。否则可能会导致玩家在场景中移动时卡在某个位置无法移动。\r
可以点开绘制旁边的**栅格吸附**。选择**半像素吸附**或**栅格吸附**(推荐)，即可获得更加准确的定位。\r
!!!\r
以下是一个绘制地图边界的示例\r
![物理示例](/images/tutorial/tilemap/tilemap_physics.png){style=width:50%}\r
以下是一个绘制不可穿过的地形的示例\r
![物理示例2](/images/tutorial/tilemap/tilemap_physics2.png){style=width:50%}\r
\r
在绘制时，还可以为形状设置不同的属性，例如是否单向碰撞等。\r
\r
### 导航\r
2D导航可以参照教程[导航系统以及A星算法](/tutorial/navigation)，接下来介绍使用TilemapLayer构建导航区域。\r
![导航层](/images/tutorial/tilemap/tilemap_physics_draw.png)\r
\r
创建导航层之后，即可像是物理那样在绘制中绘制导航层形状。\r
![导航示例](/images/tutorial/tilemap/tilemap_navigation_example.png){style=width:50%}\r
\r
绘制导航层后，即可像使用NavigationRegion2D那样开始使用2D导航了。\r
\r
!!! warning 需要注意的是，由于Tilemap的导航层不可以烘焙，所以无法使用根据StaticBody来动态的烘焙导航区域。请根据需求自行选择方案。\r
\r
此处提供另一种方案，即为TilemapLayer添加分组，并使NavigationRegion2D根据分组烘焙导航区域。\r
![烘焙导航区域](/images/tutorial/tilemap/tilemap_bake_navation_region.png){style=width:50%}\r
\r
!!!\r
\r
### 光照\r
光照系统就像是给瓦片地图添加"光影魔法"。在游戏中，墙壁可以阻挡光线形成阴影，营造出真实的明暗效果。TileMap的光照遮挡系统让你可以直接在瓦片上绘制遮挡形状，就像使用LightOccluder2D节点一样，为游戏场景添加丰富的光影效果，增强视觉层次感。\r
\r
首先需要在TileSet的Rendering（渲染）设置中找到Occlusion Layers（遮挡层），新建一个遮挡层。\r
\r
然后在TileSet面板中切换到绘制面板，选择刚才新建的遮挡层，在下方添加遮挡形状后点击瓦片开始绘制。遮挡形状定义了哪些区域会阻挡光线，从而产生阴影效果。\r
\r
绘制完成后，场景中的Light2D节点就会根据这些遮挡形状自动计算阴影，就像使用LightOccluder2D节点一样。你可以为不同的瓦片设置不同的遮挡形状，比如墙壁需要完全遮挡光线，而栅栏可能只需要部分遮挡。\r
\r
!!! note 遮挡层的使用方式与LightOccluder2D完全相同，如果你熟悉LightOccluder2D的使用方法，那么在TileMap中使用遮挡层会非常容易上手。\r
如果绘制完成后发现没有出现遮挡，需要开启光线节点的\`shadow_enable\`属性\r
!!!\r
\r
![光照示例](/images/tutorial/tilemap/tileset-light.png){style=width:50%}\r
\r
### Y排序\r
在使用瓦片地图时，往往会出现瓦片大小超出网格的情况，此时需要离相机更近的瓦片盖住离相机更远的瓦片，因为是2D环境下，所以通常会使用瓦片所在的Y坐标作为渲染前后的依据，即：Y坐标更大的瓦片会盖住Y坐标更小的瓦片。\r
\r
Godot中开启Y坐标排序的方法很简单，只需要在节点上勾选\`Ordering\`中的\`Y Sort Enabled\`即可。开启选项之后，会将这个节点的**所有子节点**按照Y坐标大小排序。\r
\r
我们继续使用案例来讲解，这是一个没有开启Y排序的地图，可以看到，虽然所有图块都在同一层，但是出现了莫名奇妙的重叠现象。\r
\r
![关闭Y排序示例](/images/tutorial/tilemap/tileset_with_no_ysort.png){style=width:50%}\r
\r
而打开Y排序后，无需任何其他操作，Y坐标更大的下方的瓦片就会自然的盖住上方的瓦片。\r
![开启Y排序示例](/images/tutorial/tilemap/tileset_with_ysort.png){style=width:50%}\r
\r
### 场景合集\r
瓦片图集通常可以满足大部分的需求，但有时我们会遇到更复杂的场景。\r
类似希望能将一个Area2D像瓦片那样可以在网格上放置。此时我们就需要场景合集功能。\r
\r
首先在TileSet面板中点击下方的加号\r
然后选择场景合集，然后上方即可出现一个场景合集，选中它之后，即可看到一个空的场景合集。\r
\r
![场景合集](/images/tutorial/tilemap/tilemap_scene_collection.png){style=width:50%}\r
\r
然后将需要作为场景合集的场景拖入到场景合集中，然后切换到TileMap面板，即可像正常绘制的瓦片那样绘制场景。\r
\r
![使用场景合集](/images/tutorial/tilemap/tilemap_add_scene.png){style=width:50%}\r
\r
\r
## 具体案例\r
### 案例1：防止相机移出地图范围\r
\r
​基于地图信息的游戏逻辑：利用获取到的地图信息来决定游戏行为。一个典型的应用是**设置相机边界**，防止相机移出地图范围\r
\r
1.添加TileMapLayer节点和玩家相机节点\r
2.设置相机边界逻辑\r
\r
完整代码如下：\r
\r
\`\`\`gdscript\r
# 1. 添加TileMapLayer节点和玩家相机节点\r
@onready var camera_2d: Camera2D = $player/Camera2D\r
@export var tile_map: TileMapLayer\r
\r
# 2. 设置相机边界逻辑\r
func _ready() -> void:\r
	# 获取TileMap节点中所有已被放置图块所占据的矩形区域\r
	# grow(-1)为缩进一格 为避免在相机视口内露出地图边缘\r
	var used := tile_map.get_used_rect().grow(-1)\r
\r
	# 获取 TileMap 一个图块的尺寸\r
	var tile_size := tile_map.tile_set.tile_size\r
\r
	# 根据TileMap地块尺寸 限制相机的移动\r
	# 将网格坐标表示的边界转换为像素坐标表示的边界\r
	# 2d相机的最高/低点\r
	camera_2d.limit_top = used.position.y * tile_size.y\r
	camera_2d.limit_bottom = used.end.y * tile_size.y\r
\r
	# 2d相机的最左/右点\r
	camera_2d.limit_left = used.position.x * tile_size.x\r
	camera_2d.limit_right = used.end.x * tile_size.x\r
\r
	# 重置相机的平滑过渡，防止玩家初始位置在限制区域外时产生镜头抖动\r
	camera_2d.reset_smoothing()\r
\`\`\`\r
### 案例2：鼠标点击添加图块\r
通过获取TileMap的瓦片位置坐标来修改地图效果，制作出游戏中实时添加地图图块的效果\r
\r
1.添加需要修改的TileMapLayer节点引用\r
2.在func _input()中设置好输入映射（鼠标点击动作），获取鼠标点击位置，再转换为瓦片位置\r
3.定位要添加的瓦片素材坐标位置（图片ID+图集坐标）\r
4.使用set_cell函数将瓦片在运行时添加到TileMap中\r
5.拓展：添加鼠标右击清除图块的效果\r
\r
完整代码如下：\r
\r
\`\`\`gdscript\r
# 1. 添加TileMapLayer节点引用\r
@onready var tile_map_layer: TileMapLayer = $TileMapLayer\r
\r
# 2. TileSet中图片素材的id\r
var source_id : int = 0 \r
# TileSet中图片素材的坐标位置\r
var atlas_coord : Vector2i = Vector2i(0,0)\r
\r
func _input(event):\r
	# 当鼠标左键点击，执行以下操作\r
	if Input.is_action_just_pressed("click"):\r
		# 获取鼠标点击的全局位置\r
		var mouse_pos : Vector2 = get_global_mouse_position()\r
		# 转换鼠标全局为瓦片位置\r
		var tile_mosuse_pos : Vector2i = tile_map_layer.local_to_map(mouse_pos)\r
		# 使用set_cell将瓦片添加到TileMap中\r
		tile_map_layer.set_cell(tile_mosuse_pos , source_id , atlas_coord)\r
	\r
	# 拓展：鼠标右击清除图块\r
	if Input.is_action_just_pressed("right_click"):\r
		var mouse_pos : Vector2 = get_global_mouse_position()\r
		var tile_mosuse_pos : Vector2i = tile_map_layer.local_to_map(mouse_pos)\r
		# 将source_id设置为-1即可清除瓦片\r
		tile_map_layer.set_cell(tile_mosuse_pos , -1 , atlas_coord)\r
\`\`\`\r
注意\r
1.为确保类型安全可以在var 声明这个变量的类型\r
2.tile_mosuse_posw瓦片位置变量类型为Vector2i区别于Vector2，所以需要转化 \r
\r
\r
### 案例3：动态切换瓦片数据实现隐藏地图效果\r
接下来我们创建一个游戏，然后创建三层瓦片地图，一层是背景，一层是无法穿过的墙，一层是当达到条件是就会消失的墙。\r
并添加一个玩家，这里代码比较简单且无关，不再赘述。场景节点如下。Area2D盖住的部分就是会消失的墙。\r
\r
![使用场景合集](/images/tutorial/tilemap/example3-1.png){style=width:50%}\r
\r
目前由于物理形状的存在，所以所有的墙都不能穿过。接下来，我们要为MaskLayer添加一个脚本，当Area2D检测到玩家时，就将瓦片的物理关闭，并且透明度变成0.3。展示出内部的山洞并且允许玩家进入。\r
\r
\`\`\`gdscript\r
## mask_layer.gd\r
\r
extends TileMapLayer\r
\r
# 记录玩家是否进入\r
var player_entered: bool = false\r
\r
func _on_area_2d_body_entered(_body: Node2D) -> void:\r
	player_entered = true\r
	notify_runtime_tile_data_update()\r
\r
func _on_area_2d_body_exited(_body: Node2D) -> void:\r
	player_entered = false\r
	notify_runtime_tile_data_update()\r
\r
# 是否更新瓦片信息\r
func _use_tile_data_runtime_update(_coords: Vector2i) -> bool:\r
	# 因为玩家离开后会慢慢恢复不透明，所以所有的瓦片都需要更新\r
	return true\r
\r
# 隐藏层的透明度\r
var layer_alpha = 1.0\r
\r
# 更新瓦片数据\r
func _tile_data_runtime_update(_coords: Vector2i, tile_data: TileData) -> void:\r
	if player_entered:\r
		tile_data.set_collision_polygons_count(0, 0)\r
\r
# 根据玩家进入状态修改透明度\r
func _process(delta: float) -> void:\r
	if player_entered:\r
		if layer_alpha > 0.3:\r
			layer_alpha = move_toward(layer_alpha, 0.3, delta)\r
			set_modulate(Color(1, 1, 1, layer_alpha))\r
	else:\r
		if layer_alpha < 1.0:\r
			layer_alpha = move_toward(layer_alpha, 1.0, delta)\r
			set_modulate(Color(1, 1, 1, layer_alpha))\r
\r
\`\`\`\r
\r
<video src="/images/tutorial/tilemap/example3-2.mp4" controls style="width:100%"></video>\r
\r
**代码实现要点：**\r
\r
这个案例的核心思路是让地图"活起来"——当玩家靠近时，墙壁会变得透明并允许穿过，就像游戏中的"透视墙"或"隐藏通道"效果。实现的关键步骤如下：\r
\r
1. **玩家检测**：就像在门口安装感应器一样，当玩家进入\`Area2D\`区域时，系统会收到信号，然后通知所有瓦片"该更新了！"。这里使用\`notify_runtime_tile_data_update()\`来触发更新。\r
\r
2. **移除碰撞**：当玩家进入区域后，原本阻挡玩家的墙壁需要"消失"。通过重写\`_tile_data_runtime_update()\`函数，把瓦片的碰撞形状清空（设置为0），这样玩家就可以自由穿过了。就像原本坚硬的墙壁突然变成了空气。\r
\r
3. **透明度动画**：为了让效果更自然，使用\`move_toward()\`让透明度平滑过渡。玩家靠近时，墙壁慢慢变透明（降到0.3），让玩家能隐约看到后面的内容；玩家离开时，墙壁又慢慢恢复不透明，营造出神秘感。\r
\r
4. **同步更新**：通过让\`_use_tile_data_runtime_update()\`返回\`true\`，确保整个图层的所有瓦片都能实时响应状态变化，而不是只有个别瓦片在更新。\r
\r
这种动态修改瓦片数据的技术非常实用，你可以用它制作探索类的隐藏区域、根据剧情触发的地形变化，或者像传送门那样可穿透的特殊墙壁效果。\r
\r
示例项目代码下载 [项目文件](/zips/tilemap-example.zip)\r
 \r
### 案例4：六边形寻路\r
\r
在策略类游戏中，我们经常需要实现类似"回合制移动"的效果——玩家每回合只能移动固定步数，并且需要在地图上高亮显示所有可以到达的位置。这个案例将展示如何在六边形网格地图上实现这个功能。\r
\r
想象一下你在玩一个战棋游戏，角色每回合有3步行动力。当你点击角色时，游戏会自动计算出所有在3步内可以到达的位置，并用不同颜色标记出来。这背后的原理就是**广度优先搜索（BFS）**——就像水波扩散一样，从起点开始一层一层向外探索，每一层代表一步移动。\r
\r
**实现思路：**\r
\r
1. **准备六边形网格地图**：首先需要创建一个使用六边形瓦片的TileMapLayer。六边形网格与方形网格不同，每个六边形有6个相邻位置，而且需要特殊的坐标转换算法来正确处理相邻关系。\r
![准备六边形网格地图](/images/tutorial/tilemap/example4-1.png){style=width:50%}\r
\r
2. **获取起点和步数限制**：确定玩家当前所在的六边形坐标，以及这一回合可以移动的最大步数（比如3步）。\r
\r
3. **使用广度优先搜索探索**：就像雷达扫描一样，从起点开始，先标记起点为"可以到达"，然后探索起点周围的所有邻居位置。每个探索到的位置会被标记为"可以到达"，并且记录它是从哪个位置到达的（就像在地图上做标记"我从这里来"）。接着继续探索这些邻居位置的邻居，一层一层向外扩散，直到达到步数限制。\r
\r
4. **记录路径信息**：在搜索过程中，我们需要用一个字典（Dictionary）来记录每个可到达位置的前一个位置。这样当玩家选择移动目标时，我们就能反向追踪路径，知道要经过哪些位置才能到达目的地。\r
![使用广度优先搜索探索并记录路径信息](/images/tutorial/tilemap/example4-2.png){style=width:50%}\r
\r
5. **高亮显示可到达区域**：搜索完成后，所有在步数限制内可以到达的位置都会被标记出来。你可以通过修改这些位置的瓦片颜色、添加高亮效果，或者绘制特殊标记来让玩家清楚地看到移动范围。\r
\r
6. **处理障碍物**：在搜索过程中，如果遇到障碍物（比如墙壁、其他角色占据的位置），就要跳过这个位置，不继续从这个位置向外探索。就像实际走路遇到墙，你不可能穿墙而过。\r
\r
7. **鼠标点击检测与路径获取**：当玩家点击地图上的某个位置时，需要将鼠标的屏幕坐标转换为六边形网格坐标。使用\`local_to_map()\`函数可以将鼠标点击的位置转换为对应的瓦片坐标。然后检查这个位置是否在之前搜索到的"可到达位置"列表中。如果这个位置是可以到达的，就可以开始获取移动路径了。\r
\r
8. **反向追踪路径**：由于我们在搜索时记录了每个位置的"前一个位置"，现在可以从目标位置开始，沿着"前一个位置"一直往回追溯，直到回到起点。这个过程就像沿着标记一路走回起点，最终得到一条从起点到目标的完整路径。注意，由于是从目标往回追溯，得到的路径是反序的，需要反转一下才能得到从起点到目标的正确顺序。\r
![鼠标点击检测与路径获取和反向计算路径](/images/tutorial/tilemap/example4-3.png){style=width:50%}\r
\r
9. **使用Tween平滑移动**：得到路径后，使用Godot的Tween节点或Tween类来实现平滑的移动动画。就像让角色沿着路径"滑行"过去，而不是瞬间传送。对于路径上的每个位置，使用\`map_to_local()\`将网格坐标转换为世界坐标，然后让Tween将玩家的位置从当前位置平滑过渡到下一个位置。每到达一个位置后，继续移动到下一个位置，直到到达目标。这样玩家就能看到角色一步一步沿着规划好的路径移动，而不是直接跳过去。\r
![鼠标点击检测与路径获取和反向计算路径](/images/tutorial/tilemap/example4-4.png){style=width:50%}\r
\r
\`\`\`gdscript ::close\r
extends Node2D\r
\r
# 1、准备六边形网格地图和玩家\r
@onready var tile_map_layer: TileMapLayer = $TileMapLayer\r
@onready var player: Node2D = $Player\r
@onready var marked_layer: TileMapLayer = $MarkedLayer\r
\r
# 2、获取起点和步数限制\r
var init_position: Vector2i = Vector2i(0, 0)\r
var step_limit: int = 3\r
\r
\r
var path_info: Dictionary = {} # 记录路径信息\r
func _ready() -> void:\r
	# 将玩家起点位置\r
	player.global_position = tile_map_layer.map_to_local(init_position)\r
	search_and_show_path(init_position)\r
\r
# 搜索并展示\r
func search_and_show_path(start_position: Vector2i):\r
	search_path(start_position)\r
	print("当前位置可以移动到的坐标: ", path_info)\r
	highlight_cells()\r
\r
# 3、使用广度优先搜索探索\r
# 4、记录路径信息\r
func search_path(start_position: Vector2i) -> void:\r
	# 清空路径信息\r
	path_info = {}\r
	# 记录路径信息\r
	path_info[start_position] = {\r
		"front_point": null,\r
		"step": 0\r
	}\r
	# 记录已访问位置\r
	var visited: Dictionary = {}\r
	visited[start_position] = true\r
\r
	# 记录待探索位置\r
	var queue: Array[Vector2i] = [start_position]\r
	while not queue.is_empty():\r
		var current_position: Vector2i = queue.pop_front()\r
		if path_info[current_position]["step"] >= step_limit:\r
			continue\r
		var surrounding_cells: Array[Vector2i] = tile_map_layer.get_surrounding_cells(current_position)\r
		for cell in surrounding_cells:\r
			# 如果cell没有tile_data，则跳过\r
			if tile_map_layer.get_cell_tile_data(cell) == null:\r
				continue\r
			# 如果cell已经访问过，则跳过\r
			if not visited.has(cell):\r
				visited[cell] = true\r
				# 记录待探索位置\r
				queue.append(cell)\r
				# 记录路径信息\r
				path_info[cell] = {\r
					"front_point": current_position,\r
					"step": path_info[current_position]["step"] + 1,\r
				}\r
\r
var marked_cell_source_id = 0\r
var marked_cell_coord = Vector2i(20, 0)\r
\r
# 5、高亮可以移动到的坐标\r
func highlight_cells():\r
	# 清空标记层\r
	marked_layer.clear()\r
	for cell_coord in path_info.keys():\r
		# 设置标记层\r
		marked_layer.set_cell(cell_coord, marked_cell_source_id, marked_cell_coord)\r
\r
func _input(event: InputEvent) -> void:\r
	# 7、当玩家鼠标点击时获取坐标并处理\r
	if event is InputEventMouseButton and event.button_index == MOUSE_BUTTON_LEFT and event.pressed:\r
		var mouse_pos = get_global_mouse_position()\r
		var map_coord = tile_map_layer.local_to_map(tile_map_layer.to_local(mouse_pos))\r
		print(map_coord)\r
		# 判断是否可以移动到对应位置\r
		if tile_map_layer.get_cell_tile_data(map_coord) != null and path_info.has(map_coord):\r
			# 8、反向获得路径\r
			var path: Array[Vector2i] = []\r
			var current: Vector2i = map_coord\r
			while path_info.has(current):\r
				# 将路径点添加到路径数组前方\r
				path.push_front(current)\r
				var front_point = path_info[current]["front_point"]\r
				if front_point == null:\r
					break\r
				current = front_point\r
\r
			print("移动路径: ", path)\r
			# 移动玩家到目标位置\r
			await move_player_along_path(path)\r
			# 更新起点并重新搜索路径\r
			search_and_show_path(map_coord)\r
\r
# 9、让玩家沿着路径移动\r
func move_player_along_path(path: Array[Vector2i]) -> void:\r
	if path.is_empty():\r
		return\r
	# 删除当前位置\r
	path.pop_front()\r
	# 更新玩家位置到目标点\r
	while not path.is_empty():\r
		var next_position: Vector2i = path.pop_front()\r
		var tween = get_tree().create_tween()\r
		tween.tween_property(player, "global_position", tile_map_layer.map_to_local(next_position), 1)\r
		await tween.finished\r
\r
\`\`\`\r
\r
<video src="/images/tutorial/tilemap/example4.mp4" controls style="width:100%"></video>\r
\r
**关键要点：**\r
\r
- **队列结构**：广度优先搜索需要一个队列（Queue）来存储待探索的位置，按照"先探索的先处理"的原则，保证每次都是先处理距离起点更近的位置。\r
\r
- **已访问标记**：为了避免重复探索同一个位置（防止绕圈子），需要记录哪些位置已经被访问过。\r
\r
- **步数计数**：每个位置都要记录它距离起点多少步，只有当步数小于等于限制步数时，才继续从这个位置向外探索。\r
\r
- **路径回溯**：通过记录"前一个位置"，当玩家点击目标位置时，可以从目标位置开始，沿着"前一个位置"一路回溯到起点，就能得到完整的移动路径。\r
\r
- **坐标转换**：在鼠标点击和移动过程中，需要频繁进行坐标转换。\`local_to_map()\`将世界坐标转换为网格坐标，用于判断点击的是哪个瓦片；\`map_to_local()\`将网格坐标转换为世界坐标，用于设置角色的实际位置。\r
\r
- **Tween动画链**：移动路径可能包含多个位置，需要让Tween依次执行。可以在每个Tween动画完成后，通过回调函数或者检查Tween的状态，触发下一个位置的移动，形成一条连贯的移动动画链。\r
\r
这种寻路方式非常适合回合制策略游戏，不仅能显示移动范围，还能在玩家选择目标后自动规划最优路径。通过Tween实现的平滑移动让游戏体验更加流畅自然。你可以在战棋游戏、RPG游戏的地图探索，或者任何需要限制移动范围的游戏中使用这个技术。\r
\r
示例项目代码下载 [项目文件](/zips/tilemap-hexagon-example.zip)\r
\r
## 参考视频\r
[如何使用 TileMap｜Godot 4 教程《勇者传说》#2_哔哩哔哩_bilibili](https://www.bilibili.com/video/BV1Yz4y1b7hz)\r
[Godot 4 TileMap 教程（1）设置、图层、动画_哔哩哔哩_bilibili](https://www.bilibili.com/video/BV17t42157si)\r
`;export{C as _,S as a,x as b,b as c,h as d,f as e,v as f,y as g,g as h,m as i,u as j,p as k,d as l,c as m,_ as n,s as o,l as p,o as q,i as r,a as s,t,r as u,e as v,n as w};
